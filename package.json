{
  "name": "hanes-mes",
  "version": "0.1.0",
  "private": true,
  "description": "HANES MES - Manufacturing Execution System for Wire Harness Production",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean && rimraf node_modules",
    "db:generate": "turbo db:generate",
    "db:push": "turbo db:push",
    "db:pull": "turbo db:pull",
    "db:seed": "turbo db:seed"
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "esbuild",
      "sharp",
      "unrs-resolver"
    ]
  }
}
