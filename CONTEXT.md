# HARNESS MES — 생산·자재 컨텍스트

와이어하네스(전선 배선) 제조의 작업지시·공정·생산실적·자재소비와 자재 수입검사(IQC)를 다루는 컨텍스트.

## Language

### 품목
**원자재**:
외부에서 구매하는 재료(전선, 터미널, 그로멧, 테이프 등).

**반제품(SG)**:
단일 회로배선 1단위. 여러 묶음에서 가닥/부품을 한 개씩 모아 완성한다. 모여서 완제품이 된다.

**완제품(FG)**:
최종 출하 제품.

### 라벨
**묶음 추적 라벨**:
키오스크 최초공정에서 같은 원자재(전선 등)를 N가닥으로 절단한 묶음을 식별하는 라벨. 가닥 수로 재고가 추적되며, 가닥 단위로 SG에 투입·소비된다. 반제품(SG)이 아니다.
_Avoid_: SG 라벨과 혼용

**SG 라벨**:
반제품 1단위를 식별하는 라벨.

**FG 라벨**:
완제품 1단위를 식별하는 라벨.

### 작업·공정
**작업지시**:
특정 품목을 만들기 위한 생산 지시. 작업지시번호가 단독 식별자이며, 여러 공정이 같은 작업지시를 공유한다. 각 공정은 같은 작업지시를 열어 자기 공정 실적만 입력한다.
_Avoid_: 공정별로 작업지시를 분할(PK에 공정 추가)하는 것

**공정**:
라우팅에 정의된 작업 단계(예: 절단, 검사, 그로멧, 통전검사).

**생산실적**:
한 공정에서 만든 수량 기록.

### 자재
**출고**:
창고 담당자가 원자재 창고에서 자재를 빼 공정재고(장착 대기)로 넣는 행위. 원자재 창고가 차감되고 공정재고가 증가한다. 출고 시점에는 설비가 정해지지 않는다.

**공정재고**:
원자재 창고에서 출고되어 공정에 입고된 자재 재고. **공정 단위**(위치=공정)이며 상태는 **장착 대기**다.

**설비재고(장착됨)**:
공정재고를 설비에 장착한 자재 재고. **설비 단위**(위치=설비)이며 실적 차감 대상이다.

**설비 장착**:
작업자가 공정재고(장착 대기)를 특정 설비에 올려 설비재고(장착됨)로 옮기는 것. 자재의 위치 이동이다.

**장착 대기 / 장착됨**:
공정재고의 두 상태(입고만 됨 / 설비에 올림). (현재 코드는 미구분 — 도입 예정)

**자재 차감**:
공정 실적 발생 시 그 공정에 장착된 자재를 재고에서 빼는 것.

**백플러시**:
실적 수량을 거꾸로 계산해 자재를 자동 차감하는 방식.
_Avoid_: AUTO_ISSUE(코드 내부 이름)

**자재 장착 / 반제품 장착**:
실적 입력 시 원자재(차감 대상)와 반제품(스캔 소비 대상)을 구분해 거는 것.

### 수입검사(IQC)
**입하 행**:
공급사가 납품한 자재를 받아 적은 기록 한 줄. `(입하번호, SEQ)` 두 값이 한 행을 특정하며, 입하번호 하나에 여러 행이 달린다.
_Avoid_: 입하번호(그건 행이 아니라 묶음 번호다), 입고

**자재 시리얼**:
개별 자재 묶음 하나를 식별하는 단위. 하나의 **입하 행**에 여러 개가 달릴 수 있다.
_Avoid_: 자재 LOT, LOT

**입고**:
수입검사를 통과한 **자재 시리얼**을 창고 재고로 올리는 행위. **입하**와 별개 단계다.
_Avoid_: 입하와 혼용

**검사의뢰**:
담당자가 같은 품목의 여러 **입하 행**을 하나의 **모집단**으로 묶어 검사를 요청한 건.
_Avoid_: 의뢰 LOT, 검사의뢰 LOT

**시료 / 대표**:
**검사의뢰**를 구성하는 **입하 행**의 두 역할. 시료=실제로 검사하는 행, 대표=검사하지 않고 시료의 판정을 함께 받는 행.

**모집단**:
AQL이 시료수와 합격판정개수(Ac/Re)를 뽑는 기준 수량. **검사의뢰** 확정 시점에 합의되어 고정된다.
_Avoid_: LOT 수량

**판정**:
한 **검사 단위**에 대해 AQL이 내린 합격/불합격 결정 한 건. 검사 단위가 무엇이든 판정은 1건이다.
_Avoid_: 검사(행위), 검사결과

**검사 단위**:
한 번의 **판정**이 덮는 범위. 현재 세 가지 — 자재 시리얼 단건 / 입하번호+품목 / **검사의뢰**.

**판정 대상**:
하나의 **판정**이 실제로 덮은 **입하 행**(또는 단건일 때 **자재 시리얼**)의 목록. 검사 단위가 무엇이든 같은 방식으로 기록되는 역추적의 정본이다.

**특채**:
불합격 판정을 받은 **자재 시리얼**을 예외적으로 **입고**할 수 있게 허용하는 결정.

## Relationships

- 하나의 **작업지시**는 여러 **공정**에서 공유되며, 각 **공정**이 자기 **생산실적**을 기록한다 (작업지시 PK는 작업지시번호 단독)
- 하나의 **설비**는 하나의 **공정**에 대응한다 (떼어낼 수 없는 복합 작업은 라우팅에서 1개 통합 공정으로 정의)
- **생산실적** 입력 시 선택한 **설비**가 **공정**을 결정하며, 그 공정이 작업지시의 라우팅에 있어야 한다(없으면 오류)
- 자재는 **원자재 창고 → 공정재고(공정 위치, 장착 대기) → 설비재고(설비 위치, 장착됨)** 로 위치를 이동하며, **차감은 설비재고에서만** 일어난다
- **출고**(창고 담당자)는 **원자재**를 원자재 창고에서 빼 **공정재고**(장착 대기)로 넣는다
- **설비 장착**(작업자)은 **장착 대기** 공정재고를 특정 설비의 **장착됨**으로 전환한다
- 각 **공정**의 **생산실적**은 그 공정에 **장착됨** 상태인 **공정재고**만 **차감**한다 (**장착 대기**는 차감 대상이 아님)
- **묶음 추적 라벨**(여러)의 가닥이 한 개씩 모여 **반제품(SG)** 1개(=단일 회로)가 된다
- **반제품(SG)** 여러 개가 모여 **완제품(FG)** 1개(=하네스)가 된다
- **묶음 추적 라벨**은 최초공정 산출물(가닥 단위로 소비되는 중간재)이며 **반제품(SG)**과 다르다
- 하나의 **입하 행**에는 여러 **자재 시리얼**이 달린다 (입하번호 하나에도 여러 **입하 행**이 달린다)
- 하나의 **검사의뢰**는 같은 품목의 여러 **입하 행**을 묶으며, 그중 최소 한 행이 **시료**이고 나머지는 **대표**다
- 하나의 **검사 단위**는 하나의 **판정**을 낳고, 그 **판정**은 여러 **판정 대상**을 가진다
- **판정**은 그 **판정 대상**에 속한 모든 **자재 시리얼**의 검사상태를 한꺼번에 바꾼다 (**대표**도 **시료**와 같은 결과를 받는다)
- **입고**는 합격 판정을 받았거나 **특채** 허가를 받은 **자재 시리얼**만 받는다
- **모집단**은 **검사의뢰** 확정 시점의 스냅샷이며, 판정 시점의 실제 수량과 갈라질 수 있다

## Flagged ambiguities

- "LOT"이 **자재 시리얼**(MAT_LOTS 한 행), **검사의뢰**(IQC_REQUEST_LOTS 한 건), **모집단**(LOT_QTY) 세 가지로 쓰임 — 해결: 셋 다 "LOT"이라 부르지 않는다. 테이블명은 그대로 두고 말과 신규 코드에서만 구분한다.
- "입하"와 "입고"가 혼용됨 — 해결: 별개 단계다. 입하=받아 적는 것, 입고=검사 통과 후 창고 재고로 올리는 것.
- **판정 대상**을 기록하는 정본이 없어 `IQC_LOGS.ARRIVAL_NO` 한 컬럼이 그 역할을 겸함 — **검사의뢰** 판정에서 **시료** 행 하나만 남고 **대표** 행을 잃는다. 해결: 판정-대상 매핑을 별도로 기록한다.
- "SG 라벨"이 키오스크 **묶음 추적 라벨**과 **반제품(SG, 회로)** 둘 다로 쓰임 — 도메인상 별개이나 현재 코드는 SG_LABELS 한 테이블로 통합(가닥 수·설비장착·소비가 한 곳에서, 단계/종류 구분 컬럼 없음). 분리 필요.

## Example dialogue

> **개발:** 키오스크에서 전선을 절단하면 SG 라벨이 나오나요?
> **도메인:** 아니요. 거긴 **묶음 추적 라벨**입니다. **반제품(SG)**은 여러 공정을 거쳐 회로배선이 완결됐을 때 나옵니다.

> **개발:** 검사의뢰 하나에 입하 행 3개를 묶으면 판정도 3건 나오나요?
> **도메인:** 아니요. **판정**은 1건입니다. **시료** 행만 실제로 검사하고 **대표** 행은 그 결과를 같이 받습니다. 대신 그 판정이 세 행을 다 덮었다는 사실은 **판정 대상**으로 남아야 합니다 — 안 남기면 나중에 대표 행에서 판정을 되찾을 수 없습니다.
