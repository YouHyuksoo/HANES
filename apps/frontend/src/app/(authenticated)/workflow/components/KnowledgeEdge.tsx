import { BaseEdge, EdgeLabelRenderer, getBezierPath, type EdgeProps } from "@xyflow/react";
import type { KnowledgeEdgeData } from "../knowledge/knowledge-view-model";
export function KnowledgeEdge(props: EdgeProps) { const data = props.data as (KnowledgeEdgeData & { condition?: string }) | undefined; const [path,x,y] = getBezierPath(props); const dashed = data?.kind === "branchesTo" || data?.kind === "raises" || data?.kind === "recoversWith"; const double = data?.kind === "requires" || data?.kind === "validates"; return <><BaseEdge {...props} path={path} markerEnd={props.markerEnd} style={{ ...props.style, strokeWidth: double ? 3 : 1.5, strokeDasharray: dashed ? "7 5" : undefined, opacity: data?.opacity }} /><EdgeLabelRenderer><div className="pointer-events-none absolute rounded-sm border border-border bg-card px-2 py-1 font-mono text-[10px] text-text" style={{ transform:`translate(-50%, -50%) translate(${x}px,${y}px)` }}>{String(props.label ?? data?.kind ?? "관계")}{data?.condition && <span className="block text-warning">조건: {data.condition}</span>}</div></EdgeLabelRenderer></>; }
