# AI 채팅 출처 표시 + 좋아요/싫어요/복사 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** AI 채팅 응답 하단에 RAG 출처를 인라인 확장 목록(클릭 시 도움말 패널 딥링크)으로 보여주고, 모든 응답에 복사·좋아요·싫어요 버튼을 추가한다(좋아요/싫어요는 Oracle DB에 저장).

**Architecture:** 백엔드 `AiSqlService.process()`가 이미 계산하는 RAG 청크를 모든 응답 경로에 `sources` 필드로 실어 보내도록 얇은 래퍼로 감싼다. 새 `AiChatFeedback` 엔티티/테이블 + `AiFeedbackService` + 2개 엔드포인트(`POST/DELETE /ai/chat/feedback`)로 피드백을 저장/삭제한다. 프론트는 `AiChatPanel`에 출처 접이식 목록·복사·좋아요/싫어요 버튼을 추가하고, `helpStore`/`HelpPanel`에 메뉴코드+섹션 오버라이드를 넣어 출처 클릭 시 도움말 패널이 해당 문서 섹션으로 스크롤 이동하게 한다.

**Tech Stack:** NestJS + TypeORM(Oracle) 백엔드, Next.js + Zustand + react-markdown 프론트, Oracle DDL은 `oracle-db` 커넥터로 직접 적용.

**참고 스펙:** `docs/superpowers/specs/2026-07-01-ai-chat-sources-feedback-design.md`

## Global Constraints

- 패키지 매니저는 `pnpm`(`pnpm.cmd`)만 사용한다. `npm` 금지.
- Oracle 채번은 SEQUENCE(또는 그 위에 얹힌 IDENTITY)만 사용한다 — `MAX(...)+1` 금지. 이 프로젝트는 로그/규칙성 테이블(예: `EQUIP_CONDITION_RULES.RULE_ID`)에 한해 `GENERATED BY DEFAULT AS IDENTITY`(TypeORM `@PrimaryGeneratedColumn`)를 예외적으로 허용하며, `AI_CHAT_FEEDBACKS`도 같은 성격(로그성 테이블)이라 동일 패턴을 쓴다.
- DDL은 SQL 파일 작성 후 `python C:/Users/hsyou/.claude/skills/oracle-db/scripts/oracle_connector.py --site JSHANES --execute-file <file>`로 직접 적용하고 pre/post 조회로 확인한다. SQL 파일만 만들고 끝내지 않는다.
- CREATED_AT 등 감사 컬럼은 `DEFAULT SYSTIMESTAMP`(DDL)로 채운다 — TypeORM Oracle이 INSERT 시 JS에서 채워주지 않으므로 DB 쪽 DEFAULT가 없으면 ORA-01400이 난다.
- 멀티테넌시: 신규 테이블/엔드포인트는 `COMPANY`/`PLANT_CD` 스코프를 반드시 포함한다.
- 사용자 식별(`CREATED_BY`)과 테넌트 추출은 `getRequestUser`(`apps/backend/src/common/utils/request-user.util.ts`) + `getHeaderString`(`apps/backend/src/common/utils/header-value.util.ts`) 조합을 재사용한다(`activity-log.controller.ts`의 `tenant()`/`extractUserId()` 패턴과 동일).
- 전역 `ValidationPipe`가 `whitelist:true, forbidNonWhitelisted:true`로 설정되어 있다(`apps/backend/src/main.ts`) — 모든 DTO 필드에 class-validator 데코레이터가 있어야 한다. 데코레이터 누락 필드는 요청에서 자동 제거/거부된다.
- UI 문자열은 `apps/frontend/src/locales/{ko,en,zh,vi}.json` 4개 파일을 항상 함께 수정한다(JSON에 UTF-8 BOM 금지).
- 이미 프론트(3002)/백엔드(3003) dev 서버가 떠 있으면 `pnpm build`를 실행하지 않는다. 검증은 아래 typecheck 명령으로 한다:
  ```
  pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false
  pnpm.cmd --filter @harness/backend exec tsc --noEmit --pretty false
  ```
- `apps/backend/src/modules/ai/` 모듈에는 기존 unit test가 없다(spec.ts 없음, LLM/DB 외부 의존 위주). 이 계획도 동일 컨벤션을 따라 새 테스트 프레임워크를 억지로 도입하지 않고 tsc + DB 조회 + 수동 스모크로 검증한다.
- 커밋은 main에 직접, 태스크 단위로 나눠서 한다(브랜치 불필요). `git add`는 파일 단위로 지정한다.

---

### Task 1: `AI_CHAT_FEEDBACKS` 테이블 마이그레이션 작성 및 적용

**Files:**
- Create: `apps/backend/src/migrations/2026-07-01_ai_chat_feedbacks.sql`

**Interfaces:**
- Produces: 테이블 `AI_CHAT_FEEDBACKS(FEEDBACK_ID PK IDENTITY, COMPANY, PLANT_CD, ROUTE, MENU_CODE, QUESTION CLOB, ANSWER CLOB, SOURCES_JSON CLOB, RATING, CREATED_BY, CREATED_AT)` — Task 2의 엔티티가 이 스키마를 그대로 매핑한다.

- [ ] **Step 1: 마이그레이션 SQL 파일 작성**

```sql
-- 2026-07-01: AI 채팅 좋아요/싫어요 피드백 테이블 생성
--
-- 배경: AiChatPanel 응답 하단 좋아요/싫어요 버튼 클릭 시 질문/답변/출처 스냅샷을 저장해
--       추후 답변 품질 분석에 활용한다. FEEDBACK_ID는 EQUIP_CONDITION_RULES.RULE_ID와
--       동일하게 Oracle IDENTITY(GENERATED BY DEFAULT AS IDENTITY)로 채번한다.

CREATE TABLE AI_CHAT_FEEDBACKS (
  FEEDBACK_ID   NUMBER          GENERATED BY DEFAULT AS IDENTITY,
  COMPANY       VARCHAR2(10)    NOT NULL,
  PLANT_CD      VARCHAR2(10)    NOT NULL,
  ROUTE         VARCHAR2(200),
  MENU_CODE     VARCHAR2(50),
  QUESTION      CLOB            NOT NULL,
  ANSWER        CLOB            NOT NULL,
  SOURCES_JSON  CLOB,
  RATING        VARCHAR2(10)    NOT NULL,
  CREATED_BY    VARCHAR2(50)    NOT NULL,
  CREATED_AT    TIMESTAMP       DEFAULT SYSTIMESTAMP NOT NULL,
  CONSTRAINT PK_AI_CHAT_FEEDBACKS PRIMARY KEY (FEEDBACK_ID),
  CONSTRAINT CK_AI_CHAT_FEEDBACKS_RATING CHECK (RATING IN ('LIKE','DISLIKE'))
);

CREATE INDEX IDX_AI_CHAT_FEEDBACKS_TENANT  ON AI_CHAT_FEEDBACKS (COMPANY, PLANT_CD);
CREATE INDEX IDX_AI_CHAT_FEEDBACKS_MENU    ON AI_CHAT_FEEDBACKS (MENU_CODE);
CREATE INDEX IDX_AI_CHAT_FEEDBACKS_CREATED ON AI_CHAT_FEEDBACKS (CREATED_AT DESC);

COMMIT;
```

- [ ] **Step 2: 실제 DB(JSHANES)에 적용**

Run:
```bash
python C:/Users/hsyou/.claude/skills/oracle-db/scripts/oracle_connector.py --site JSHANES --execute-file apps/backend/src/migrations/2026-07-01_ai_chat_feedbacks.sql
```
Expected: `"success": true` 및 `blocks_executed` 로그(1블록 CREATE TABLE + 3블록 CREATE INDEX + COMMIT — 세미콜론 기준이 아니라 파일 전체가 순수 DDL/COMMIT 나열이므로 커넥터가 한 번에 실행해도 무방; 실패하면 각 문장을 `/`로 분리해 재실행).

- [ ] **Step 3: 생성 결과 검증**

Run:
```bash
python C:/Users/hsyou/.claude/skills/oracle-db/scripts/oracle_connector.py --site JSHANES --query "SELECT column_name, data_type, nullable, identity_column FROM user_tab_columns WHERE table_name='AI_CHAT_FEEDBACKS' ORDER BY column_id"
```
Expected: 10개 컬럼 반환, `FEEDBACK_ID`의 `IDENTITY_COLUMN`이 `YES`.

- [ ] **Step 4: Commit**

```bash
git add apps/backend/src/migrations/2026-07-01_ai_chat_feedbacks.sql
git commit -m "feat(ai): AI_CHAT_FEEDBACKS 테이블 마이그레이션 추가"
```

---

### Task 2: `AiChatFeedback` 엔티티 생성

**Files:**
- Create: `apps/backend/src/entities/ai-chat-feedback.entity.ts`

**Interfaces:**
- Consumes: Task 1에서 만든 `AI_CHAT_FEEDBACKS` 테이블 스키마.
- Produces: `AiChatFeedback` 클래스 — Task 4(`AiFeedbackService`)와 Task 7(`ai.module.ts`)에서 `@InjectRepository(AiChatFeedback)`/`TypeOrmModule.forFeature([AiChatFeedback])`로 사용.

- [ ] **Step 1: 엔티티 파일 작성**

```ts
/**
 * @file entities/ai-chat-feedback.entity.ts
 * @description AI 채팅 응답 좋아요/싫어요 피드백 — 질문/답변/출처 스냅샷 저장
 */
import { Entity, PrimaryGeneratedColumn, Column, CreateDateColumn } from 'typeorm';

@Entity({ name: 'AI_CHAT_FEEDBACKS' })
export class AiChatFeedback {
  @PrimaryGeneratedColumn({ name: 'FEEDBACK_ID' })
  feedbackId: number;

  @Column({ type: 'varchar2', name: 'COMPANY', length: 10 })
  company: string;

  @Column({ type: 'varchar2', name: 'PLANT_CD', length: 10 })
  plant: string;

  @Column({ type: 'varchar2', name: 'ROUTE', length: 200, nullable: true })
  route: string | null;

  @Column({ type: 'varchar2', name: 'MENU_CODE', length: 50, nullable: true })
  menuCode: string | null;

  @Column({ type: 'clob', name: 'QUESTION' })
  question: string;

  @Column({ type: 'clob', name: 'ANSWER' })
  answer: string;

  @Column({ type: 'clob', name: 'SOURCES_JSON', nullable: true })
  sourcesJson: string | null;

  /** LIKE | DISLIKE */
  @Column({ type: 'varchar2', name: 'RATING', length: 10 })
  rating: string;

  @Column({ type: 'varchar2', name: 'CREATED_BY', length: 50 })
  createdBy: string;

  @CreateDateColumn({ name: 'CREATED_AT', type: 'timestamp' })
  createdAt: Date;
}
```

- [ ] **Step 2: 타입체크로 문법 확인**

Run: `pnpm.cmd --filter @harness/backend exec tsc --noEmit --pretty false`
Expected: `AiChatFeedback` 관련 에러 없음(아직 어디서도 import하지 않으므로 신규 에러 발생 안 함).

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/entities/ai-chat-feedback.entity.ts
git commit -m "feat(ai): AiChatFeedback 엔티티 추가"
```

---

### Task 3: DTO 추가 — `AiKnowledgeSourceDto`, `AiChatFeedbackDto`

**Files:**
- Modify: `apps/backend/src/modules/ai/dto/ai-chat.dto.ts`

**Interfaces:**
- Produces: `AiKnowledgeSourceDto`(응답 sources 요약 + 피드백 요청의 sources 검증 겸용), `AiChatFeedbackDto`(POST 피드백 요청 바디) — Task 5(`AiSqlService`)와 Task 6(`AiController`)에서 사용.

- [ ] **Step 1: import에 `IsNumber` 추가**

`apps/backend/src/modules/ai/dto/ai-chat.dto.ts` 최상단 import를 다음으로 교체:

```ts
import { IsArray, IsString, IsIn, IsNumber, ValidateNested, ArrayNotEmpty, MaxLength, IsOptional, IsBoolean } from 'class-validator';
```

- [ ] **Step 2: 파일 끝에 두 DTO 추가**

`apps/backend/src/modules/ai/dto/ai-chat.dto.ts` 맨 끝(`AiTestDto` 클래스 뒤)에 추가:

```ts
/** RAG 지식 청크 출처 요약 — 채팅 응답(sources)과 피드백 요청(sources) 양쪽에서 사용 */
export class AiKnowledgeSourceDto {
  @IsString()
  chunkId: string;

  @IsString()
  sourcePath: string;

  @IsOptional()
  @IsString()
  menuCode?: string;

  @IsOptional()
  @IsString()
  audience?: string;

  @IsOptional()
  @IsString()
  title?: string;

  @IsOptional()
  @IsString()
  heading?: string;

  @IsOptional()
  @IsNumber()
  score?: number;
}

/** 좋아요/싫어요 피드백 등록 요청 */
export class AiChatFeedbackDto {
  @IsString()
  @MaxLength(4000)
  question: string;

  @IsString()
  @MaxLength(50000)
  answer: string;

  @IsOptional()
  @IsArray()
  @ValidateNested({ each: true })
  @Type(() => AiKnowledgeSourceDto)
  sources?: AiKnowledgeSourceDto[];

  @IsOptional()
  @IsString()
  @MaxLength(200)
  route?: string;

  @IsOptional()
  @IsString()
  @MaxLength(50)
  menuCode?: string;

  @IsIn(['LIKE', 'DISLIKE'])
  rating: 'LIKE' | 'DISLIKE';
}
```

- [ ] **Step 3: 타입체크**

Run: `pnpm.cmd --filter @harness/backend exec tsc --noEmit --pretty false`
Expected: 0 errors.

- [ ] **Step 4: Commit**

```bash
git add apps/backend/src/modules/ai/dto/ai-chat.dto.ts
git commit -m "feat(ai): 출처/피드백 DTO 추가"
```

---

### Task 4: `AiFeedbackService` 작성

**Files:**
- Create: `apps/backend/src/modules/ai/ai-feedback.service.ts`

**Interfaces:**
- Consumes: `AiChatFeedback`(Task 2), `AiChatFeedbackDto`(Task 3).
- Produces: `AiFeedbackService.create(dto, company, plant, createdBy): Promise<{ id: number }>`, `AiFeedbackService.remove(id, company, plant): Promise<{ ok: true }>` — Task 6(`AiController`)에서 호출.

- [ ] **Step 1: 서비스 파일 작성**

```ts
/**
 * @file src/modules/ai/ai-feedback.service.ts
 * @description AI 채팅 응답 좋아요/싫어요 피드백 저장/삭제
 */
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { AiChatFeedback } from '../../entities/ai-chat-feedback.entity';
import { AiChatFeedbackDto } from './dto/ai-chat.dto';

@Injectable()
export class AiFeedbackService {
  constructor(
    @InjectRepository(AiChatFeedback)
    private readonly repository: Repository<AiChatFeedback>,
  ) {}

  async create(
    dto: AiChatFeedbackDto,
    company: string,
    plant: string,
    createdBy: string,
  ): Promise<{ id: number }> {
    const entity = this.repository.create({
      company,
      plant,
      route: dto.route ?? null,
      menuCode: dto.menuCode ?? null,
      question: dto.question,
      answer: dto.answer,
      sourcesJson: dto.sources && dto.sources.length > 0 ? JSON.stringify(dto.sources) : null,
      rating: dto.rating,
      createdBy,
    });
    const saved = await this.repository.save(entity);
    return { id: saved.feedbackId };
  }

  async remove(feedbackId: number, company: string, plant: string): Promise<{ ok: true }> {
    await this.repository.delete({ feedbackId, company, plant });
    return { ok: true };
  }
}
```

- [ ] **Step 2: 타입체크**

Run: `pnpm.cmd --filter @harness/backend exec tsc --noEmit --pretty false`
Expected: 0 errors (아직 모듈에 등록 전이라 DI 에러는 Task 7에서 해소).

- [ ] **Step 3: Commit**

```bash
git add apps/backend/src/modules/ai/ai-feedback.service.ts
git commit -m "feat(ai): AiFeedbackService 추가"
```

---

### Task 5: `AiSqlService.process()`가 모든 응답에 `sources`를 실어 보내도록 리팩터

**Files:**
- Modify: `apps/backend/src/modules/ai/ai-sql.service.ts:8-34` (import + `AiSqlResult`), `:118-187` (`process()`)

**Interfaces:**
- Consumes: `AiKnowledgeService.search()`가 반환하는 `KnowledgeSearchResult[]`(`apps/backend/src/modules/ai-knowledge/ai-knowledge.service.ts`).
- Produces: `AiSqlResult.sources?: AiKnowledgeSourceSummary[]` — Task 6(컨트롤러 응답 그대로 통과), 프론트 Task 12에서 소비.

- [ ] **Step 1: import에 `KnowledgeSearchResult` 타입 추가**

`apps/backend/src/modules/ai-sql.service.ts` 17번째 줄:
```ts
import { AiKnowledgeService } from '../ai-knowledge/ai-knowledge.service';
```
을 다음으로 교체:
```ts
import { AiKnowledgeService, KnowledgeSearchResult } from '../ai-knowledge/ai-knowledge.service';
```

- [ ] **Step 2: `AiSqlResult`에 `sources` 필드, `AiKnowledgeSourceSummary` 타입 추가**

26~34번째 줄의 기존:
```ts
export interface AiSqlResult {
  content: string;
  sql?: string;
  executed?: boolean;
  rowCount?: number;
  requiresApproval?: boolean;
  /** 승인 후 실행할 페이지 도구 호출 제안(write 도구) */
  pageToolCall?: AiPageToolCallProposal;
}
```
를 다음으로 교체:
```ts
export interface AiKnowledgeSourceSummary {
  chunkId: string;
  sourcePath: string;
  menuCode?: string;
  audience?: string;
  title?: string;
  heading?: string;
  score: number;
}

export interface AiSqlResult {
  content: string;
  sql?: string;
  executed?: boolean;
  rowCount?: number;
  requiresApproval?: boolean;
  /** 승인 후 실행할 페이지 도구 호출 제안(write 도구) */
  pageToolCall?: AiPageToolCallProposal;
  /** 답변 근거로 사용한 RAG 지식 청크 요약 (검색 결과가 있을 때만) */
  sources?: AiKnowledgeSourceSummary[];
}
```

- [ ] **Step 3: `process()`를 얇은 래퍼로 교체하고 기존 분기 로직을 `processWithKnowledge()`로 이동**

118~187번째 줄의 기존 `process()` 전체를 다음으로 교체:

```ts
  async process(
    messages: AiChatMessageDto[],
    pageToolContext?: AiPageToolContextDto,
    knowledgeContext?: AiKnowledgeContextDto,
  ): Promise<AiSqlResult> {
    const userMessage = [...messages].reverse().find((m) => m.role === 'user')?.content ?? '';
    if (!userMessage.trim()) return { content: '질문을 입력해 주세요.' };

    let knowledgePrompt = '';
    let knowledgeChunks: KnowledgeSearchResult[] = [];
    try {
      knowledgeChunks = await this.knowledge.search(userMessage, knowledgeContext, 5);
      knowledgePrompt = this.knowledge.formatContext(knowledgeChunks);
    } catch (error) {
      this.logger.warn(`AI 지식 검색 실패: ${error instanceof Error ? error.message : String(error)}`);
    }

    const result = await this.processWithKnowledge(userMessage, messages, pageToolContext, knowledgePrompt);
    return this.withSources(result, knowledgeChunks);
  }

  /** 검색된 지식 청크를 응답의 sources 필드로 병합(청크가 없으면 그대로 반환) */
  private withSources(result: AiSqlResult, chunks: KnowledgeSearchResult[]): AiSqlResult {
    if (chunks.length === 0) return result;
    return {
      ...result,
      sources: chunks.map((chunk) => ({
        chunkId: chunk.chunkId,
        sourcePath: chunk.sourcePath,
        menuCode: chunk.menuCode,
        audience: chunk.audience,
        title: chunk.title,
        heading: chunk.heading,
        score: chunk.score,
      })),
    };
  }

  /** 기존 process()의 테이블선택→SQL생성→실행/일반대화 분기 로직(변경 없음, knowledgePrompt를 인자로 받도록만 조정) */
  private async processWithKnowledge(
    userMessage: string,
    messages: AiChatMessageDto[],
    pageToolContext: AiPageToolContextDto | undefined,
    knowledgePrompt: string,
  ): Promise<AiSqlResult> {
    if (pageToolContext && this.looksLikePageWorkflowRequest(userMessage)) {
      // 등록/처리 요청 → 페이지의 write 도구로 매핑(있으면 승인 카드로 제안)
      const call = await this.selectPageTool(userMessage, pageToolContext.pageId);
      if (call) {
        return {
          content: '아래 작업을 검토하고, 실행하려면 승인해 주세요.',
          pageToolCall: call,
          requiresApproval: true,
        };
      }
      return this.generalChat(messages, pageToolContext, knowledgePrompt);
    }

    // [1단계] 관련 테이블 선택 (없으면 일반 대화)
    const tables = await this.selectTables(userMessage);
    if (tables.length === 0) return this.generalChat(messages, pageToolContext, knowledgePrompt);

    // [2단계] SQL 생성 (스키마 + 카탈로그 관계(JOIN 키) 주입)
    const schemaText = await this.schemaInfo.getSchemaText(tables);
    const relations = await this.catalog.getRelationsText(tables);
    const rawSql = await this.generateSql(userMessage, relations ? `${schemaText}\n\n${relations}` : schemaText);
    if (!rawSql) return this.generalChat(messages, pageToolContext, knowledgePrompt);

    // [검증]
    const v = this.validator.validate(rawSql);
    const sql = this.validator.stripFences(rawSql);
    if (!v.valid) {
      return { content: `생성된 SQL이 보안 정책에 위배됩니다: ${v.error}`, sql };
    }

    // 쓰기: 승인 대기 (실행하지 않음)
    if (v.kind === 'write') {
      return {
        content: '아래 데이터 변경 작업을 검토하고, 실행하려면 승인해 주세요.',
        sql,
        requiresApproval: true,
      };
    }

    // 조회: 즉시 실행 + 분석
    try {
      const rows = await this.runSelect(sql);
      const analysis = await this.analyze(userMessage, sql, rows, knowledgePrompt);
      return { content: analysis, sql, executed: true, rowCount: rows.length };
    } catch (error: unknown) {
      this.logger.error(
        `SELECT 실행 실패: ${error instanceof Error ? error.message : String(error)}`,
      );
      return {
        content: `조회 실행 중 오류가 발생했습니다: ${error instanceof Error ? error.message : '알 수 없음'}`,
        sql,
      };
    }
  }
```

- [ ] **Step 4: 타입체크**

Run: `pnpm.cmd --filter @harness/backend exec tsc --noEmit --pretty false`
Expected: 0 errors.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/modules/ai/ai-sql.service.ts
git commit -m "feat(ai): process()가 모든 응답 경로에 RAG sources를 실어 보내도록 리팩터"
```

---

### Task 6: `AiController`에 피드백 엔드포인트 추가

**Files:**
- Modify: `apps/backend/src/modules/ai/ai.controller.ts`

**Interfaces:**
- Consumes: `AiFeedbackService`(Task 4), `AiChatFeedbackDto`(Task 3), `getRequestUser`/`getHeaderString`.
- Produces: `POST /ai/chat/feedback` → `{ id: number }`, `DELETE /ai/chat/feedback/:id` → `{ ok: true }` — 프론트 Task 13에서 호출.

- [ ] **Step 1: import 구역 교체**

`apps/backend/src/modules/ai/ai.controller.ts` 1~14번째 줄 전체를 다음으로 교체:

```ts
/**
 * @file src/modules/ai/ai.controller.ts
 * @description AI 채팅 컨트롤러
 * - GET  /ai/status      : 활성화/provider/model/키설정여부
 * - POST /ai/chat         : 데이터 질의(text-to-SQL) 통합 — 일반대화 폴백
 * - POST /ai/execute-sql  : 승인된 INSERT/UPDATE 실행
 * - POST /ai/chat/feedback   : 응답 좋아요/싫어요 저장
 * - DELETE /ai/chat/feedback/:id : 좋아요/싫어요 취소
 */
import { Controller, Get, Post, Put, Delete, Body, Param, Req, ParseIntPipe, BadRequestException } from '@nestjs/common';
import { Request } from 'express';
import { AiService } from './ai.service';
import { AiSqlService } from './ai-sql.service';
import { AiCatalogService } from './ai-catalog.service';
import { SchemaInfoService } from './schema-info.service';
import { AiFeedbackService } from './ai-feedback.service';
import { EmbeddingService } from '../ai-knowledge/embedding.service';
import { AiChatDto, AiExecuteSqlDto, AiTestDto, AiEmbeddingTestDto, AiCatalogSaveDto, AiCatalogTablesDto, AiChatFeedbackDto } from './dto/ai-chat.dto';
import { getRequestUser } from '../../common/utils/request-user.util';
import { getHeaderString } from '../../common/utils/header-value.util';
```

- [ ] **Step 2: 생성자에 `AiFeedbackService` 주입**

기존:
```ts
  constructor(
    private readonly aiService: AiService,
    private readonly aiSqlService: AiSqlService,
    private readonly aiCatalogService: AiCatalogService,
    private readonly schemaInfoService: SchemaInfoService,
    private readonly embeddingService: EmbeddingService,
  ) {}
```
를 다음으로 교체:
```ts
  constructor(
    private readonly aiService: AiService,
    private readonly aiSqlService: AiSqlService,
    private readonly aiCatalogService: AiCatalogService,
    private readonly schemaInfoService: SchemaInfoService,
    private readonly embeddingService: EmbeddingService,
    private readonly aiFeedbackService: AiFeedbackService,
  ) {}
```

- [ ] **Step 3: `@Post('chat')` 핸들러 바로 뒤에 피드백 엔드포인트 2개 추가**

`chat(@Body() dto: AiChatDto) { ... }` 메서드 바로 뒤(`executeSql` 메서드 앞)에 삽입:

```ts
  @Post('chat/feedback')
  async createFeedback(@Body() dto: AiChatFeedbackDto, @Req() req: Request) {
    const { company, plant } = this.tenant(req);
    if (!company || !plant) {
      throw new BadRequestException('회사/사업장 정보가 없습니다.');
    }
    const createdBy = getRequestUser(req)?.id ?? 'unknown';
    return this.aiFeedbackService.create(dto, company, plant, createdBy);
  }

  @Delete('chat/feedback/:id')
  async deleteFeedback(@Param('id', ParseIntPipe) id: number, @Req() req: Request) {
    const { company, plant } = this.tenant(req);
    if (!company || !plant) {
      throw new BadRequestException('회사/사업장 정보가 없습니다.');
    }
    return this.aiFeedbackService.remove(id, company, plant);
  }

  private tenant(req: Request) {
    const user = getRequestUser(req) ?? {};
    return {
      company: getHeaderString(req.headers['x-company']) || user.company,
      plant: getHeaderString(req.headers['x-plant']) || user.plant,
    };
  }
```

- [ ] **Step 4: 타입체크**

Run: `pnpm.cmd --filter @harness/backend exec tsc --noEmit --pretty false`
Expected: 0 errors.

- [ ] **Step 5: Commit**

```bash
git add apps/backend/src/modules/ai/ai.controller.ts
git commit -m "feat(ai): 채팅 응답 좋아요/싫어요 저장/삭제 엔드포인트 추가"
```

---

### Task 7: `ai.module.ts`에 엔티티/서비스 등록

**Files:**
- Modify: `apps/backend/src/modules/ai/ai.module.ts`

**Interfaces:**
- Consumes: `AiChatFeedback`(Task 2), `AiFeedbackService`(Task 4).

- [ ] **Step 1: 파일 전체 교체**

```ts
/**
 * @file src/modules/ai/ai.module.ts
 * @description AI 채팅 모듈 (Mistral 연동 + text-to-SQL)
 */
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { SysConfig } from '../../entities/sys-config.entity';
import { AiChatFeedback } from '../../entities/ai-chat-feedback.entity';
import { AiPageToolsModule } from '../ai-page-tools/ai-page-tools.module';
import { AiKnowledgeModule } from '../ai-knowledge/ai-knowledge.module';
import { AiController } from './ai.controller';
import { AiService } from './ai.service';
import { AiSqlService } from './ai-sql.service';
import { AiCatalogService } from './ai-catalog.service';
import { SchemaInfoService } from './schema-info.service';
import { SqlValidatorService } from './sql-validator.service';
import { AiFeedbackService } from './ai-feedback.service';

@Module({
  imports: [TypeOrmModule.forFeature([SysConfig, AiChatFeedback]), AiPageToolsModule, AiKnowledgeModule],
  controllers: [AiController],
  providers: [AiService, AiSqlService, AiCatalogService, SchemaInfoService, SqlValidatorService, AiFeedbackService],
})
export class AiModule {}
```

- [ ] **Step 2: 타입체크**

Run: `pnpm.cmd --filter @harness/backend exec tsc --noEmit --pretty false`
Expected: 0 errors.

- [ ] **Step 3: 백엔드 재시작 후 라우트 등록 확인**

dev 서버가 `nest start --watch`라면 파일 저장 시 자동 재시작된다. 재시작 로그(터미널)에서 `AiController` 매핑에 `POST /api/v1/ai/chat/feedback`, `DELETE /api/v1/ai/chat/feedback/:id`가 찍히는지 확인. 자동 재시작이 안 되어 있으면 사용자에게 백엔드 재시작을 요청한다(임의로 프로세스를 죽이거나 새 포트로 띄우지 않는다).

- [ ] **Step 4: Commit**

```bash
git add apps/backend/src/modules/ai/ai.module.ts
git commit -m "feat(ai): AiChatFeedback/AiFeedbackService 모듈 등록"
```

---

### Task 8: 프론트 — `rehype-slug` 추가 + `MarkdownRenderer` 헤딩 id 통과

**Files:**
- Modify: `apps/frontend/package.json`
- Modify: `apps/frontend/src/components/help/MarkdownRenderer.tsx`

**Interfaces:**
- Produces: 도움말 렌더링 시 모든 `h1/h2/h3`에 github-slugger 규칙의 `id` 속성 부여 — Task 11(`HelpPanel`)의 `scrollIntoView(id)`가 이 id를 찾는다.

- [ ] **Step 1: 의존성 추가**

`apps/frontend/package.json`의 `remark-gfm` 옆(dependencies, 알파벳 순서 근처)에 추가:
```json
    "rehype-slug": "^6.0.0",
```

- [ ] **Step 2: 설치**

Run: `pnpm.cmd install`
Expected: lockfile 갱신, 에러 없음.

- [ ] **Step 3: `MarkdownRenderer.tsx`에 플러그인 추가 + heading 컴포넌트가 `id`를 받도록 수정**

전체 파일을 다음으로 교체:

```tsx
"use client";

import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import rehypeRaw from "rehype-raw";
import rehypeSlug from "rehype-slug";

interface MarkdownRendererProps {
  content: string;
}

/** 도움말 Markdown 렌더러 — GFM(표/코드/링크) + 제한적 raw HTML(img width 등) + 헤딩 id(딥링크용) */
export default function MarkdownRenderer({ content }: MarkdownRendererProps) {
  return (
    <div className="help-md max-w-none text-sm leading-relaxed text-text">
      <ReactMarkdown
        remarkPlugins={[remarkGfm]}
        rehypePlugins={[rehypeRaw, rehypeSlug]}
        components={{
          h1: ({ id, children }) => <h1 id={id} className="mb-3 mt-1 scroll-mt-4 text-lg font-bold text-text">{children}</h1>,
          h2: ({ id, children }) => <h2 id={id} className="mb-2 mt-5 scroll-mt-4 border-b border-border pb-1 text-base font-semibold text-text">{children}</h2>,
          h3: ({ id, children }) => <h3 id={id} className="mb-1.5 mt-4 scroll-mt-4 text-sm font-semibold text-text">{children}</h3>,
          p: ({ children }) => <p className="my-2 text-text">{children}</p>,
          ul: ({ children }) => <ul className="my-2 list-disc space-y-1 pl-5">{children}</ul>,
          ol: ({ children }) => <ol className="my-2 list-decimal space-y-1 pl-5">{children}</ol>,
          li: ({ children }) => <li className="text-text">{children}</li>,
          a: ({ href, children }) => <a href={href} className="text-primary underline hover:opacity-80">{children}</a>,
          code: ({ children }) => <code className="rounded bg-surface px-1.5 py-0.5 font-mono text-[12px] text-primary">{children}</code>,
          pre: ({ children }) => <pre className="my-2 overflow-x-auto rounded-lg bg-slate-900 p-3 text-[12px] text-slate-100">{children}</pre>,
          table: ({ children }) => <table className="my-3 w-full border-collapse text-[13px]">{children}</table>,
          th: ({ children }) => <th className="border border-border bg-surface px-2 py-1 text-left font-semibold">{children}</th>,
          td: ({ children }) => <td className="border border-border px-2 py-1 align-top">{children}</td>,
          img: ({ src, alt, ...props }) => (
            // eslint-disable-next-line @next/next/no-img-element
            <img src={typeof src === "string" ? src : ""} alt={alt ?? ""} {...props} className="my-2 max-w-full rounded-lg border border-border" />
          ),
          blockquote: ({ children }) => <blockquote className="my-2 border-l-4 border-primary/40 bg-surface/60 px-3 py-1.5 text-text-muted">{children}</blockquote>,
        }}
      >
        {content}
      </ReactMarkdown>
    </div>
  );
}
```

(`scroll-mt-4`는 스크롤 후 헤딩이 패널 상단에 딱 붙지 않도록 하는 여백이다.)

- [ ] **Step 4: 타입체크**

Run: `pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false`
Expected: 0 errors.

- [ ] **Step 5: Commit**

```bash
git add apps/frontend/package.json pnpm-lock.yaml apps/frontend/src/components/help/MarkdownRenderer.tsx
git commit -m "feat(help): rehype-slug로 도움말 헤딩에 딥링크용 id 부여"
```

---

### Task 9: `lib/help.ts`에 `slugify()` 유틸 추가

**Files:**
- Modify: `apps/frontend/src/lib/help.ts`

**Interfaces:**
- Produces: `slugify(text: string): string` — Task 13(`AiChatPanel`)이 출처의 `heading` 텍스트를 도움말 문서의 실제 heading id와 같은 규칙으로 변환할 때 사용.

- [ ] **Step 1: 파일 끝에 함수 추가**

`apps/frontend/src/lib/help.ts` 맨 끝에 추가한다. 아래 정규식의 문자 클래스 앞부분은 공백이 아니라 유니코드 U+2000~U+206F(일반 구두점)와 U+2E00~U+2E7F(추가 구두점) 범위를 이루는 실제 문자다 — 손으로 다시 입력하지 말고 이 블록을 그대로 복사해서 붙여넣는다:

```ts
/**
 * rehype-slug(github-slugger)와 동일한 규칙으로 헤딩 텍스트를 id 슬러그로 변환한다.
 * 정확히 같은 헤딩이 여러 번 나오면 github-slugger는 `-1`,`-2`를 붙이지만
 * 이 함수는 순서 정보가 없어 base slug만 반환한다 — 매치 실패 시 그냥 문서 맨 위가 열리는 것으로 충분하다.
 */
export function slugify(text: string): string {
  return text
    .trim()
    .toLowerCase()
    .replace(/[ -⁯⸀-⹿\'!"#$%&()*+,./:;<=>?@[\]^`{|}~]/g, "")
    .replace(/\s+/g, "-");
}
```

- [ ] **Step 2: 타입체크**

Run: `pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false`
Expected: 0 errors.

- [ ] **Step 3: Commit**

```bash
git add apps/frontend/src/lib/help.ts
git commit -m "feat(help): 헤딩 텍스트→슬러그 변환 유틸 추가"
```

---

### Task 10: `helpStore`에 메뉴코드/섹션 오버라이드 추가

**Files:**
- Modify: `apps/frontend/src/stores/helpStore.ts`

**Interfaces:**
- Produces: `useHelpStore().openHelpFor(menuCode: string, tab: HelpTab, headingSlug?: string): void`, 상태 `overrideMenuCode?: string`, `overrideHeadingSlug?: string` — Task 11(`HelpPanel`)과 Task 13(`AiChatPanel`)에서 사용.

- [ ] **Step 1: 파일 전체 교체**

```ts
import { create } from "zustand";
import type { HelpTab } from "@/lib/help";

interface HelpState {
  isOpen: boolean;
  tab: HelpTab;
  /** AI 채팅 출처 클릭 등 외부에서 특정 문서를 강제로 열 때 사용(현재 페이지 경로 대신 우선) */
  overrideMenuCode?: string;
  overrideHeadingSlug?: string;
  openHelp: () => void;
  /** menuCode/tab을 지정해 도움말 패널을 열고, headingSlug가 있으면 해당 섹션으로 스크롤 */
  openHelpFor: (menuCode: string, tab: HelpTab, headingSlug?: string) => void;
  closeHelp: () => void;
  setTab: (tab: HelpTab) => void;
}

export const useHelpStore = create<HelpState>((set) => ({
  isOpen: false,
  tab: "user",
  overrideMenuCode: undefined,
  overrideHeadingSlug: undefined,
  openHelp: () => set({ isOpen: true }),
  openHelpFor: (menuCode, tab, headingSlug) =>
    set({ isOpen: true, tab, overrideMenuCode: menuCode, overrideHeadingSlug: headingSlug }),
  closeHelp: () => set({ isOpen: false, overrideMenuCode: undefined, overrideHeadingSlug: undefined }),
  setTab: (tab) => set({ tab }),
}));
```

- [ ] **Step 2: 타입체크**

Run: `pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false`
Expected: `HelpPanel.tsx`가 아직 옛 구조분해를 쓰므로 에러 없음(그대로 호환 — 새 필드는 optional이고 기존 필드/함수 시그니처는 유지됨).

- [ ] **Step 3: Commit**

```bash
git add apps/frontend/src/stores/helpStore.ts
git commit -m "feat(help): helpStore에 문서/섹션 오버라이드 오픈 기능 추가"
```

---

### Task 11: `HelpPanel`이 오버라이드를 우선 사용 + 섹션 스크롤

**Files:**
- Modify: `apps/frontend/src/components/help/HelpPanel.tsx`

**Interfaces:**
- Consumes: `useHelpStore().overrideMenuCode/overrideHeadingSlug`(Task 10).

- [ ] **Step 1: 상태 구조분해 및 menuCode/tab 결정부 교체**

기존(12~18번째 줄):
```tsx
export default function HelpPanel() {
  const { t } = useTranslation();
  const pathname = usePathname();
  const router = useRouter();
  const { isOpen, tab, closeHelp, setTab } = useHelpStore();
  const menuCode = findMenuCodeByPath(pathname);
  const { content, loading, notFound } = useHelpDoc(isOpen ? menuCode : undefined, tab);
  const [width, setWidth] = useState(448);
```
를 다음으로 교체:
```tsx
export default function HelpPanel() {
  const { t } = useTranslation();
  const pathname = usePathname();
  const router = useRouter();
  const { isOpen, tab, overrideMenuCode, overrideHeadingSlug, closeHelp, setTab } = useHelpStore();
  const menuCode = overrideMenuCode ?? findMenuCodeByPath(pathname);
  const { content, loading, notFound } = useHelpDoc(isOpen ? menuCode : undefined, tab);
  const [width, setWidth] = useState(448);
```

- [ ] **Step 2: 본문 로드 완료 후 섹션으로 스크롤하는 effect 추가**

기존 "Escape로 닫기" `useEffect` 블록(44~51번째 줄) 바로 뒤에 추가:

```tsx
  // 출처 딥링크: 본문 로드 완료 후 해당 헤딩으로 스크롤
  useEffect(() => {
    if (!isOpen || loading || !content || !overrideHeadingSlug) return;
    const id = overrideHeadingSlug;
    const timer = setTimeout(() => {
      document.getElementById(id)?.scrollIntoView({ behavior: "smooth", block: "start" });
    }, 50);
    return () => clearTimeout(timer);
  }, [isOpen, loading, content, overrideHeadingSlug]);
```

(마크다운이 렌더된 직후 DOM에 id가 붙으므로 다음 tick에 스크롤하도록 짧은 `setTimeout`을 둔다.)

- [ ] **Step 3: 타입체크**

Run: `pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false`
Expected: 0 errors.

- [ ] **Step 4: Commit**

```bash
git add apps/frontend/src/components/help/HelpPanel.tsx
git commit -m "feat(help): 도움말 패널이 출처 오버라이드로 특정 섹션까지 스크롤"
```

---

### Task 12: `aiChatStore`에 `sources` 필드 추가 + 응답 수신부 반영

**Files:**
- Modify: `apps/frontend/src/stores/aiChatStore.ts`
- Modify: `apps/frontend/src/components/ai/AiChatPanel.tsx:94-103`

**Interfaces:**
- Produces: `AiChatSource` 인터페이스, `AiChatMessage.sources?: AiChatSource[]` — Task 13에서 렌더링에 사용.

- [ ] **Step 1: `aiChatStore.ts`에 `AiChatSource` 추가 + `AiChatMessage`에 필드 추가**

기존 `AiChatMessage` 인터페이스(17~26번째 줄):
```ts
export interface AiChatMessage {
  role: "user" | "assistant";
  content: string;
  /** 생성/실행된 SQL (조회·쓰기 공통) */
  sql?: string;
  /** INSERT/UPDATE 승인 대기 여부 */
  requiresApproval?: boolean;
  /** 실행 완료 여부 */
  executed?: boolean;
  /** 페이지 도구 실행 제안(승인 카드) */
  pageToolCall?: AiPageToolCallProposal;
}
```
를 다음으로 교체:
```ts
/** 답변 근거로 사용한 RAG 지식 청크 요약 */
export interface AiChatSource {
  chunkId: string;
  sourcePath: string;
  menuCode?: string;
  audience?: string;
  title?: string;
  heading?: string;
  score: number;
}

export interface AiChatMessage {
  role: "user" | "assistant";
  content: string;
  /** 생성/실행된 SQL (조회·쓰기 공통) */
  sql?: string;
  /** INSERT/UPDATE 승인 대기 여부 */
  requiresApproval?: boolean;
  /** 실행 완료 여부 */
  executed?: boolean;
  /** 페이지 도구 실행 제안(승인 카드) */
  pageToolCall?: AiPageToolCallProposal;
  /** 답변 근거 출처 (RAG 검색 결과가 있을 때만) */
  sources?: AiChatSource[];
}
```

- [ ] **Step 2: `AiChatPanel.tsx`의 응답 처리부에서 `sources` 전달**

`apps/frontend/src/components/ai/AiChatPanel.tsx`의 94~103번째 줄 기존:
```tsx
      const res = await api.post("/ai/chat", { messages: history, pageToolContext, knowledgeContext });
      const data = res.data?.data ?? {};
      addMessage({
        role: "assistant",
        content: data.content || t("ai.chat.empty", "응답이 비어 있습니다."),
        sql: data.sql,
        requiresApproval: data.requiresApproval,
        executed: data.executed,
        pageToolCall: data.pageToolCall,
      });
```
를 다음으로 교체:
```tsx
      const res = await api.post("/ai/chat", { messages: history, pageToolContext, knowledgeContext });
      const data = res.data?.data ?? {};
      addMessage({
        role: "assistant",
        content: data.content || t("ai.chat.empty", "응답이 비어 있습니다."),
        sql: data.sql,
        requiresApproval: data.requiresApproval,
        executed: data.executed,
        pageToolCall: data.pageToolCall,
        sources: data.sources,
      });
```

- [ ] **Step 3: 타입체크**

Run: `pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false`
Expected: 0 errors.

- [ ] **Step 4: Commit**

```bash
git add apps/frontend/src/stores/aiChatStore.ts apps/frontend/src/components/ai/AiChatPanel.tsx
git commit -m "feat(ai): AiChatMessage에 sources 필드 추가 및 응답 반영"
```

---

### Task 13: `AiChatPanel` — 출처 목록 + 복사/좋아요/싫어요 버튼 UI

**Files:**
- Modify: `apps/frontend/src/components/ai/AiChatPanel.tsx`
- Modify: `apps/frontend/src/locales/ko.json`, `en.json`, `zh.json`, `vi.json`

**Interfaces:**
- Consumes: `AiChatMessage.sources`(Task 12), `useHelpStore().openHelpFor`(Task 10), `slugify`(Task 9), `POST/DELETE /ai/chat/feedback`(Task 6).

- [ ] **Step 1: i18n 키 4개 파일에 추가**

`apps/frontend/src/locales/ko.json`의 `"sqlLabelGen": "생성된 SQL (실행 실패)",\n      "resize": "드래그하여 너비 조절"` 부분을:
```json
      "sqlLabelGen": "생성된 SQL (실행 실패)",
      "sourcesToggle": "출처 {{count}}건",
      "copy": "복사",
      "copied": "복사됨",
      "like": "좋아요",
      "dislike": "싫어요",
      "resize": "드래그하여 너비 조절"
```
로 교체.

`apps/frontend/src/locales/en.json`의 동일 위치(`"sqlLabelGen": "Generated SQL (execution failed)",\n      "resize": "Drag to resize"`)를:
```json
      "sqlLabelGen": "Generated SQL (execution failed)",
      "sourcesToggle": "{{count}} sources",
      "copy": "Copy",
      "copied": "Copied",
      "like": "Like",
      "dislike": "Dislike",
      "resize": "Drag to resize"
```
로 교체.

`apps/frontend/src/locales/zh.json`의 동일 위치(`"sqlLabelGen": "生成的 SQL（执行失败）",\n      "resize": "拖动调整宽度"`)를:
```json
      "sqlLabelGen": "生成的 SQL（执行失败）",
      "sourcesToggle": "{{count}} 个来源",
      "copy": "复制",
      "copied": "已复制",
      "like": "赞",
      "dislike": "踩",
      "resize": "拖动调整宽度"
```
로 교체.

`apps/frontend/src/locales/vi.json`의 동일 위치(`"sqlLabelGen": "SQL đã tạo (thực thi thất bại)",\n      "resize": "Kéo để thay đổi kích thước"`)를:
```json
      "sqlLabelGen": "SQL đã tạo (thực thi thất bại)",
      "sourcesToggle": "{{count}} nguồn",
      "copy": "Sao chép",
      "copied": "Đã sao chép",
      "like": "Thích",
      "dislike": "Không thích",
      "resize": "Kéo để thay đổi kích thước"
```
로 교체.

- [ ] **Step 2: 누락 키 검증**

Run: `node scripts/find_missing_i18n.js` (프로젝트에 이미 있는 누락 검사 스크립트)
Expected: `ai.chat.sourcesToggle`, `ai.chat.copy`, `ai.chat.copied`, `ai.chat.like`, `ai.chat.dislike`가 누락 목록에 없음.

- [ ] **Step 3: `AiChatPanel.tsx` import 구역 수정**

기존(10~21번째 줄):
```tsx
import { useState, useRef, useEffect, useCallback } from "react";
import { usePathname } from "next/navigation";
import { useTranslation } from "react-i18next";
import { Sparkles, X, Send, LoaderCircle, Trash2, Database, Play } from "lucide-react";
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import api from "@/services/api";
import { usePageToolStore } from "@/ai-page-tools/pageToolStore";
import { useAiChatStore } from "@/stores/aiChatStore";
import { findMenuCodeByPath } from "@/config/menuConfig";
import PageToolExecutionLog from "./PageToolExecutionLog";
import PageToolInspector from "./PageToolInspector";
```
를 다음으로 교체:
```tsx
import { useState, useRef, useEffect, useCallback } from "react";
import { usePathname } from "next/navigation";
import { useTranslation } from "react-i18next";
import { Sparkles, X, Send, LoaderCircle, Trash2, Database, Play, Copy, Check, ThumbsUp, ThumbsDown } from "lucide-react";
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import api from "@/services/api";
import { usePageToolStore } from "@/ai-page-tools/pageToolStore";
import { useAiChatStore, type AiChatMessage, type AiChatSource } from "@/stores/aiChatStore";
import { useHelpStore } from "@/stores/helpStore";
import { findMenuCodeByPath } from "@/config/menuConfig";
import { slugify } from "@/lib/help";
import PageToolExecutionLog from "./PageToolExecutionLog";
import PageToolInspector from "./PageToolInspector";
```

- [ ] **Step 4: 컴포넌트 본문에 상태 + 핸들러 추가**

기존 42~55번째 줄:
```tsx
  const { isOpen, messages, close, addMessage, clear } = useAiChatStore();
  const activeTab = usePageToolStore((state) => state.activeTab);
  const manifest = usePageToolStore((state) => state.manifest);
  const openChatTab = usePageToolStore((state) => state.openChatTab);
  const openToolsTab = usePageToolStore((state) => state.openToolsTab);
  const openLogTab = usePageToolStore((state) => state.openLogTab);
  const [input, setInput] = useState("");
  const [sending, setSending] = useState(false);
  const [approvedIdx, setApprovedIdx] = useState<Set<number>>(new Set());
  const [width, setWidth] = useState(440);
  const scrollRef = useRef<HTMLDivElement>(null);
  const inputRef = useRef<HTMLTextAreaElement>(null);
```
를 다음으로 교체:
```tsx
  const { isOpen, messages, close, addMessage, clear } = useAiChatStore();
  const activeTab = usePageToolStore((state) => state.activeTab);
  const manifest = usePageToolStore((state) => state.manifest);
  const openChatTab = usePageToolStore((state) => state.openChatTab);
  const openToolsTab = usePageToolStore((state) => state.openToolsTab);
  const openLogTab = usePageToolStore((state) => state.openLogTab);
  const openHelpFor = useHelpStore((state) => state.openHelpFor);
  const [input, setInput] = useState("");
  const [sending, setSending] = useState(false);
  const [approvedIdx, setApprovedIdx] = useState<Set<number>>(new Set());
  const [expandedSources, setExpandedSources] = useState<Set<number>>(new Set());
  const [feedbackByIdx, setFeedbackByIdx] = useState<Map<number, { feedbackId: number; rating: "LIKE" | "DISLIKE" }>>(new Map());
  const [copiedIdx, setCopiedIdx] = useState<number | null>(null);
  const [width, setWidth] = useState(440);
  const scrollRef = useRef<HTMLDivElement>(null);
  const inputRef = useRef<HTMLTextAreaElement>(null);
```

`executeTool` 콜백(132~149번째 줄) 바로 뒤, `onKeyDown` 콜백 앞에 추가:

```tsx
  const toggleSources = useCallback((idx: number) => {
    setExpandedSources((prev) => {
      const next = new Set(prev);
      if (next.has(idx)) next.delete(idx);
      else next.add(idx);
      return next;
    });
  }, []);

  const openSource = useCallback(
    (source: AiChatSource) => {
      if (!source.menuCode) return;
      const tab = source.audience === "operator" ? "operator" : "user";
      openHelpFor(source.menuCode, tab, source.heading ? slugify(source.heading) : undefined);
    },
    [openHelpFor],
  );

  const copyMessage = useCallback(async (idx: number, content: string) => {
    await navigator.clipboard.writeText(content);
    setCopiedIdx(idx);
    setTimeout(() => setCopiedIdx((cur) => (cur === idx ? null : cur)), 1500);
  }, []);

  const rate = useCallback(
    async (idx: number, message: AiChatMessage, rating: "LIKE" | "DISLIKE") => {
      const existing = feedbackByIdx.get(idx);
      if (existing) {
        try {
          await api.delete(`/ai/chat/feedback/${existing.feedbackId}`);
        } catch {
          // 삭제 실패해도 사용자 흐름을 막지 않는다
        }
        setFeedbackByIdx((prev) => {
          const next = new Map(prev);
          next.delete(idx);
          return next;
        });
        if (existing.rating === rating) return;
      }
      const question = [...messages.slice(0, idx)].reverse().find((m) => m.role === "user")?.content ?? "";
      try {
        const res = await api.post("/ai/chat/feedback", {
          question,
          answer: message.content,
          sources: message.sources,
          route: pathname,
          menuCode: findMenuCodeByPath(pathname),
          rating,
        });
        const feedbackId = res.data?.data?.id;
        if (feedbackId) {
          setFeedbackByIdx((prev) => new Map(prev).set(idx, { feedbackId, rating }));
        }
      } catch {
        // 피드백 저장 실패는 조용히 무시(대화 흐름에 영향 없음)
      }
    },
    [feedbackByIdx, messages, pathname],
  );
```

- [ ] **Step 5: 메시지 렌더링에 출처 목록 + 액션 버튼 추가**

기존 312~320번째 줄(SQL 접어보기 블록):
```tsx
              {/* 실행된/생성된 SQL 접어보기 (실행 실패 시 자동 펼침) */}
              {m.role === "assistant" && m.sql && !m.requiresApproval && (
                <details open={!m.executed} className="mt-1 w-[92%] text-[11px] text-text-muted">
                  <summary className="cursor-pointer select-none">
                    {m.executed ? t("ai.chat.sqlLabel", "실행된 SQL") : t("ai.chat.sqlLabelGen", "생성된 SQL (실행 실패)")}
                  </summary>
                  <pre className="mt-1 max-h-40 overflow-auto whitespace-pre-wrap rounded bg-surface px-2 py-1 font-mono">{m.sql}</pre>
                </details>
              )}
            </div>
          ))
        )}
```
를 다음으로 교체:
```tsx
              {/* 실행된/생성된 SQL 접어보기 (실행 실패 시 자동 펼침) */}
              {m.role === "assistant" && m.sql && !m.requiresApproval && (
                <details open={!m.executed} className="mt-1 w-[92%] text-[11px] text-text-muted">
                  <summary className="cursor-pointer select-none">
                    {m.executed ? t("ai.chat.sqlLabel", "실행된 SQL") : t("ai.chat.sqlLabelGen", "생성된 SQL (실행 실패)")}
                  </summary>
                  <pre className="mt-1 max-h-40 overflow-auto whitespace-pre-wrap rounded bg-surface px-2 py-1 font-mono">{m.sql}</pre>
                </details>
              )}

              {/* 출처 + 복사/좋아요/싫어요 액션 줄 */}
              {m.role === "assistant" && (
                <div className="mt-1.5 flex w-[92%] items-center justify-between">
                  <div>
                    {m.sources && m.sources.length > 0 && (
                      <button
                        type="button"
                        onClick={() => toggleSources(i)}
                        className="text-[11px] text-text-muted underline hover:text-text"
                      >
                        {t("ai.chat.sourcesToggle", "출처 {{count}}건", { count: m.sources.length })} {expandedSources.has(i) ? "▲" : "▼"}
                      </button>
                    )}
                  </div>
                  <div className="flex items-center gap-1">
                    <button
                      type="button"
                      onClick={() => copyMessage(i, m.content)}
                      title={t("ai.chat.copy", "복사")}
                      className="rounded p-1 text-text-muted hover:bg-surface hover:text-text"
                    >
                      {copiedIdx === i ? <Check className="h-3.5 w-3.5 text-green-600" /> : <Copy className="h-3.5 w-3.5" />}
                    </button>
                    <button
                      type="button"
                      onClick={() => rate(i, m, "LIKE")}
                      title={t("ai.chat.like", "좋아요")}
                      className={`rounded p-1 hover:bg-surface ${feedbackByIdx.get(i)?.rating === "LIKE" ? "text-primary" : "text-text-muted hover:text-text"}`}
                    >
                      <ThumbsUp className="h-3.5 w-3.5" />
                    </button>
                    <button
                      type="button"
                      onClick={() => rate(i, m, "DISLIKE")}
                      title={t("ai.chat.dislike", "싫어요")}
                      className={`rounded p-1 hover:bg-surface ${feedbackByIdx.get(i)?.rating === "DISLIKE" ? "text-red-500" : "text-text-muted hover:text-text"}`}
                    >
                      <ThumbsDown className="h-3.5 w-3.5" />
                    </button>
                  </div>
                </div>
              )}
              {m.role === "assistant" && m.sources && m.sources.length > 0 && expandedSources.has(i) && (
                <div className="mt-1 w-[92%] space-y-1 rounded-lg border border-border bg-surface/60 p-2">
                  {m.sources.map((source, si) => (
                    <button
                      key={si}
                      type="button"
                      onClick={() => openSource(source)}
                      className="block w-full rounded px-1.5 py-1 text-left text-[11px] text-text-muted hover:bg-surface hover:text-text"
                    >
                      <span className="font-medium text-text">{source.title ?? source.menuCode ?? source.sourcePath}</span>
                      {source.heading ? ` > ${source.heading}` : ""}
                    </button>
                  ))}
                </div>
              )}
            </div>
          ))
        )}
```

- [ ] **Step 6: 타입체크**

Run: `pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false`
Expected: 0 errors.

- [ ] **Step 7: Commit**

```bash
git add apps/frontend/src/components/ai/AiChatPanel.tsx apps/frontend/src/locales/ko.json apps/frontend/src/locales/en.json apps/frontend/src/locales/zh.json apps/frontend/src/locales/vi.json
git commit -m "feat(ai): 채팅 응답에 출처 목록, 복사, 좋아요/싫어요 버튼 추가"
```

---

### Task 14: 브라우저 수동 검증 (End-to-End)

**Files:** 없음(검증 전용 태스크)

**Interfaces:**
- Consumes: Task 1~13 전체.

- [ ] **Step 1: 프론트/백엔드 전체 typecheck 재확인**

Run:
```
pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false
pnpm.cmd --filter @harness/backend exec tsc --noEmit --pretty false
```
Expected: 둘 다 0 errors.

- [ ] **Step 2: SHIP_ORDER 관련 질문으로 출처 확인**

브라우저에서 로그인 후 AI 채팅에 "출하지시 확정취소 언제 가능해?" 질문 → 응답 하단에 "출처 N건 ▾" 노출 확인 → 클릭해서 펼치고 목록 확인 → 항목 클릭 → 우측에서 도움말 패널이 `SHIP_ORDER` 문서로 열리고 해당 섹션(`입력 규칙 / 검증` 또는 `자주 묻는 질문`)으로 스크롤되는지 확인.

- [ ] **Step 3: 좋아요/싫어요 DB 저장 확인**

응답에서 👍 클릭 → 아이콘이 강조 표시되는지 확인 후:
```bash
python C:/Users/hsyou/.claude/skills/oracle-db/scripts/oracle_connector.py --site JSHANES --query "SELECT feedback_id, rating, menu_code, created_by, created_at FROM ai_chat_feedbacks ORDER BY feedback_id DESC FETCH FIRST 5 ROWS ONLY"
```
Expected: 방금 클릭한 행이 `RATING='LIKE'`로 존재.

다시 같은 👍 버튼 클릭(토글 해제) → 같은 쿼리 재실행 → 해당 행이 삭제되어 없는지 확인.

- [ ] **Step 4: 복사 버튼 확인**

응답의 복사 아이콘 클릭 → 아이콘이 체크로 잠깐 바뀌는지 확인 → 아무 텍스트 입력창에 붙여넣기 → 응답 마크다운 원문이 그대로 복사됐는지 확인.

- [ ] **Step 5: 최종 커밋 여부 확인**

Run: `git status --porcelain=v1`
Expected: 이 계획으로 변경한 파일 중 커밋 안 된 게 없어야 한다(각 태스크에서 이미 커밋했으므로 보통 clean).
