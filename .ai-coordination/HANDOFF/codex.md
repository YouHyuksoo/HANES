# codex Handoff

## Last Update

2026-06-26 KST

## Current Review

- `T-IQC-AQL-ISO-REDESIGN`: `/quality/aql`을 ISO 2859 구조로 즉시 전환했다. 신규 테이블은 `AQL_CODE_LETTER_RULES`(LOT+검사수준 -> Code Letter), `AQL_CODE_LETTER_SAMPLES`(Code Letter -> 표준 Sample Size), `AQL_ACCEPTANCE_RULES`(Code Letter+AQL -> Sample Code/Ac/Re)이다. AQL resolve는 표준 샘플수량과 실제 검사수량을 분리해 `standardSampleSize`, `actualInspectQty`를 반환한다. `/quality/aql` UI는 `AQL 정책관리`, `AQL 기준`, `Code Letter 표`, `Sampling Plan 표` 4탭으로 재구성했고 구형 LOT별 판정기준 직접입력 UI는 제거했다. JSHANES migration 적용 및 대표값 검증 완료: rules 105, samples 16, acceptance 62, `LOT 350 + II -> H`, `H + AQL 1.0 -> Ac1/Re2`. 검증: backend focused spec, frontend structure test, FE/BE tsc, ERD regen, 3002 Playwright 탭 전환 PASS. 커밋하지 않았다. 이전 `T-IQC-AQL-SAMPLE-SIZE-NORMALIZE`의 LOT 상한 cap 보정 방향은 폐기됐다.
- 후속 보강: 사용자가 Code Letter 표 빈 화면을 보고해 확인했다. DB/API/3002 런타임에는 105건이 표시됐고, 초기 `/iso` 호출 실패 시 빈 상태로 남는 경우를 막기 위해 Code Letter/Sampling Plan 탭 진입 시 `fetchIsoTables()`를 재호출하고 실패 toast를 표시하도록 보정했다. 검증: structure test, FE tsc, JSHANES count 105, 3002 Playwright `전체 105건` 확인.
- 후속 UI 보정: `Code Letter 표`와 `Sampling Plan 표`를 일반 DataGrid가 아니라 사용자가 제공한 ISO 이미지형 행/열 매트릭스 표로 변경했다. Code Letter는 LOT Size x `I/II/III/S1~S4`, Sampling Plan은 Code Letter x AQL `Ac/Re` 표다. 구조 테스트/FE tsc/3002 Playwright 캡처 확인 PASS.
- `T-IQC-AQL-SAMPLE-SIZE-NORMALIZE`: 후속 사용자 정정으로 폐기됐다. `SAMPLE_SIZE > LOT_QTY_TO`를 비정상으로 보고 LOT 상한으로 cap하는 방향은 ISO 2859 해석과 맞지 않는다. 관련 correction migration/test는 삭제했고, seed 주석/값은 표준 샘플수량 의미로 되돌렸다. 신규 기준은 `T-IQC-AQL-ISO-REDESIGN`이다.
- `T-IQC-CERT-OPTIONAL`: `/material/iqc` 검사성적서 첨부 강제 정책 해제 완료 후 REVIEW. 실제 강제는 IQC 저장이 아니라 `ReceivingService`의 입고 가능/입고 확정 정책이었다. 성적서 없는 PASS IQC 대상도 `findReceivable()`에서 차단 사유 없이 내려가고 `createBulkReceive()`가 통과하도록 보정했다. 성적서 업로드 API와 `certFilePath` 저장은 유지했다. `/material/receive` 그리드는 첨부 파일이 있으면 `certRequired=false`여도 `첨부`를 먼저 표시한다. 검증: backend focused spec, 신규 frontend structure test, FE/BE tsc, diff check PASS. 커밋하지 않았다.
- `T-TRACE-WEBDISPLAY-WIZARD`: `/quality/trace`를 WebDisplay식 추적 시작 모달 UI로 전환했다. 페이지 진입 시 `TraceSearchWizard`가 자동 오픈되고, 사용자는 제품 바코드/자재 UID·LOT/박스/팔레트/출하지시/설비+기간/작업지시/SG 바코드 중 하나를 카드로 선택한다. 조회 후 좌측 후보 목록을 표시하고, FG 후보 선택 시 기존 `GET /quality/trace?serial=` 제조이력 상세를 중앙에 표시한다. 신규 `GET /quality/trace/candidates`는 HANES 실제 데이터 키(`FG_LABELS`, `SG_LABELS`, `PRODUCT_GENEALOGY`, `MAT_ISSUES`, `BOX_MASTERS`, `PALLET_MASTERS`, `SHIPMENT_ORDERS`, `PROD_RESULTS`) 기준으로 후보를 만든다. locale 파일은 건드리지 않고 fallback 문자열만 사용했다. 검증: 구조 테스트 RED/GREEN, FE/BE tsc, diff check PASS. 기존 3002 `/quality/trace`는 5초 HTTP 타임아웃이라 브라우저 확인은 못 했고, 대체 포트/서버는 사용하지 않았다. `T-TRACE-FULL` active lock과 겹쳤으나 사용자 진행 승인 후 최소 범위로 작업했다.
- `T-SHIP-ORDER-UNCONFIRM`: `/shipping/order` 확정취소 기능을 추가했다. `CONFIRMED` 행/패널에서 `PUT /shipping/orders/:id/unconfirm`을 호출한다. 서버는 `CONFIRMED` + 품목 `shippedQty=0`일 때 동작하며, 빈 `OPEN` 팔레트만 있으면 같은 transaction에서 팔레트를 삭제하고 `DRAFT`로 되돌린다. 박스/출하수량/마감 이후 팔레트가 있으면 차단한다. 후속으로 `CONFIRMED` 삭제 버튼 노출 때문에 발생한 `DELETE /shipping/orders/:id` 400도 보정해 삭제 버튼/handler는 `DRAFT`만 허용한다. `/shipping/pallet`에는 `OPEN + boxCount 0 + shipmentId 없음` 조건의 `빈 팔레트 삭제` 버튼을 추가했다. JSHANES 실측 `SH2606220004`는 빈 `OPEN` 팔레트 `PLT2606220002`만 있어 새 로직 대상이다. 실제 unconfirm 재호출은 DB 상태 변경이라 수행하지 않았다. 검증: backend focused spec 31/31 PASS, frontend structure tests PASS, FE/BE tsc PASS, diff check PASS. `ship-order.service.ts`는 `T-SHIP-ORDER-CANCEL` active lock과 겹쳤으나 사용자 직접 후속 지시로 최소 변경했다.

## Latest Done

- `T-DB-LOCAL-BACKUP-20260626` 완료. `oracle-db` JSHANES 프로필 접속 사용자는 `TEST`, 스키마도 `TEST`로 확인했다. classic Oracle `exp.exe`로 로컬 백업을 생성했고 산출물은 `db_backups/JSHANES_TEST_20260626_213823.dmp`, `.log`, `.zip`이다. zip SHA256은 `B23B9AF920EA896FF16092FF04BDD7BF0A8846BF78006E7C22609581E1086F38`. 로그는 `Export terminated successfully with warnings.`로 끝나며 `ORA-01455`/`ORA-01403` 경고가 있다. 이는 기존 `20260519` 백업 로그와 같은 계열 경고다.
- `T-PRODUCT-RECEIVE-BX2606260001-CLEANUP` 완료 후 REVIEW. 사용자가 `BX2606260001` 제품입고 후 취소했지만 이미 입고된 박스로 판단된다고 요청해 JSHANES를 확인했다. 원인 행은 `PRODUCT_TRANSACTIONS.PTX2026062600001 / WIP_OUT / REF_TYPE=BOX / REF_ID=BX2606260001 / STATUS=DONE`이며, 이 행이 `receiveFinishedFromWip()` 이중입고 가드에 걸렸다. 신규 SQL `apps/backend/src/migrations/2026-06-26_cleanup_product_receive_bx2606260001.sql`로 해당 전표 및 취소참조 전표를 삭제하고, `FG_MAIN / N91H00-X9800` 5개를 제거한 뒤 `WIP_MAIN / N91H00-X9800` 가용 5개를 복원했다. 박스 `CLOSED / QTY=5`와 FG 라벨 `PACKED / BOX_NO=BX2606260001`은 유지했다. post-check: 정상 BOX 전표 0, duplicate guard count 0, WIP_MAIN 가용 5. 남은 의심: 제품입고 취소가 원본 WIP_OUT을 CANCELED 처리하지 않는 버그가 있어 후속 코드 수정 필요.
- `T-PRODUCT-RECEIVE-BX2606260001-WIP-SEED` 완료 후 REVIEW. `/product/receive`의 `BX2606260001` 입고 오류 원인은 `POST /inventory/fg/receive`가 `WIP_MAIN / N91H00-X9800` 제품재고를 먼저 `WIP_OUT` 차감하는데 JSHANES `PRODUCT_STOCKS`에 해당 재고가 0건이어서 발생했다. 신규 SQL `apps/backend/src/migrations/2026-06-26_seed_wip_stock_bx2606260001_n91h00.sql`을 적용해 `WIP_MAIN / N91H00-X9800 / QTY=5 / AVAILABLE_QTY=5 / STATUS=NORMAL`과 추적용 `PTX-SEED-BX2606260001-WIP` 전표(`REF_TYPE=SEED`)를 만들었다. `BX2606260001` 정상 BOX 전표는 아직 0건이라 사용자가 UI에서 재시도 가능하다. SQL 적용/재실행, post-check, diff check PASS.
- `T-SHIP-PACK-BX2606260001-SEED` 완료 후 REVIEW. JSHANES에 `/shipping/pack`용 시드 `apps/backend/src/migrations/2026-06-26_seed_pack_bx2606260001_n91h00.sql`을 추가/적용했다. 기존 `BOX_MASTERS.BX2606260001`은 이미 `OPEN / N91H00-X9800 / QTY=0 / SERIAL_LIST IS NULL`였고, 부족한 `FG_LABELS` 포장대기 데이터를 `FG-N91-X9800-001`~`005` 5건으로 생성했다. 상태는 모두 `VISUAL_PASS / INSPECT_PASS_YN='Y' / BOX_NO NULL`이며 `MAT_LOTS` 같은 바코드 0건이라 `BoxService.addSerial()` 검증 충돌도 없다. SQL 적용/재실행, packable 조건 post-check, diff check PASS.
- `T-RECEIVE-HISTORY-CONCESSION-FLAG` 구현 후 REVIEW. `/material/receive-history` 그리드에 `특채여부` 컬럼을 추가했다. `/material/receiving` 이력 응답은 LOT 기준 `IQC_STATUS='FAIL' AND SPECIAL_ACCEPT_YN='Y'`이면 `isConcession=true`와 `specialAcceptYn`, `lot.iqcStatus`, `lot.specialAcceptYn`을 내려준다. `ReceivingHistoryTable`은 `isConcession` 값으로 `특채`/`일반` 배지를 표시한다. `receiving.service.ts`는 `T-RECEIVE-LOCATION` active lock과 겹쳤으나 사용자 요청에 따라 `findAll()` 반환부만 최소 수정했다. 검증: backend focused spec, frontend structure tests, FE/BE tsc, diff check PASS. 3002 브라우저 수동 확인은 미실행.
- `T-CONCESSION-WORKER` 후속 UI 정리 완료 후 REVIEW. `/material/concession` 특채 승인 모달의 작업자 입력을 QR 스캔 입력과 작업자 선택이 나란히 있는 단일 영역으로 정리했다. 둘 중 어느 방식이든 동일한 `specialAcceptWorkerCode`만 채우며, 저장/검증/DB 컬럼 동작은 선행 구현 그대로 유지한다. 검증: frontend structure tests, FE tsc, diff check PASS.
- `T-CONCESSION-WORKER` 구현 완료 후 REVIEW. `/material/concession` 특채 승인 모달에 공용 `WorkerSelect` 특채처리작업자 선택과 작업자 QR 스캔 입력을 추가했고, 목록에도 특채처리자 컬럼을 표시한다. QR 스캔은 `GET /master/workers/by-qr/:qrCode`로 조회해 응답 `workerCode`를 기존 `specialAcceptWorkerCode` 저장 필드에 채운다. 서버는 `specialAcceptWorkerCode` 필수/활성 작업자 검증 후 `MAT_LOTS.SPECIAL_ACCEPT_WORKER_CODE`에 저장하고, 특채 취소 시 null로 복원한다. JSHANES에 `SPECIAL_ACCEPT_WORKER_CODE` 컬럼, `IDX_MAT_LOTS_SA_WORKER`, `FK_MAT_LOTS_SA_WORKER`를 적용했고 재실행 성공. `useWorkerOptions()`는 실제 `/master/workers` 응답의 `workerCode`를 select value로 쓰도록 보정했다. 검증: backend focused spec, frontend structure tests, JSHANES pre/post, ERD regen, FE/BE tsc, diff check PASS. 3002 브라우저 수동 확인은 미실행.
- `T-IQC-AQL-S1-0015-DATA` 완료. `DLMLS6-3-3` active IQC 검사항목이 `S-1 / 0.015`를 참조하지만 JSHANES `AQL_STANDARDS`/`AQL_SAMPLING_RULES`에 해당 조합이 없어 `/quality/aql/resolve-iqc-items` 404가 발생했다. 신규 migration `apps/backend/src/migrations/2026-06-26_aql_standard_s1_0_015.sql`을 추가하고 JSHANES에 적용/재실행했다. post-check: `AQL-S-1-0.015` 1건, sampling rule 15건, LOT 100은 `91~150`, `CODE_LETTER=B`, `SAMPLE_SIZE=80`, `ACCEPT_QTY=0`, `REJECT_QTY=1`로 매칭. 현재 listen 포트는 3002 프론트뿐이라 실제 API 호출은 생략했고 임의 백엔드/대체 포트는 띄우지 않았다.
- `T-IQC-AQL-STANDARD-GUARD` 완료. `/master/iqc-part-spec`의 검사항목 행 편집은 이제 `GET /quality/aql?useYn=Y&limit=5000` 기준으로 실제 등록된 `AQL_STANDARDS` 조합만 검사수준/AQL 선택지에 표시한다. 매칭 AQL 값이 없으면 `AQL 기준관리에서 먼저 등록하세요`를 표시하고 행 확정을 막는다. 백엔드 `IqcPartSpecService.upsert()`도 저장 전 활성 AQL 기준과 sampling rule 존재를 다시 검증해 화면 우회 저장을 400으로 차단한다. 검증: 신규 RED/GREEN structure test, backend focused spec, FE/BE tsc, diff check PASS. JSHANES active IQC AQL 조합 미매칭 0건.
- `T-SHIP-WORKFLOW-QA` 완료. `tools/hanes-shipping-workflow-scenario-qa.mjs`를 추가해 출하관리 8개 화면(`/shipping/order`, `/shipping/pack`, `/shipping/pallet`, `/shipping/pallet-ship`, `/shipping/confirm`, `/shipping/return`, `/shipping/history`, `/shipping/box-stock`)을 Playwright로 열고 API/버튼/스크린샷을 수집했다. 새 테스트 키 `SWF260625235757`로 출하지시 DRAFT/CONFIRMED 정역전이, 박스 OPEN/CLOSED/SHIPPED/취소, 팔레트 OPEN/CLOSED/재오픈/박스 제거, 후속 box-stock 제외/재노출과 shipped-detail 노출을 실제 API/DB로 확인했다. 최종 테스트 잔여 0건. 리포트: `docs/reports/hanes-shipping-workflow-scenario-qa-2026-06-25/index.html`. 발견: 일반 `shipping/shipments` 전이 API는 현재 출하관리 메뉴 페이지 연결이 없고, `OQC_ENABLED=N`이어도 팔레트 경로와 `/shipping/confirm` 후보 조회는 `OQC_STATUS=PASS`를 요구한다.
- `T-MANUAL-INDEX-PAGE` 완료. `docs/manuals/index.html`을 추가해 `docs/manuals`의 7개 매뉴얼 그룹(기준정보/자재/생산/품질/검사/제품수불/출하)과 104개 화면을 검색 가능한 정적 목차로 묶었다. 각 화면 행은 해당 매뉴얼의 `#screen-N` 앵커로 이동한다. 검증: HTML script syntax/runtime smoke, embedded link existence, result total 104 일치, diff check PASS.
- `T-REMAINING-HELP-MANUALS` 완료. `.ai-coordination/TASK-quality-manual.md`, `TASK-inspection-manual.md`, `TASK-product-mgmt-manual.md`, `TASK-shipping-manual.md` 기준으로 user 도움말 36개/operator 도움말 30개를 새로 작성했고 `apps/frontend/public/help/manifest.json`을 보강했다. 공식 `help-manual-export-runner.mjs`를 3002 기준으로만 재시도해 `docs/manuals/hanes-quality-manual-2026-06-24.html`, `hanes-inspection-manual-2026-06-24.html`, `hanes-product-mgmt-manual-2026-06-24.html`, `hanes-shipping-manual-2026-06-24.html` 및 각 `.result.json`을 생성했다. 최종 result는 quality `total=25`, inspection `total=6`, product `total=4`, shipping `total=9`, 4개 모두 `missingHelp=[]`, `missingCapture=[]`. 추가 포트/대체 runner/우회 캡처는 사용하지 않았다.
- `T-PRODUCTION-HELP-MANUAL` 완료. `.ai-coordination/TASK-production-manual.md` 기준 생산관리 19개 화면의 누락 도움말을 보강했다. 신규 user 도움말 15개, operator 도움말 12개, `apps/frontend/public/help/manifest.json` 보강을 완료했고, 공식 `help-manual-export-runner.mjs`를 3002 기준으로 실행해 `docs/manuals/hanes-production-manual-2026-06-24.html` 및 `.result.json`을 생성했다. 검증 결과 runner JSON `total=19`, `missingHelp=[]`, `missingCapture=[]`, 도움말/manifest 검증 errors 0건, diff check PASS. 추가 포트는 사용하지 않았다.
- `T-MATERIAL-HELP-MANUAL` 완료. `.ai-coordination/TASK-material-manual.md` 기준 자재관리 26개 화면의 누락 도움말을 보강했다. 신규 user 도움말 13개, operator 도움말 10개, `apps/frontend/public/help/manifest.json` 보강을 완료했고, 공식 `help-manual-export-runner.mjs`로 `docs/manuals/hanes-material-manual-2026-06-24.html` 및 `.result.json`을 생성했다. 검증 결과 runner JSON `total=26`, `missingHelp=[]`, `missingCapture=[]`, 도움말/manifest 검증 errors 0건, diff check PASS. 3002 캡처 타임아웃 때문에 3004 임시 dev 서버를 사용했다.
- `T-ALL-MENU-QA` 완료. `menuConfig.ts` 등록 메뉴 157개를 실제 브라우저로 순회해 최종 `PASS 157 / FAIL 0 / MISSING 0`까지 맞췄고, 누적 요약은 `docs/reports/hanes-all-menu-scenario-qa-summary-2026-06-23/index.html`에 남겼다. 중간에 잡힌 `INSP_RESULT`는 Turbopack HMR 콘솔 노이즈와 `login?_rsc` aborted request를 러너에서 제외하는 보정으로 정리했다.
- `T-ALL-MENU-QA` 진행 중 체크포인트. `tools/hanes-all-menu-page-scenario-qa.mjs`를 추가해 `menuConfig.ts` 등록 메뉴를 실제 브라우저로 열고 route/API/console/request failure/화면 기능 목록/스크린샷/페이지 HTML을 수집한다. smoke 검증은 `HANES_REPORT_DATE=2026-06-23-smoke HANES_MENU_LIMIT=3 node tools/hanes-all-menu-page-scenario-qa.mjs`로 `/dashboard`, `/workflow`, `/equipment/status` 3/3 PASS. 산출물은 `docs/reports/hanes-all-menu-scenario-qa-2026-06-23-smoke/`. 다음 단계는 `HANES_REPORT_DATE=2026-06-23-all-menu-sweep-1 node tools/hanes-all-menu-page-scenario-qa.mjs` 전체 실행 후 실패 메뉴별 수정-재테스트다. 아직 전체 메뉴 상세 기능 완료가 아니다.
- `T-ER-VIEW-DROP-FK` 완료. `/system/er-view` 물리 FK 관계 선택 시 `FK 제거 후보` 버튼을 노출하고 `DROP_FK` dry-run/execute action을 추가했다. 서버는 `constraintName`과 `childTable`이 최신 snapshot의 실제 FK와 일치할 때만 `ALTER TABLE <child> DROP CONSTRAINT <fk>`를 생성한다. PK/UK 제거는 범위 제외, 실제 FK DROP 실행은 하지 않았다. 검증: backend ER VIEW spec 20/20, ER VIEW structure 12/12, FE/BE tsc, diff check, 3002 `/system/er-view` HTTP 200 PASS.
- `T-ACTIVITY-LOGS-NAME-COLUMN` 완료. JSHANES `ACTIVITY_LOGS.USER_NAME`을 `NAME`으로 rename했고 `ActivityLog.userName` property는 `@Column({ name: 'NAME' })`로 매핑한다. post-check: `ACTIVITY_LOGS` 사용자 컬럼은 `EMAIL`, `NAME`만 존재. ERD/live schema 재생성 완료. 검증: migration 적용/재실행, activity-log+er-view spec, FE/BE tsc, diff check PASS.
- `T-ACTIVITY-LOGS-EMAIL-COLUMN` 완료. 사용자 정정에 따라 `ACTIVITY_LOGS.USER_EMAIL` 물리 컬럼을 `EMAIL`로 rename했고, FK도 `FK_ACTIVITY_LOGS_EMAIL: ACTIVITY_LOGS.EMAIL -> USERS.EMAIL ENABLE VALIDATE`로 재생성했다. 이전 `FK_ACTIVITY_LOGS_USER_EMAIL`은 drop됐다. `ActivityLog.userEmail` property는 API/서비스 호환상 유지하되 `@Column({ name: 'EMAIL' })`로 매핑한다. ER VIEW 추정은 `ACTIVITY_LOGS.EMAIL -> USERS.EMAIL`로 변경. JSHANES post-check: `USER_EMAIL` 컬럼 0, `EMAIL` 컬럼 1, FK `ENABLED/VALIDATED`, orphan 0. 검증: migration 적용/재실행, activity-log+er-view spec, ERD regen, live schema regen, diff check PASS. 전체 backend tsc는 기존 `ai-page-tools.service.spec.ts` 생성자 인자 오류로 실패.
- `T-ACTIVITY-LOGS-USERS-FK` 완료. JSHANES `ACTIVITY_LOGS.USER_EMAIL -> USERS.EMAIL` 물리 FK `FK_ACTIVITY_LOGS_USER_EMAIL`을 `ENABLE VALIDATE`로 생성했고 재실행 PASS. orphan 0, FK post-check `ENABLED/VALIDATED`. ER VIEW 추정 로직에는 `USER_EMAIL -> USERS`, parent column `EMAIL` 매핑을 추가했고, `USERS.EMAIL` 참조는 tenant 컬럼을 자동 조합하지 않는다. ERD 문서 재생성 완료. 검증: migration 적용/재실행, backend ER VIEW spec, backend tsc, ERD regen, diff check PASS. 별도 발견: `USER_AUTHS.USER_EMAIL` 주석의 과거 표현 `USERS.ID`는 이번 범위에서 미수정.

## Current Review

- `T-MASTER-TENANT-PK`: 완료 후 active task/lock 제거. JSHANES backup 제외 `COMPANY/PLANT_CD` 보유 마스터 테이블 19개 모두 PK 선두가 `COMPANY,PLANT_CD`가 되도록 변경했다. 신규 migration `apps/backend/src/migrations/2026-06-23_master_tenant_composite_pk.sql` 적용/재실행 PASS. 관련 FK 7개는 tenant composite FK로 재생성되어 ENABLED/VALIDATED. 마스터 entity의 `COMPANY`/`PLANT_CD`를 `@PrimaryColumn`으로 변경했고, `create-hanes-schema.sql`, `docs/reports/db-schema-erd.md`, 결정 `D-20260623-MASTER-TENANT-PK` 갱신. 검증: DB pre/post, ERD regen, backend build, er-view spec 16/16, ER VIEW structure 9/9, FE tsc, diff check PASS. 전체 backend `tsc --noEmit`는 기존 `ai-page-tools.service.spec.ts` union type 오류로 실패.
- `T-BOX-ID-TO-BOX-NO`: 완료 후 active task/lock 제거. JSHANES에서 `TRACE_LOGS.BOX_ID -> BOX_NO`, `WORKER_ID` 물리 컬럼 20개 테이블을 `WORKER_NO`로 rename했다. `REPAIR_ORDERS` 인덱스는 `IDX_REPAIR_ORDERS_WORKER_NO(WORKER_NO)`로 rename. 관련 entity 매핑, `WipMatStockService.findTransactions()` raw select, ER VIEW `BOX_NO`/`WORKER_NO -> WORKER_CODE` 추정관계, `create-hanes-schema.sql`, `docs/reports/db-schema-erd.md` 갱신 완료. DB post-check는 `BOX_ID/WORKER_ID` 0개, `BOX_NO` 4개, `WORKER_NO` 20개. `ASSIGNED_WORKER_ID`는 PM 작업지시 별도 의미라 유지. 검증: worker migration 적용/재실행, ERD 재생성, backend er-view spec 16/16, wip-mat-stock spec 11/11, ER VIEW structure 9/9, FE/BE tsc, diff check PASS. 커밋하지 않았다.
- `T-ER-VIEW-NODE-COLUMN-SCROLL`: `/system/er-view` 테이블 노드 컬럼 스크롤/확인문구/추정 관계 선 유형/중복 제약 실행 방어 완료. 원인은 테이블 노드가 `data.columns.slice(0, 12)`로 컬럼을 잘라 렌더링하고 `overflow-hidden`으로 막은 것, 확인 문구가 constraint별 긴 문구였던 것, 추정 edge 유형이 고정이었던 것, backend cwd에서 migration 경로가 `apps/backend/apps/backend/src/migrations`로 중복 조립된 것, 이미 존재하는 FK/UK 후보가 다시 실행될 수 있던 것이다. 변경: 컬럼 전체 렌더링+노드 내부 세로 스크롤, 공통 확인 문구 `실행`, 추정 관계 선 유형 `곡선/완만한 계단/계단/직선`, repo/backend cwd 양쪽 migration 경로 보정, ADD_FK/ADD_UK preview 최신 snapshot 기반 중복 FK/UK/constraint name 차단, Oracle `ORA-02275/02264/02261` BadRequest 변환, 물리 FK 관계에서는 `이미 물리 FK`로 DDL 버튼 비활성화. JSHANES 실측상 `CONSUMABLE_STOCKS -> CONSUMABLE_MASTERS`는 `FK_CONSUMABLE_S_CONSUMABLE_C`가 이미 `ENABLED/VALIDATED` 상태다. 검증: ER VIEW 구조 테스트 9/9, backend er-view spec 11/11, FE/BE tsc, diff check, Chrome headless 3002에서 선 유형 선택 및 `IQC_LOGS` 컬럼 스크롤 확인. DDL 추가 실행 없음.
- `T-ER-VIEW-PARENT-UK-FLOW`: `/system/er-view`에서 부모 PK/UK 없는 FK 후보 실행 흐름 보정 완료. 사용자 에러 `IQC_LOGS(COMPANY, PLANT_CD, VENDOR_CODE) -> VENDOR_MASTERS(COMPANY, PLANT_CD, VENDOR_CODE)`는 JSHANES 실측상 `VENDOR_MASTERS` 키가 `PK_VENDOR_MASTERS(VENDOR_CODE)` 하나뿐이라 정상 차단된 케이스다. 해당 부모 UK 후보 조합은 duplicate 0/null 0이라 `ADD_UK` dry-run 가능하지만, FK 후보 orphan은 1건이라 UK 후에도 orphan 정리 전 FK `ENABLE VALIDATE`는 차단된다. 프론트는 `parentKeyReady=false`일 때 안내문과 `부모 UK 후보` 버튼을 표시하고 `FK DDL 후보`를 disabled 처리한다. `previewPayload`로 dry-run payload를 execute에 그대로 넘겨 `ADD_UK` 실행 payload도 보존한다. 백엔드는 DDL 성공 후 schema snapshot cache를 무효화한다. 검증: JSHANES 키/중복/null/orphan 실측, ER VIEW 구조 테스트 6/6, backend er-view spec 7/7, FE/BE tsc, diff check, Chrome Playwright UI 및 `ADD_UK` dry-run 201 확인. DDL/DB 변경은 하지 않았다.
- `T-ER-VIEW-INTERACTION-FIX`: `/system/er-view` 그래프 조작/하단 잘림 보정 완료. 원인은 ReactFlow 전역 CSS 누락으로 viewport/node 레이어가 깨져 hit target이 SVG rect로 잡힌 것과, controlled `nodes`에 `onNodesChange`가 없어 드래그 좌표가 상태에 반영되지 않은 것. `globals.css`에 `@xyflow/react/dist/style.css`를 import했고, `page.tsx`는 `useNodesState`/`onNodesChange`, `nodeDragRef` click guard, `minZoom={0.05}`, `maxZoom={2.5}`, `nodesDraggable`, `nodesConnectable={false}`를 적용했다. 검증: ER VIEW 구조 테스트 5/5 PASS, FE tsc PASS, git diff --check PASS, Chrome Playwright 1440x900에서 `MAT_LOTS` 휠 줌 transform 변경/노드 드래그 좌표 변경/body overflow 없음, 1366x768에서 캔버스 하단 viewport 맞춤 및 줌아웃 변경 확인. DB/백엔드 변경 없음. ER VIEW page/test는 이전 작업부터 git 미추적 상태이므로 커밋 시 포함 범위 확인 필요.
- `T-ER-VIEW-SCHEMA-GOVERNANCE`: `/system/er-view` 실시간 DB 스키마 시각화/관계 분석/실행 도구 1차 구현 완료 후 REVIEW. 백엔드는 `ErViewController`/`ErViewService`로 `/api/v1/system/er-view/summary|tables|graph|relationships|actions/*`를 제공한다. 실시간 Oracle `USER_*` snapshot 기준으로 물리 PK/FK/UK와 보수적 추정 관계를 분리하고, 테넌트 포함 FK 우선/부모 PK·UK/orphan/rule-based risk를 계산한다. 실행은 raw SQL 수신 없이 구조화 action payload만 허용하고, FK/UK는 `ENABLE VALIDATE` 기본, DEV 모드 migration 파일 생성 + ERD 재생성 + `logs/schema-governance/*.jsonl` 로그 경로를 넣었다. 프론트는 `@xyflow/react` 3패널 UI(테이블 목록/그래프/관계 상세)와 orphan scan/DDL preview/확인 문구 실행 흐름을 제공한다. 검증: backend er-view spec 4/4, frontend ER VIEW structure 3/3, FE/BE tsc, diff check, 3002 `/system/er-view` HTTP 200, 3003 `/api/v1/system/er-view/summary` 401 인증 게이트 PASS. 실제 인증 세션으로 summary 데이터 렌더링/orphan scan 실행은 미실행. 작업 중 발견된 무관 변경 `system/config/page.tsx`, `AiCatalogPanel.tsx`, help 문서 4개는 되돌리지 않았다. 커밋하지 않았다.
- `T-AI-PAGE-TOOL-WORKFLOW`: 구현 완료 후 active task/lock 제거. HANES 전체 화면에 반복 적용할 AI Page Tool Workflow 표준 문서와 `/production/order` draft-only 파일럿을 반영했다. 설계는 `docs/superpowers/specs/2026-06-22-ai-page-tool-workflow-design.md`, 계획은 `docs/superpowers/plans/2026-06-22-ai-page-tool-workflow.md`. 백엔드는 `AiPageToolsModule`로 manifest/read-only 후보조회 API를 제공하고, 프론트는 `usePageAiTools`, `PageToolInspector`, `PageToolExecutionLog`, AI 패널 `채팅|도구|실행로그`, `/production/order` `도구보기` 버튼과 `applyJobOrderDraft` 초안 반영 도구를 추가했다. AI 채팅은 `pageToolContext`를 받아 등록/생성류 요청을 SQL write가 아닌 도구 절차 안내로 보낸다. 작업지시 신규번호는 서버 자동채번과 맞춰 DTO/order form을 optional로 보정했다. 검증: AI page tool 구조 테스트, production order 구조 테스트, backend ai-page-tools spec, job-order service spec, FE/BE tsc PASS. 커밋하지 않았다.

## In Review

- `T-WORKFLOW-BUSINESS-MAP`: `/workflow`를 기존 카드형 건수 대시보드에서 `@xyflow/react` 기반 업무 이해용 스윔레인 캔버스로 재구성했다. 정적 정의는 `apps/frontend/src/config/workflowMap.ts`에 있으며 6개 레인/29개 업무 활동/업무 산출물 edge를 포함한다. 노드 클릭은 우측 상세 패널 갱신이며, 관련 화면 버튼으로만 route 이동한다. `/workflow/summary`와 `PKG_WORKFLOW` 건수 흐름은 제거/미사용. 사용자 피드백 후 기본 viewport를 `x=180, y=28, zoom=0.62`로 조정해 스윔레인 설명이 잘리지 않게 했고, 출하 구간 좌표 겹침과 미니맵 가림을 제거했다. 생산 레인에는 `조립실적(키오스크)`를 별도 현장 시작 노드로 추가해 `작업지시 -> 조립실적(키오스크) -> 서브공정 키팅/조립·라벨 실적` 흐름을 명시했다. 장거리 보조 연결은 기본 숨김이며 선택 업무 주변 또는 `보조 연결 보기`에서만 노출한다. 검증: 구조 테스트 PASS, FE tsc PASS, 3002 실제 로그인 후 `/workflow` HTTP 200, 업무 노드 29개/레인 6개/키오스크 노드 표시/겹침 0/건수 배지 0/console error 0 확인. 캡처: `docs/reports/workflow-business-map-3002.png`. 임시 대체 포트는 사용하지 않았다. 커밋하지 않았다.

- `T-SHIP-PALLET-SCAN-CREATE`: `/shipping/pallet` 팔레트 생성 모달을 출하지시번호 스캔 입력 중심으로 바꾸고, 좌측에 `팔레트 대기중인 출하지시` 목록을 표시한다. 대기 목록은 `CONFIRMED` + 미출하 잔량 + `palletCount === 0`만 남긴다. 백엔드 `ShipOrderService.createPalletForOrder()`는 같은 출하지시 기존 팔레트가 있으면 `BadRequestException`으로 재생성을 차단한다. 팔레트 그리드 상태 컬럼은 `PalletStatusHeader`의 `?` 도움말로 `OPEN -> CLOSED -> LOADED -> SHIPPED`, `CLOSED -> OPEN` 되돌림 조건과 각 상태 의미를 제공한다. 검증: 팔레트 구조 테스트 4건 PASS, `ship-order.service.spec.ts` + `pallet.service.spec.ts` 44/44 PASS, FE/BE tsc PASS, diff check PASS. Browser 플러그인 `iab` 없음 및 Playwright 인증 토큰 부재로 3002 DOM 실측은 못 했다. 커밋하지 않았다.
- `T-SHIP-PALLET-ORDER-REQUIRED`: `/shipping/pallet`에서 출하지시 없는 팔레트 생성/구성을 막았다. 화면은 `CONFIRMED` + 미출하 잔량 출하지시를 선택해야 `POST /shipping/orders/:shipOrderNo/pallets`로 팔레트를 생성하고, 박스 적재/제거/마감도 출하지시 기준 API만 사용한다. 출하지시번호 없는 팔레트의 구성/마감 버튼은 비활성화된다. 백엔드는 일반 `PalletService.create/addBox`를 `BadRequestException`으로 차단하고, 출하지시 없는 일반 마감도 차단한다. 검증: 신규 구조 테스트 PASS, `pallet.service.spec.ts` 18/18 PASS, FE/BE tsc PASS, 3002 브라우저에서 생성 모달 출하지시 required 및 일반 `/shipping/pallets` POST 미호출 확인. 커밋하지 않았다.
- `T-SHIP-ORDER-STATUS-HELP`: `/shipping/order`와 `/shipping/history` 상태 컬럼 헤더에 `?`(`HelpCircle`) 도움말을 추가해 `DRAFT/CONFIRMED/SHIPPING/SHIPPED/CLOSED` 상태 의미를 `title`/`aria-label`로 제공했다. `/shipping/order` 등록 버그는 프론트 `canSave`와 required 표시, 백엔드 `CreateShipOrderDto`/`ShipOrderService.create()` 검증으로 고객사 출하일 누락 저장을 차단했다. 검증: order/history 구조 테스트 10건, `ship-order.service.spec.ts` 25/25, FE/BE tsc, 3002 브라우저 DOM 확인, diff check PASS. 커밋하지 않았다.
- `T-SHIP-HISTORY-PALLET-DETAIL`: `/shipping/history` 좌측 출하이력 그리드 행 선택 시 우측 팔레트 상세 패널이 기존 `/shipping/orders/:shipOrderNo/fulfillment` 데이터를 조회해 팔레트번호/상태/박스수/총수량/하위 박스를 표시한다. 날짜 필터는 당일 기본값으로 시작하고 목록 API는 `/shipping/history`를 사용한다. 상태 배지는 `DRAFT/CONFIRMED/SHIPPING/SHIPPED/CLOSED`별 로컬 색상과 `title`/`aria-label` 설명 도움말을 제공한다. 백엔드 출하이력 DTO는 `CLOSED` 상태 필터를 허용한다. 검증: 구조 테스트 3건 + backend status 구조 테스트 PASS, FE/BE tsc PASS, 3002 브라우저에서 기본날짜 `2026-06-22`, `/api/shipping/history?...` 200, `SO-RV-26061202215660` 선택 후 `PLT2606200001` 표시, 상태 도움말 확인. 커밋하지 않았다.
- `T-MASTER-LABEL-PALLET-BACKEND`: `/master/label-templates?category=pallet` 400 원인을 백엔드 DTO category enum 누락으로 확인하고 수정했다. `LABEL_TEMPLATE_CATEGORIES`에 `pallet`을 포함했고 create/query DTO와 Swagger enum이 같은 목록을 사용한다. 검증: 신규 구조 테스트 PASS, `label-template.service.spec.ts` 7/7 PASS, backend tsc PASS. 3003 직접 호출은 토큰 없음 401로 전환되어 기존 category validation 400은 무인증 경로에서 재현되지 않았다. 커밋하지 않았다.
- `T-MASTER-LABEL-PALLET-SOURCE`: `/master/label` 라벨 디자인관리 소스테이블에 `팔레트`를 추가했다. `LabelCategory`/`LabelSourceTable`에 `pallet`이 포함됐고, `labelSources.ts`에 `palletNo`, `boxCount`, `totalQty`, `status`, `shipOrderNo`, `customerName`, `itemCode`, `itemName`, `createdAt` 샘플 필드를 추가했다. 기본 팔레트 디자인은 바코드/코드=`palletNo`, 이름=`boxCount`, 보조=`totalQty`를 사용한다. 검증: 신규 팔레트 구조 테스트 + design-only 구조 테스트 PASS, FE tsc PASS, 3002 `/master/label` HTTP 200, diff check PASS. 커밋하지 않았다.
- `T-DEFECT-MODEL-GROUP-CLASSIFICATION`: 불량코드 2레벨 기준을 제품류가 아니라 품목 모델구분으로 보정했다. 최신 기준은 1레벨 `IQC/LQC/OQC`, 2레벨 `DEFECT_MODEL_GROUP`(`LV`=저전압, `HV`=고전압), 3레벨 `FUNCTION/APPEARANCE/ETC`다. `ITEM_MASTERS.DEFECT_MODEL_GROUP` 컬럼을 추가했고 JSHANES 기존 품목 36건은 `LV`로 백필했다. `GET /material/iqc-history/pending-arrivals`가 `defectModelGroup`을 반환하고, `IqcModal`은 `/quality/defect-codes/options` 호출 시 해당 값을 내부 호환 파라미터 `productType`으로 넘긴다. JSHANES 활성 분류는 1레벨 3건/2레벨 6건/3레벨 18건이며 HARNESS/WIRE/TERMINAL/CONNECTOR 기반 활성 분류 0건, 불량코드 모델구분 매핑은 LV 12건이다. 검증: item/reclass migration 재실행 PASS, ERD 재생성, 구조 테스트 26건, backend defect-code spec 4건, FE/BE tsc, diff check PASS. 3002/3003은 재시작하지 않았다. 커밋하지 않았다. 아래 `T-DEFECT-CODE-MASTER`의 이전 PRODUCT_TYPE 설명은 이 최신 보정 기준으로 대체해서 읽는다.
- `T-DEFECT-CODE-MASTER`: 불량코드를 `COM_CODES.DEFECT_TYPE`에서 분리해 전용 기준정보로 관리하도록 구현했다. 신규 테이블/엔티티/API는 `DEFECT_CATEGORY_MASTERS`, `DEFECT_CODE_MASTERS`, `DEFECT_CODE_PRODUCT_TYPES`이며, 분류는 3레벨 tree이고 불량코드는 leaf category만 참조한다. `/quality/defect-code` 페이지를 품질관리 메뉴 `QC_DEFECT_CODE`로 추가했고, 현재 화면은 좌측 전체 불량코드 그리드 + 우측 1/2/3레벨 선택 등록/수정 폼이다. 최신 분류 기준은 1레벨 `IQC/LQC/OQC`, 2레벨 `PRODUCT_TYPE`, 3레벨 `FUNCTION/APPEARANCE/ETC`이며, JSHANES 활성 분류는 1레벨 3건/2레벨 48건/3레벨 144건이다. 기존 `APPEARANCE/FUNCTION/PROCESS/MATERIAL/ETC` 계열 18건은 `USE_YN='N'` 처리했다. 등록 저장은 2레벨 코드에서 제품류를 파생해 `productTypes`를 저장하고, 옵션 조회는 제품류 매핑 없는 코드를 공통처럼 노출하지 않는다. IQC 모달 불량코드 선택, `AqlService` 불량등급 조회, 기존 `/quality/defect` 필터/등록 모달, `DefectLogService.create/update` 저장 검증까지 전용 마스터로 전환했다. 최신 검증: backend defect-code source 구조 3/3, frontend defect-code 구조 10/10, backend defect-code spec 4/4, BE/FE tsc, JSHANES `2026-06-22_reclassify_defect_categories.sql` 적용, 대상 파일 diff check PASS. 3002/3003은 사용자 요청대로 재시작하지 않았다. 커밋하지 않았다.
- `T-PRODUCTION-ORDER-EDIT-SYNC`: `/production/order` 우측 수정패널 동기화를 보정했다. 원인은 좌측 그리드 행 클릭이 `selectedRow`만 바꾸고 패널 source인 `editingOrder`를 바꾸지 않던 것. `toJobOrderFormData(row)` helper를 추가해 행→폼 매핑을 단일화했고, 수정패널이 열린 수정 모드에서 다른 행 클릭 시 `editingOrder`도 새 행으로 갱신한다. 신규 생성 패널은 행 클릭으로 덮어쓰지 않는다. 우측 폼의 `라인`/`공정` Select는 한 행 2컬럼으로 묶었다. `설비`는 드롭다운을 제거하고 `useEquipOptions(form.processCode || undefined)` 결과를 버튼형 선택 목록으로 즉시 표시한다. 공정 미선택 시 전체 설비, 공정 선택 시 해당 공정 설비를 바로 클릭해 선택한다. 검증: 구조 테스트 4/4, FE tsc, 3002 `/production/order` HTTP 200, diff check PASS. 커밋하지 않았다.
- `T-IQC-AQL-TRACEABILITY-FIX`: IQC AQL 저장 추적성/판정 의미를 보정했다. `IqcModal`은 `sampleBarcode`에 전체 시리얼 join을 보내지 않고 500바이트 이내 요약만 보낸다. 전체 시리얼/검사항목 상세는 `DETAILS` CLOB에 유지된다. FAIL 판정이 있으면 불량코드를 요구하고, FAIL 없이 불량코드만 입력한 상태는 제출을 막는다. 서버 `IqcHistoryService.createArrivalResult()`도 `details`가 모두 PASS인데 불량코드만 있는 API 저장을 `BadRequestException`으로 차단하고, 검사항목 없는 수동 FAIL은 시리얼 result FAIL을 근거로 인정한다. 서버 저장 시 `sampleBarcode`가 길면 `...(+N more)` 요약으로 `IQC_LOGS.SAMPLE_BARCODE` 500바이트 초과를 방지한다. 검증: RED→GREEN, IQC 모달 구조 5/5, `iqc-history.service.spec.ts` 20/20, IQC 관련 구조 15/15, FE/BE tsc, diff check PASS. 커밋하지 않았다.
- `T-IQC-AQL-ACTUAL-PREVIEW`: `/material/iqc` 검사 모달 AQL 미리보기를 실제 저장 판정 경로와 맞췄다. `IqcModal`은 `/quality/aql/resolve-iqc-items`를 호출하고 항목별 `검사항목 기준`, `파괴/고정`, `Ac/Re` 또는 고정시료 정보를 표시한다. `useIqcData`는 `vendorCode`와 `supplierName`을 분리해 preview에는 실제 업체코드 `vendorCode`를 넘긴다. `AqlService.resolvePartPolicy()`는 `IQC_AQL_POLICY_CODE` 미설정 품목을 `BadRequestException`으로 차단해 Major/Minor 불량이 정책 부재로 PASS 되는 상황을 막는다. 검증: IQC 모달 구조 테스트 10건, `aql.service.spec.ts` 17건, `iqc-history.service.spec.ts` 17건, FE/BE tsc, 3002 `/material/iqc` HTTP 200, `git diff --check` PASS. 커밋하지 않았다.
- `T-IQC-AQL-POLICY-CODE`: `/master/part`의 검사수준/Critical/Major/Minor AQL 개별 속성을 제거하고 `ITEM_MASTERS.IQC_AQL_POLICY_CODE` 참조 구조로 전환했다. 신규 `IQC_AQL_POLICIES` 테이블/엔티티와 `GET/POST/PUT/DELETE /quality/aql/policies` API를 추가했고, `/quality/aql` 좌측상단에 `AQL 정책관리` 폼/목록을 배치했다. 좌측 정책관리에는 `?` 도움말을 붙였고 자체 세로 스크롤이 생기지 않도록 3열 폼+남은 영역 정책 그리드로 정리했다. 우측은 `AQL_STANDARDS` 기준 목록과 AQL 기준/LOT 판정기준 관리 영역이며, 상단 버튼은 `AQL 기준 추가`로 표시한다. `AqlFieldHelp` 설명은 `ITEM_MASTERS.IQC_AQL_POLICY_CODE -> IQC_AQL_POLICIES -> AQL_STANDARDS` 구조 기준이다. `/master/iqc-part-spec` 상단 AQL 미리보기는 품목 정책 단독 endpoint가 아니라 신규 `GET /quality/aql/resolve-iqc-items`로 `AqlService.resolveIqcPolicyByItem()` 결과를 사용한다. 화면 요약에는 `검사항목 기준`과 `파괴/고정` 건수를 추가해 실제 IQC 검사항목별 등급/AQL/파괴검사 기준을 같이 보이게 했다. JSHANES 마이그레이션 `2026-06-21_iqc_aql_policy_code.sql` 적용/재실행 PASS, 정책 3건(`AQLP-II-1.0-2.5`, `AQLP-II-1.0-4.0`, `AQLP-II-2.5-4.0`), IQC 대상 19건 정책 코드 보유, orphan 0건, 활성 `IQC_PART_SPEC_ITEMS` 53건, 정책-검사항목 AQL/검사수준 불일치 0건 확인. ERD 재생성. 검증: 관련 구조 테스트 18건, `aql.service.spec.ts` 16건, FE/BE tsc, `git diff --check`, 3002 `/master/part`·`/quality/aql` HTTP 200 PASS. 커밋하지 않았다.
- `T-HARNESS-WIRE-SPEC-SEPARATION`: 전선 길이/스트리핑 조건을 품목마스터에서 제거하고 회로스펙/라우팅 연결로 이동했다. `/master/part`에서 `LENGTH`, `STRIP_BEFORE`, `STRIP_AFTER` UI/API/엔티티 계약 제거, `HARNESS_CIRCUIT_SPECS.WIRE_ITEM_CODE` 추가 및 BOM child 검증, `ROUTING_MATERIALS.CIRCUIT_ID` 추가 및 공정 투입자재-회로사양 선택 연결을 구현했다. JSHANES 마이그레이션 `2026-06-21_harness_wire_spec_separation.sql` 적용 PASS, 확인 쿼리에서 품목 컬럼 제거와 신규 컬럼/FK ENABLED 확인. `docs/reports/db-schema-erd.md` 재생성. 검증: 구조 테스트 17/17, backend focused 35/35, FE/BE tsc, `git diff --check` PASS. 커밋하지 않았다.
- `T-MASTER-PART-MODEL-NAME`: `/master/part` 품목관리에 `차종`을 추가했다. DB는 `ITEM_MASTERS.MODEL_NAME VARCHAR2(100)` nullable 컬럼이며 JSHANES 마이그레이션 적용/재실행/컬럼 주석 확인 완료. 프론트는 목록 컬럼, 검색, 우측 패널, 레거시 모달, help, ko/en/zh/vi locale에 `modelName`을 연결했다. 검증: 구조 테스트 RED→GREEN, FE/BE tsc, `git diff --check`, ERD 재생성, 3002 Playwright DOM 목록 헤더/값/추가 패널 라벨 확인 PASS. 커밋하지 않았다.
- `T-MENU-OPEN-DELAY`: 메뉴 클릭 후 화면이 30초 이상 늦게 열리던 원인을 추가 보정했다. 원인은 `pageRegistry.generated.ts`가 authenticated page 전체 `dynamic()` 목록을 한 파일에 들고 있어 Next/Turbopack on-demand compile/HMR 범위가 커진 것이다. 단순 App Router `children` 캐시는 실제 브라우저에서 `/master/part` 품목 추가 패널이 탭 복귀 시 초기화되어 실패 확인했다. 직전 단일 lazy factory도 runtime 호출은 1개였지만 파일 안에 모든 page switch가 남아 trace에서 계속 크게 잡혔고, 그룹별 registry도 그룹 내 여러 page compile 범위가 남았다. 최종 구현은 메인 registry가 경로별 `page-registries/<route>.generated.ts` 파일 하나만 async import하고, 각 route registry는 page 1개 `dynamic()`만 가진다. `TabKeepAlive`는 async `getPageComponent(path)` 결과를 받아 실제 page component를 최대 `MAX_TABS`개 hidden mount로 유지한다. 검증: layout 구조 테스트 2건 PASS, `pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false` PASS. `.next` 캐시 삭제 후 3002 clean restart 기준 cold compile은 `/dashboard` 14913ms, `/master/part` 10331ms, `/production/wip-stock` 10769ms, `/system/menu-categories` 13657ms, `/shipping/confirm` 15599ms로 아직 dev 서버 첫 컴파일 비용이 남지만, warm 재방문은 `/dashboard` 457ms, `/master/part` 269ms, `/production/wip-stock` 248ms, `/system/menu-categories` 199ms, `/shipping/confirm` 163ms. 직전 동일 `TabKeepAlive` 코드에서 Playwright 로그인 세션으로 품목 추가 폼 입력값 보존 true 확인. 현재 `/dashboard/summary` API 500 전역 모달이 브라우저 자동 메뉴 클릭 재검증을 막았다. 로컬 미추적 `/production/fg-stock` route는 generated commit 대상에서 제외해야 한다. PowerShell에서는 `pnpm` 직접 호출 금지, 반드시 `pnpm.cmd` 사용.
- `T-ROUTING-LABEL-ISSUE-UI`: `/master/routing` 공정 등록/수정 모달에 `라벨발행` 영역을 추가해 `SG 라벨 발행`, `FG 라벨 발행` 체크박스를 설정할 수 있게 했다. 백엔드 DTO/service create/update가 `issueSgLabelYn`, `issueFgLabelYn`을 저장하고, 프론트 공정 그리드는 `라벨발행` 컬럼에 SG/FG 배지를 표시한다. 검증: 구조 테스트 RED→GREEN 6/6, backend focused 2/2 및 전체 spec 28/28, FE/BE tsc, 3012 브라우저 모달/그리드 확인, API 생성/수정/조회 roundtrip, JSHANES 검증 데이터 잔여 0, diff check PASS. 3002는 HTTP 타임아웃이라 3012 dev 서버에서 확인했다. 커밋하지 않았다.
- `T-PROD-WIP-FG-STOCK-MENU-SPLIT`: `/production/wip-stock` 합산 화면을 공통 `WipStockView`로 분리하고, `/production/wip-stock`은 `SEMI_PRODUCT` 고정 `반제품재공조회`, 신규 `/production/fg-stock`은 `FINISHED` 고정 `제품재공조회`로 추가했다. 제품 화면에만 미포장 FG라벨 우측 패널이 보인다. `PROD_FG_STOCK` 메뉴/validator/locale/pageRegistry/seed JSON/DB migration 반영 완료. JSHANES에는 `PROD_WIP_STOCK` sort 70, `PROD_FG_STOCK` sort 71, MANAGER/OPERATOR 권한 Y 확인. 구조 테스트 5건, FE tsc, 3002 브라우저 제목/API itemType 분리/console error 0, diff check PASS. 커밋하지 않았다.

## Coordination Cleanup

- `LOCKS.md`는 현재 활성 잠금 없음으로 정리했다.
- 앞으로 완료된 작업은 `JOURNAL.md`/`ARCHIVE.md`/`HANDOFF`에 기록한 뒤 `LOCKS.md`의 lock 항목을 제거한다. `status: released`를 Active Locks에 누적하지 않는다.

## In Review

- `T-MASTER-PART-IQC-CODE-SELECT`: `/master/part` 품목 등록/수정의 코드성 검사 항목을 선택 방식으로 전환했다. 기본시료수는 코드값이 아니라 수량 기준값이므로 `step=0.001` 숫자 입력으로 소수점 직접 입력을 허용한다. 검사수준은 `AQL_INSP_LEVEL`, Critical/Major/Minor AQL은 `AQL_VALUE`, 모달 검사구분은 `IQC_INSPECT_METHOD` 공통코드를 사용한다. 백엔드 `CreatePartDto.sampleQty`도 `@IsNumber()`로 보정해 소수 저장을 막지 않는다. 개발 표준 `docs/standards/implementation-rules.md`와 `AGENTS.md`, 사용자 기억 메모에 "코드성/기준정보성 값은 자유입력보다 공통코드 또는 기준정보 선택 우선" 원칙을 기록했다. 검증: `/master/part` 구조 테스트 RED→GREEN, BE/FE tsc PASS. 커밋하지 않았다.
- `T-BOX-STOCK-PACKED-VS-RECEIVED`: `/shipping/box-stock`에서 박스포장 상태와 창고입고 상태를 구분했다. `FG_LABELS.BOX_NO`는 포장 식별자로 유지하고, 창고재고 여부는 `PRODUCT_TRANSACTIONS`의 `refType='BOX'`, `transType IN ('WIP_OUT','FG_IN')`, `status='DONE'` 입고 이동 이력으로 판정한다. 백엔드 `findStockByBox()`/`findStockSerials()`는 `inventoryState`, `warehouseCode`, `receivedAt`를 반환하고, 프론트는 재고상태/창고/입고일시 컬럼과 수불 조인 SQL 미리보기를 표시한다. 박스 입고취소 시에도 `FG_LABELS.BOX_NO`는 지우지 않고 포장대기 상태로 돌아가도록 `ProductInventoryService`를 보정했다. 검증: backend RED→GREEN focused, `product-inventory.service.spec.ts box.service.spec.ts` 36/36 PASS, frontend 구조 테스트 RED→GREEN, 기존 box-stock 카드 제거 테스트 PASS, locale JSON parse, BE/FE tsc, JSHANES SQL, diff check PASS. API 직접 호출은 인증 누락 401로 브라우저 세션 실측은 못 했다. 커밋하지 않았다.
- `T-IQC-AQL-Z14-POLICY`: 품목별 AQL + 업체 검사강도 정석 구조 구현 완료. `ITEM_MASTERS` AQL 필드, `PARTNER_MASTERS` 품질등급/검사모드, `IQC_LOGS` AQL 판정근거/불량수, `AQL_SAMPLING_RULES.CODE_LETTER`, `VENDOR_INSPECTION_MODE_HISTORY` 추가. `/material/iqc-history/arrival`은 프론트 요청 결과를 그대로 믿지 않고 서버 AQL 판정 결과로 PASS/FAIL 저장한다. Critical 1건 이상 즉시 FAIL, Major/Minor Ac 초과 FAIL, 업체 검사모드 자동전환 이력 저장 포함. `/master/part`와 IQC 모달 UI 반영. JSHANES 마이그레이션 적용 및 ERD 갱신 완료. 검증: backend focused Jest 23건, BE/FE tsc, IQC modal/master part 구조 테스트, DB post-check, diff check PASS. 커밋하지 않았다.
- `T-MATERIAL-PO-STATUS-RECEIVED-GREEN`: `/material/po-status` PO현황 좌측 그리드에서 상태코드가 `RECEIVED`이거나 `입고율(receiveRate) >= 100`이면 상태 배지를 `입고완료` 초록으로 표시하고, 좌측 입고율 진행바도 100%는 초록으로 표시하도록 보정했다. 다른 PO 상태는 기존 공통코드 색상을 유지한다. 검증: 구조 테스트 RED→GREEN, FE tsc PASS, 3002 Playwright에서 100% 행들이 `입고완료` 초록 배지/초록 진행바로 표시됨 확인, diff check PASS. 커밋하지 않았다.
- `T-TOAST-BOTTOM-LEFT`: 전역 toast 이벤트 메시지 위치를 우상단에서 좌하단으로 이동했다. [providers.tsx](/c:/Project/HANES/apps/frontend/src/app/providers.tsx)의 공통 `Toaster position`을 `bottom-left`로 변경했고, `toaster-position.structure.test.mjs`로 계약을 고정했다. 검증: 구조 테스트 RED→GREEN, FE tsc PASS. 커밋하지 않았다.
- `T-MASTER-PART-LABEL-TERMS`: `/master/part` 품목정보 용어 변경과 입력 도움말 추가 완료. 그리드/우측 패널/레거시 모달에서 `박스장입수량`, `최소불출단위수량(자재)`, `묶음단위수량(생산공정품)`, `품목고정 적재로케이션`으로 표시를 맞췄고 `거래처 / 수량관리` 제목과 미사용 `택타임` 컬럼/입력/type/QA 입력을 제거했다. `PartFieldHelp.tsx`로 입력 라벨 옆 `?` 아이콘을 공통화해 설명과 `ITEM_MASTERS.*` DB 컬럼명을 title/aria-label에 표시한다. `박스장입수량`은 10~100 후보 datalist와 직접 입력을 지원한다. 검증: 구조 테스트 RED→GREEN, FE tsc, 3002 HTTP 200, 3002 Playwright DOM 도움말 27개/DB 컬럼/새 라벨/택타임 미표시/후보값 확인, diff check PASS. 커밋하지 않았다.
- `T-SHIP-ORDER-SQL-PREVIEW`: `/shipping/order` DataGrid SQL 미리보기를 실제 조회 구조에 맞췄다. 기존 `SHIPPING_ORDERS` 오기를 제거하고 `SHIPMENT_ORDERS so` + `SHIPMENT_ORDER_ITEMS soi` + `ITEM_MASTERS im` + `PARTNER_MASTERS pm` LEFT JOIN, tenant 조건, `so.CREATED_AT DESC`를 표시한다. 검증: 신규 구조 테스트 RED→GREEN, 기존 ship-order print/payload 구조 테스트, FE tsc, 3014 `/shipping/order` 200/compile, diff check PASS. 커밋하지 않았다.
- `T-SHIP-CONFIRM-ORDER-PANEL`: `/shipping/confirm` 좌측에 미출하 출하지시 패널을 추가했다. `/shipping/orders?status=CONFIRMED&limit=5000` 응답 중 `orderQty > shippedQty` 잔여가 있는 지시만 표시하고, 행 클릭 시 `BoxScanShipModal`이 출하지시번호를 자동 조회해 박스 바코드 입력부터 진행한다. 상단 `박스 스캔 출하` 버튼은 기존 수동 입력 흐름을 유지한다. 검증: 구조 테스트 RED/GREEN 및 회귀, FE tsc, FE build, 3003 API 확인, 3002 Playwright 확인, diff check PASS. 커밋하지 않았다.
- `T-SHIP-ORDER-PRINT`: `/shipping/order` 등록 출하지시서 출력 기능을 추가했다. 목록 행에 출력 버튼을 넣고, 선택한 출하지시를 `ship-order-print-root` A4 print 영역으로 렌더링한다. 상단에는 출하지시번호 텍스트와 `react-qr-code` 2D QR 바코드를 같이 표시한다. 검증: 구조 테스트 RED→GREEN, locale JSON parse, diff check, `http://localhost:3014/shipping/order` 200 및 compile PASS. 3002는 timeout이라 건드리지 않았다. 전체 FE tsc는 기존 `/shipping/confirm/page.tsx:318` `selectedRowId` 타입 오류로 실패한다. 커밋하지 않았다.
- `T-SHIP-CONFIRM-CARD-REMOVE`: `/shipping/confirm` 상단 4개 정보카드를 제거했다. `StatCard` 렌더링, `stats` 계산, 관련 미사용 import만 제거했고 조회/필터/등록/박스스캔/출하확정 모달 흐름은 유지했다. 검증: 구조 테스트 RED→GREEN, FE tsc, diff check PASS. `localhost:3002`는 node PID `55728` listen 중이나 HTTP 요청이 30초 타임아웃되어 브라우저 확인은 못 했다. 커밋하지 않았다.
- `T-SHIP-CONFIRM-BOXSCAN-ORDERNO`: `/shipping/confirm` 박스스캔출하 모달의 주문 조회 요약 영역 첫 줄에 출하지시번호를 강조 표시했다. API 출하/취소 흐름은 그대로다. 검증: 구조 테스트 RED→GREEN, FE tsc, 3002 HTTP 200, Playwright에서 `SO-RV-26061202215660` 조회 후 번호/고객사/박스입력 표시와 console error 0 확인, diff check PASS. 커밋하지 않았다.
- `T-SHIP-BOX-STOCK-CARD-REMOVE`: `/shipping/box-stock` 좌/우 조회 영역의 `Card/CardContent` 프레임을 제거했다. 박스 목록/개별제품 DataGrid와 API 흐름은 유지. 검증: 구조 테스트 RED→GREEN, FE tsc, 3002 HTTP 200, Playwright title 표시/카드형 wrapper 0/console error 0, diff check PASS. 커밋하지 않았다.
- `T-MENU-LOCALE-MISSING`: 사이드바 메뉴 `labelKey` 누락 번역 보정 완료. `menuConfig.ts` labelKey를 ko/en/zh/vi locale에서 검사하는 구조 테스트를 추가했고, i18next 점 포함 키 구조를 반영했다. RED에서 실제 누락 `en/zh/vi: menu.material.shelfLifeHistory`를 확인한 뒤 각 locale에 번역을 추가했다. 검증: focused node test PASS, locale JSON parse PASS. 커밋하지 않았다.
- `T-IQC-AQL-CRUD`: `/quality/aql` 빈 placeholder를 실제 AQL 기준관리 화면으로 교체했다. JSHANES에 `AQL_STANDARDS`, `AQL_SAMPLING_RULES`를 생성했고, NestJS API(`/quality/aql` CRUD, `/quality/aql/resolve`)와 React 등록/수정/삭제/rule 관리 UI를 추가했다. 검증: backend/frontend RED→GREEN 구조·서비스 테스트, BE/FE tsc, JSHANES 마이그레이션 재실행성, `ORACLE_SITE=JSHANES` ERD 재생성 및 AQL 테이블 반영, API 목록/등록/상세/resolve/수정/삭제, 3002 브라우저 UI 확인 PASS. 검증 데이터 `AQL-CODEX-260619`는 삭제 완료. IQC 품목별 AQL 연결/검사 적용은 후속 범위다. 커밋하지 않았다.
- `T-IQC-HISTORY-ARRIVALNO-COLUMN`: `/material/iqc-history` 그리드에 `입하번호` 컬럼을 추가했다. 백엔드 응답/타입의 `arrivalNo`를 그대로 표시하며 API/DB 변경은 없다. 검증: 구조 테스트 RED→GREEN, FE tsc PASS, Playwright CLI 3002 DOM에서 헤더 `입하번호`와 행 값 `R26061900002` 표시 확인. 커밋하지 않았다.
- `T-IQC-AQL-MENU`: 품질관리 하위에 `AQL 기준관리` 메뉴와 `/quality/aql` 라우트 진입점을 추가했던 선행 작업이다. 이후 `T-IQC-AQL-CRUD`에서 DB/API/CRUD 화면까지 구현 완료했다. 커밋하지 않았다.
- `T-HNS02-260619-SEED-CLEANUP`: JSHANES `40/1000`에서 HNS02 260619 시드성 데이터 정리 완료. `tools/seed/cleanup_hns02_260619_seed.py`를 추가했고 dry-run 후 `--commit`으로 679건 삭제했다. 삭제 범위는 `ARH/RVH/STH/ISH/WOH/PRH/FGH/SGH/BXH/SOH/PTH/POH-260619` 마커와 `created_by='seed'`로 제한했다. 사후 검증: 시드 마커 잔여 0, LOT-입고/LOT-수불 품목 불일치 0, `MAT_STOCKS`/`MAT_ARRIVAL_STOCKS` invariant 0, 미완료 출고요청 0. `R26061900002`는 PASS 10건 1,000,000 수량, 입하재고 AVAILABLE 10건 1,000,000으로 남아 있다. 커밋하지 않았다.
- `T-IQC-HISTORY-LOTNO-FALLBACK`: `/material/iqc-history` LOT No. 컬럼이 입하단위 IQC 이력의 `sampleBarcode`를 fallback 표시하도록 보정했다. 원인은 `IQC_LOGS.MAT_UID`가 입하단위 저장에서 `NULL`인데 그리드가 `matUid`만 표시한 것. 검증: JSHANES/API shape, 구조 테스트, FE tsc, 3002 브라우저 첫 행 `R26061900002 / CBL-B` LOT No. `VH1-RM260619-00011` 표시 PASS. 커밋하지 않았다.
- `T-MASTER-REQUIRED-MARKS`: 기준정보 하위 메뉴 필수컬럼 별표 표시를 보정했다. 공통 `Select`가 `required` 라벨 별표와 native required를 처리하도록 바꿨고, 작업자/거래처/회사/공정/설비/계측기/CAPA/창고위치/공통코드/BOM/IQC/라우팅/제조사바코드/품목유형 등 저장 필수 필드에 `required`를 추가했다. 작업지도서의 수동 `*` 라벨은 중복 방지를 위해 제거했다. 검증: 신규 구조 테스트 RED→GREEN, FE tsc PASS. 커밋은 하지 않았다.
- `T-KIOSK-MOUNTED-RELOAD`: `/production/input-kiosk` 재진입 시 소모품 장착 상태가 선택 설비 기준 DB에서 복원되도록 보정했다. `MaterialListPanel`과 `ConsumableScanModal`이 `/production/job-orders/:orderNo/consumables` 조회/스캔 호출에 `equipCode: selectedEquip?.equipCode`, `includeMounted: 1`을 전달한다. localStorage에 장착 UID를 저장하지 않는다. 검증: 신규 구조 테스트 RED→GREEN, FE tsc PASS, JSHANES `WO2606150066/HNS02C2ABCDE/EQ-ATCNS-01`에서 `CONSUMABLE_STOCKS` mounted UID `CT26061600001`, `CT26061600002` 확인, 3002 API와 headless 브라우저 화면 표시 PASS. `JOB_MATERIAL_LOTS`는 현재 0건이라 자재 샘플 표시는 못 했지만 기존 재조회 경로는 테스트로 고정했다.
- `T-CONSUMABLE-LABEL-REPRINT`: 재발행 전 미리보기와 바코드 렌더 완료 대기까지 보강했다. 각 UID 행에 `미리보기` 버튼이 추가됐고, Modal 안에서 실제 `LabelDesignRenderer`로 같은 라벨을 본다. `LabelDesignRenderer`는 바코드 placeholder에 `data-label-barcode-pending`, 완료 이미지에 `data-label-barcode-ready`를 표시하며, 신규 발행 브라우저 인쇄와 재발행 agent PNG 캡처는 `waitForLabelRenderReady()`를 거쳐 pending이 사라지고 이미지 로드가 끝난 뒤에만 진행한다. Playwright에서 `C26061700029` 미리보기 모달 ready barcode 1/pending 0, UID 표시 true, 재발행 popup 0, agent `/print` 1회(`jobId=CON-REPRINT-C26061700029`, `contentBase64Length=2864`) 확인. 구조 테스트 3개와 FE tsc PASS. Playwright에서는 OS 저장 대화상자를 피하려고 agent `/print`를 mock했고, 실제 agent/PDF queued는 이전 테스트에서 확인했다.
- `T-CONSUMABLE-LABEL-REPRINT`: Microsoft Print to PDF 결과에서 바코드가 검은 블록/잘림처럼 나온 원인은 agent가 아니라 agent에 전달되는 PNG가 이미 깨진 것이었다. `renderLabelNodeToPngBase64()`가 SVG `foreignObject`로 DOM을 직렬화할 때 Tailwind class가 적용되지 않아 `relative/absolute/object-contain/w-full/h-full/box-border`가 빠졌다. `LabelDesignRenderer`의 라벨 루트, 객체, 바코드/이미지 필수 스타일을 inline style로 보강했다. 전송 PNG before는 `docs/reports/label-print-debug-2026-06-17/CON-REPRINT-C26061700029.png`, after는 `...-after.png`이며 after는 QR 전체와 UID 텍스트가 정상이다. 구조 테스트/FE tsc/print-agent 구조 테스트/diff check PASS. 선택 템플릿 `consumable_label` 자체는 `10x10mm`라 물리 출력 크기는 작게 나간다.
- `T-CONSUMABLE-LABEL-REPRINT`: 우측 420px 상세 패널의 기발행 UID 목록을 넓은 테이블에서 리스트형 행으로 보정했다. `재발행` 버튼은 각 UID 행 우측에 항상 보이며 `aria-label="${conUid} 라벨 재발행"`으로 찾을 수 있다. Playwright에서 `C26061700029 라벨 재발행` 버튼이 panelBox 내부에 있음을 확인했고, 클릭 후 팝업 UID/window.print 포함, `/consumables/label/create` 0건, `/material/label-print/log` 201, 상태 문구 확인까지 PASS. 구조 테스트 3개, FE tsc, diff check PASS. `ConLabelDetailPanel.tsx`와 `consumable-label-reprint.structure.test.mjs`는 아직 untracked.
- `T-PRINT-AGENT-GO`: agent 자체 설정관리 보강 완료. `GET /settings` 로컬 HTML 설정 화면을 추가했고, Windows tray 메뉴에 `설정`을 추가해 `ShellExecuteW`로 `http://<listenAddress>/settings`를 연다. `/config`는 `configPath`, `effectiveListenAddress`, `restartRequired`를 반환하며, token은 `clearToken=true` 없이 빈 저장으로 지워지지 않는다. `/test-print` 내장 PNG checksum 실패도 런타임 PNG 생성으로 수정. 현재 새 agent PID `42964`가 `C:\Project\HANES\apps\print-agent\dist\hanes-print-agent.exe`에서 실행 중이며 `/settings` 200, 기본 프린터 `Microsoft Print to PDF` 저장, `/test-print` queued, listenAddress 변경 시 restartRequired true 확인 완료. 트레이 설정 메뉴 마우스 클릭 육안 검증과 Zebra 실출력은 남음.

## Blocked

- 없음.

## Completed

- `T-IQC-AQL-PLAN`: IQC 우선 AQL 기준관리 설계/구현 계획 문서 작성 완료. 산출물은 `docs/superpowers/specs/2026-06-19-iqc-aql-design.md`, `docs/superpowers/plans/2026-06-19-iqc-aql-implementation.md`. 구현 코드는 수정하지 않았고 커밋하지 않았다. 기존 `IQC_PART_SPECS.SAMPLE_QTY`는 1차에서 화면/로직 미사용, 물리 삭제는 2차로 계획했다.
- `T-MATERIAL-ARRIVAL-QTY-FORMAT`: `/material/arrival` 자재입하처리 모달의 읽기 전용 입수량(`serialUnitQty`), 예상 시리얼 계산식, 예상 시리얼수에 천단위 구분자 표시를 적용했다. 입력용 입하수량 `Input type="number"`는 기존 동작 유지. 구조 테스트 RED→GREEN, frontend tsc PASS. 커밋은 하지 않았다.
- `T-WIP-STOCK-ACTUAL-SQL`: `/production/wip-stock` DataGrid SQL 조회문을 실제 백엔드 조회 구조(`PRODUCT_STOCKS s LEFT JOIN ITEM_MASTERS im LEFT JOIN WAREHOUSES wh`)로 교체했다. 기존 `WIP_STOCKS` 하드코딩 제거, 화면 유형/검색 필터 반영, 구조 테스트 RED→GREEN, FE tsc, 3002 브라우저 SQL 모달 검증 완료. 커밋은 하지 않았다.
- `T-ARRIVAL-RESULT-AGENT-REPRINT`: `/material/arrival-result` 라벨 재발행도 `/material/arrival`과 같은 방식으로 맞췄다. 페이지가 `mat_lot` 템플릿 목록을 조회하고 우측 재발행 영역의 `입하 라벨 템플릿` Select 선택값을 `MatLabelPreviewModal`에 전달한다. Playwright로 `R26061800003` 선택, `matlot_label::mat_lot` 유지, 바코드 ready 후 agent `/print` 1회, `MAT-ARRIVAL-VH1-RM260618-00003.pdf` 45,103 bytes 생성 확인. 구조 테스트, FE tsc, print-agent 구조 테스트, Go test PASS.
- `T-PRINT-AGENT-PDF-OUTPUT`: `Microsoft Print to PDF` 출력이 `Access is denied`로 실패하던 원인을 보정했다. agent가 PDF 프린터를 감지하면 `logs/prints/<jobId>.pdf`를 `DOCINFO.lpszOutput`으로 넘긴다. 새 exe 빌드 후 agent 재시작 완료. `/test-print`는 `HANES-TEST-PRINT.pdf` 생성, `/material/arrival`에서는 `matlot_label::mat_lot` 선택 후 `PO-26-T01/TMN-C` 1개 입하 발행 및 `MAT-ARRIVAL-VH1-RM260618-00003.pdf` 생성 확인. 테스트 과정에서 `PO-26-T01 / TMN-C` 누적 입하수량은 3이 됐다.
- `T-MATERIAL-ARRIVAL-AGENT-LABEL`: `/material/arrival` 입하 라벨 모달을 소모품 라벨과 같은 출력 방식으로 전환했다. 모달 오픈 시 `/master/label-templates?category=mat_lot`를 조회해 기본/저장 템플릿을 선택하고, 미리보기와 출력 모두 `LabelDesignRenderer`를 사용한다. 기존 숨김 iframe/browser `window.print()`는 제거했다. 출력 버튼은 바코드 렌더 완료와 이미지 로드를 기다린 뒤 라벨별 PNG를 만들고 로컬 print-agent `/print`로 `jobId=MAT-ARRIVAL-${matUid}` 전송한다. 검증: 구조 테스트 RED→GREEN, frontend tsc PASS, 3002 `/material/arrival` headless 브라우저 접속 console/page error 0.
- `T-CONSUMABLE-STOCK-DEPLOY-QUERY`: `/consumables/stock` 배포 조회 빈 화면은 `GET /consumables/stocks` 응답이 `{ success, data: { data: rows } }`로 이중 감싸질 수 있는데 `useStockData`가 첫 번째 `data`까지만 읽어 배열이 아니면 빈 배열 처리한 것이 원인이다. `limit=5000`을 포함해 조회하고, 중첩 `data`까지 풀어 배열만 반영하도록 보정했다. 검증: focused 구조 테스트, frontend tsc PASS. commit/push 예정.
- `T-CONSUMABLE-LABEL-CLICK-OPEN-PRINT`: `/consumables/label` UID 발행 출력창을 버튼 클릭 즉시 `window.open("", "_blank")`으로 먼저 열고 대기 HTML을 표시한 뒤, UID 발행 API 완료 후 같은 창에 `LabelPrintRenderer` HTML과 `window.print()` 스크립트를 주입하도록 보정했다. 실패/0건/출력 준비 실패 시 선점한 창을 닫고 기존 toast/한 줄 상태 피드백을 유지한다. 구조 테스트는 숨김 iframe 금지와 API 전 출력창 선점을 검증하도록 갱신했다. 검증: RED 확인 후 구조 테스트 GREEN, 템플릿/print-log 구조 테스트, frontend tsc, 3002 Playwright mock popup 검증, diff check PASS. commit/push 안 함.
- `T-CONSUMABLE-LABEL-TEMPLATE-SELECT-PRINT`: `/consumables/label` UID 발행 화면에 라벨디자인마스터 저장 템플릿 선택 Select를 추가했다. `/master/label-templates?category=jig` 목록을 `templateKey/designData`와 함께 보관하고, 사용자가 선택한 템플릿의 `designData`를 `ensureObjectLabelDesign(..., "jig")`로 정규화해 `LabelPrintRenderer`에 전달한다. 검증용 템플릿 `CODEX_CON_TPL_60546393`을 3013 브라우저에서 선택해 UID 발행했고, 인쇄 HTML에 marker `CODEX_TEMPLATE_MARK_60546393`, 생성 UID `C26061700014`, `@page{size:50mm 25mm`가 포함됨을 확인했다. 검증 템플릿/UID/로그는 JSHANES에서 잔여 0건으로 정리했다. 검증: 신규 구조 테스트, 기존 소모품 이미지 URL 테스트, `useConLabelIssue` 구조 테스트, frontend tsc, diff check PASS.
- `T-EQUIP-INSPECT-ITEM-DEPLOY-IMAGE-URL`: 서버 배포 후 `/master/equip-inspect-item` 이미지 깨짐 원인을 `/uploads/equip-inspect-items/...` 상대 URL 직접 사용 + `apps/backend/uploads` gitignore로 인한 점검항목 시드 SVG 미배포로 확인하고 보정 완료. `/master/equip-inspect-item` 목록/편집 패널과 공용 `InspectItemImage`가 `resolveBackendFileUrl()`로 `/uploads/...`를 `NEXT_PUBLIC_API_URL` backend base 기준으로 변환한다. `.github/workflows/deploy.yml`은 배포 때 `node tools\generate-equip-inspect-item-seed-images.mjs`를 실행해 `apps/backend/uploads/equip-inspect-items/*.svg` 50개를 서버에서 재생성한다. 검증: 생성 스크립트, 신규 구조 테스트, 기존 패널 구조 테스트, frontend tsc, diff check PASS.
- `T-CONSUMABLE-LABEL-DEPLOY-IMAGE-URL`: 서버 배포 후 `/consumables/label` 이미지 깨짐 원인을 `/uploads/consumables/...` 상대 URL 그대로 사용 + `apps/backend/uploads` gitignore로 인한 시드 SVG 미배포로 확인하고 보정 완료. `apps/frontend/src/utils/file-url.ts`의 `resolveBackendFileUrl()`로 `/uploads/...`를 `NEXT_PUBLIC_API_URL` backend base 기준으로 변환하고, 라벨 발행 그리드와 `LabelDesignRenderer` 이미지 객체에 적용했다. `.github/workflows/deploy.yml`은 배포 때 `node tools\generate-consumable-master-seed-images.mjs`를 실행해 `apps/backend/uploads/consumables/*.svg` 37개를 서버에서 재생성한다. 검증: 생성 스크립트, 신규 구조 테스트, frontend tsc, diff check PASS.
- `T-MATERIAL-FLOW-FE-RUNTIME`: 자재관리 등록 메뉴 전체와 자재요청 -> 자재출고 -> 재고 -> 공정입고 흐름 검증 완료. `http://localhost:3012` 프론트에서 `/menu-categories/tree` 기준 MATERIAL 24개 메뉴를 순회해 24/24 PASS, 보고서 `docs/reports/hanes-material-menu-scenario-qa-2026-06-17/index.html`. 실흐름은 요청 `MR2606170035`, 작업지시 `JO-MATFE-26061618422034`로 `/material/request` 요청 생성, `/material/issue` 승인/LOT 선택/출고, `/inventory/material-stock` 자재재고, `/production/wip-material-stock` 공정재고, `/production/input-kiosk` 작업지시 로딩까지 PASS. JSHANES `MAT_ISSUE_REQUESTS`, `MAT_ISSUE_REQUEST_ITEMS`, `MAT_ISSUES`, `WIP_MAT_TRANSACTIONS`, `WIP_MAT_STOCKS` 체크 OK. 보고서 `docs/reports/hanes-material-flow-frontend-runtime-qa-2026-06-17/index.html`. 보정: QA 스크립트 proxy/API 경로 매칭, GET 503 재시도, 재고 실제 경로/공정재고 테이블 기준, 통합 MATERIAL 메뉴 24개 매핑, `/material/hold` SQL 안내 `MAT_LOTS`/`MAT_STOCKS` 기준 수정.
- `T-MASTER-LABEL-BARTENDER-DESIGNER`: `/master/label`을 객체 기반 라벨 디자이너로 전환 완료. 상단 탭은 제거했고 좌측 `소스테이블` 선택으로 설비/소모품/작업자/자재 LOT 소스를 고른다. 객체 타입은 글자/1D/2D/박스/선/원/이미지이며, 선택 객체 드래그 이동과 모서리 앵커 리사이즈가 동작한다. `LabelDesignRenderer`/`LabelPrintRenderer`는 저장된 `elements[]`와 `sourceField`를 실제 데이터로 치환한다. `/consumables/label`은 `jig` 템플릿을 조회해 소모품 UID 출력에 사용한다. 실제 3002에서 임시 템플릿 `CODEX_PRINT_1781631331283` 저장 후 `UID 발행`으로 `C26061700011`을 생성했고, 인쇄 HTML에 `conUid`/소모품코드/소모품명/정적문구/바코드 이미지 2개가 들어가는 것을 확인했다. 검증용 템플릿/UID/로그는 모두 삭제 후 잔여 0건 확인. 검증: 구조 테스트 3개, FE tsc, 3002 브라우저 도구/앵커/템플릿 요청, UI 저장/API 조회/삭제, 실제 UID 발행 인쇄 HTML 치환 통과.
- `T-MASTER-LABEL-CUSTOM-SOURCE-FIELDS`: `/master/label` 좌측 필드 목록을 고정값에서 사용자 정의 저장값으로 전환 완료. `LabelDesign.sourceFields`에 key/표시명/샘플값을 저장하고, 좌측 패널에서 추가/수정/삭제할 수 있다. 텍스트/바코드/이미지 객체의 `소스 필드` 선택지는 이 목록을 기준으로 표시된다. 검증: 구조 테스트 2개, FE tsc, 3002에서 `customTraceNo` 필드 추가/객체 매핑/저장 요청 201, `CODEX_FIELD%` 임시 템플릿 삭제 후 0건 확인.
- `T-MASTER-LABEL-DESIGN-ONLY`: `/master/label` 라벨다자인관리 화면을 모든 카테고리 디자인 제공 전용으로 전환 완료. 탭은 `설비/소모품/작업자/자재롯트라벨`만 남기고 `품목`을 제거했다. 대상 조회 API/대상 선택 그리드/선택 출력 패널을 제거했으며, 디자인/템플릿 저장과 샘플 미리보기만 제공한다. 검증: 구조 테스트 RED→GREEN, frontend tsc, 3002 Playwright에서 탭/안내문/대상 API 호출 0건 확인.
- `T-EQUIPMENT-PERIODIC-DAILY-FLOW`: `/equipment/periodic-inspect`를 `/equipment/daily-inspect`와 같은 대상 설비 목록 + 항목별 입력 흐름으로 통일 완료. 백엔드는 기존 `/equipment/periodic-inspect` `PERIODIC` 엔드포인트를 그대로 쓰고, 프론트는 `PERIODIC` 항목 배정 설비 목록과 선택 설비 입력 패널로 처리한다. 미점검 설비 선택 시 404 상세조회 모달이 뜨지 않도록 기존이력 여부가 있을 때만 단건 조회하고, 항목 상태 key는 `itemCode` 우선으로 보정했다. 검증: 구조 테스트, frontend tsc, diff check, 3002 브라우저에서 정기점검 선택 후 항목 테이블 표시 및 콘솔 오류 0. 증적: `docs/reports/equipment-periodic-inspect-selected-2026-06-17.png`.
- `T-EQUIPMENT-INSPECT-CARDS-REMOVE`: `/equipment/inspect-history`, `/equipment/periodic-inspect` 상단 정보카드 제거 완료. `inspect-history`는 그리드/필터/내보내기 유지, `periodic-inspect`는 이후 일일점검형 처리 화면으로 전환됐다. 검증: frontend tsc, diff check, 3002 브라우저 카드 미표시 확인. 증적: `docs/reports/equipment-inspect-history-no-cards-2026-06-17.png`, `docs/reports/equipment-periodic-inspect-no-cards-2026-06-17.png`.
- `T-EQUIP-INSPECT-HISTORY-BLANK-ROWS`: `/equipment/inspect-history` 빈 행 문제 수정 완료. 원인은 `EquipInspectService.findAll()`이 `getRawMany()` 결과를 `log.log`로 잘못 펼쳐 API가 `{ equip: {} }` 15행을 반환한 것. raw alias 명시 매핑과 Oracle 대문자 alias fallback을 추가했고, 화면 점검일은 `YYYY-MM-DD`로 표시한다. 검증: backend focused test 14/14, backend/frontend tsc, API shape 확인, 3002 브라우저에서 `EQ-ATCUT-01/자동절단 설비 #1/오지훈/합격` 표시 및 ISO 원문 미표시 확인. 증적: `docs/reports/equipment-inspect-history-grid-2026-06-16-after.png`.
- `T-KIOSK-WI-SEED-HNS02C1ABCD`: `/production/input-kiosk` 작업지도서 미표시 원인 확인 및 시드 보완 완료. `WO2606150060`은 `HNS02C1ABCD/ATCUT/EQ-ATCUT-01`이고 키오스크 API 조건도 동일했으나 JSHANES `WORK_INSTRUCTIONS` 0건이었다. `apps/backend/src/migrations/2026-06-16_work_instruction_hns02c1abcd_seed.sql` MERGE 시드를 적용해 Rev.A 작업지도서 1건을 생성했다. 검증: DB 1건, API total 1, 임시 headless Chrome에서 3002 `/production/input-kiosk` DOM에 제목/본문 표시 true 및 `작업지도서 없음` false, 스크린샷 `docs/reports/kiosk-work-instruction-hns02c1abcd-2026-06-16.png`.
- `T-CONSUMABLE-LIFE-STATUS-SHAPE`: `/consumables/life` 런타임 `data.filter is not a function` 수정 완료. 원인은 백엔드 `getLifeStatus()`가 카운트 객체를 반환하지만 화면은 수명현황 행 배열을 기대한 계약 불일치였다. `getLifeStatus()`를 행 배열 반환으로 바꾸고 테스트를 보강했다. 검증: focused backend test 20/20, backend/frontend tsc, 3003 API `data` 배열 37건, Playwright 3002 `/consumables/life` 런타임 에러 없음 및 37건 렌더링 확인.
- `T-CONSUMABLE-LABEL-RESPONSE-FIX`: `/consumables/label` 라벨 발행 대상 미조회 수정 완료. 원인은 라벨 컨트롤러 `return { data }`가 전역 응답 인터셉터에 의해 `success/data/data`로 이중 래핑되어 프론트가 배열을 못 읽은 것. `ResponseUtil.success(data)`로 통일했고 구조 테스트, backend build, API 37건, 브라우저 37행 표시를 확인했다.
- `T-FRONTEND-DELETE-CONFIRM-GUARD`: 프론트 삭제 버튼 직접 실행 누락을 공용 `ConfirmModal` 기준으로 보강 완료. 라우팅/라인/공정 탭, 소모품 사용매핑, 소모품/사용자/품목/점검항목 이미지 제거, 교육 참석자, 팔레트 박스, 포장 시리얼, 공정-설비 매핑, 자주검사 항목, 라벨/IQC 템플릿, 품질 audit/control-plan 하위 항목 삭제가 확인 모달 후 실행된다. 검증: `node --test apps/frontend/src/delete-confirm-guard.structure.test.mjs`, `pnpm --filter @harness/frontend exec tsc --noEmit --pretty false`, 대상 파일 `git diff --check`, 직접 삭제 호출 `rg` 검색 통과.
- `T-CONSUMABLE-MASTER-USAGE-MAP-FIXED`: `/consumables/master`의 `CONSUMABLE_USAGE_MAP` UI를 등록/수정 패널 내부가 아닌 항상 보이는 우측 고정 섹션으로 분리 완료. 목록에서 소모품을 선택하면 우측 섹션이 해당 소모품 기준으로 매핑을 조회하고 제품/모델 + 설비 + 단위사용량 + 사용여부 + 비고를 저장/토글/삭제한다. 검증: frontend/backend tsc, 3002 HTTP 200. 기존 usage-maps API 조회/생성/삭제 및 JSHANES 테스트 잔여 0건 검증은 `T-CONSUMABLE-MASTER-USAGE-MAP`에서 완료.
- `T-CONSUMABLE-MASTER-USAGE-MAP`: `/consumables/master` 우측 패널 안에 `소모품 사용매핑` 섹션 추가 완료. 별도 페이지/메뉴 없이 선택 소모품 기준으로 제품/모델 + 설비 + 단위사용량 + 사용여부 + 비고를 저장/토글/삭제한다. 백엔드는 `/consumables/:id/usage-maps` GET/POST 및 `/consumables/:id/usage-maps/:productItemCode/:equipCode` PUT/DELETE 추가. 검증: backend/frontend tsc, API 조회/생성/삭제, JSHANES 테스트 잔여 0, 3002 HTTP 200. Playwright 패키지/설정 부재로 DOM 자동 검증은 미수행.
- `T-CONSUMABLE-MASTER-IMAGE-SEED`: `/consumables/master` 소모품 37건 전체에 SVG 시드 이미지 등록 완료. `tools/generate-consumable-master-seed-images.mjs`가 `apps/backend/uploads/consumables/*.svg` 37개와 `apps/backend/src/migrations/2026-06-16_consumable_master_image_seed.sql`을 생성한다. JSHANES 적용 결과 `CONSUMABLE_MASTERS` total 37, with_image 37, missing 0. API `/api/v1/consumables?limit=100&useYn=Y`도 missing 0, static `/uploads/consumables/cm_jg_ct1.svg` 200 `image/svg+xml`, 3002 `/consumables/master` 브라우저에서 이미지 37개 모두 로드 확인.
- `T-ITEM-CONSUMABLE-MOVE`: JSHANES `ITEM_MASTERS`의 소모품 12건(`APPCT*`, `CUTBL*`, `JIGHD*`)을 `CONSUMABLE_MASTERS`로 이동 완료. `CONSUMABLE_CODE`는 기존 품목코드 유지, 분류는 `JIGHD*=JIG`, 나머지 `TOOL`로 매핑했다. 적용 파일은 `apps/backend/src/migrations/2026-06-16_move_item_consumables_to_consumable_master.sql`. 검증: 마이그레이션 재실행 성공, `ITEM_MASTERS` 소모품 0건, `CONSUMABLE_MASTERS` 이동 12건, 백업 12건, 이동 분류 `JIG=4`/`TOOL=8`.
- `T-CONSUMABLE-LABEL-CARDS-REMOVE`: `/consumables/label` 상단 정보카드 제거 완료. `page.tsx`에서 `StatCard` grid, 카드 전용 `stats` useMemo/import만 제거했고 마스터 조회/검색/선택/수량 입력/UID 발행/브라우저 인쇄/결과 배너/DataGrid 기능은 유지했다. 검증: 카드 관련 잔여 참조 0건, `git diff --check`, frontend tsc, 3002 HTTP 200.
- `T-EQUIP-INSPECT-ITEM-UNIT-DROPDOWN`: `/master/equip-inspect-item` 등록/수정 패널의 측정 단위 입력을 `UNIT_TYPE` 공통코드 드롭다운으로 전환 완료. JSHANES 기준 기존 단위 그룹은 `UNIT_TYPE`이며, 현 데이터에서 쓰는 `°C`, `Ω`를 공통코드에 추가하고 `mm` 4건은 `MM`으로 정규화했다. 검증: 구조 테스트 4/4, frontend tsc, JSHANES/API 공통코드 확인, 3002 브라우저에서 `단위`가 `SELECT`이고 `MM/°C/Ω` 옵션 노출 확인.
- `T-CONSUMABLE-MASTER-CARDS-REMOVE`: `/consumables/master` 상단 정보카드 제거 완료. `page.tsx`에서 `StatCard` grid, `computedStats`, 카드 전용 import만 제거했고 목록 조회/검색/분류 필터/DataGrid/등록·수정 패널/삭제 흐름은 유지했다. 검증: 카드 관련 잔여 참조 0건, `git diff --check`, frontend tsc, 3002 HTTP 200. Playwright DOM 확인은 3002 페이지 로드 타임아웃으로 미수행.
- `T-EQUIP-INSPECT-ITEM-IMAGE-SEED`: 설비점검 위치 안내용 SVG 50개를 `apps/backend/uploads/equip-inspect-items`에 생성하고, `2026-06-16_equip_inspect_item_image_seed.sql`을 JSHANES에 적용해 `EQUIP_INSPECT_ITEM_MASTERS.IMAGE_URL` 50/50을 채웠다. `uploads`는 git ignore 대상이라 파일 자체는 로컬 서버에 생성되어 있고, 재생성은 `node tools/generate-equip-inspect-item-seed-images.mjs`로 가능하다. 검증: DB `WITH_IMAGE=50`, 로컬 SVG 50개, 3003/3002 static 200 `image/svg+xml`, API `imageUrl` 반환, 실제 브라우저 `/master/equip-inspect-item` 이미지 50개 렌더링 및 첫 이미지 720x420 확인.
- `T-EQUIP-INSPECT-ITEM-IMAGE-PANEL`: `/master/equip-inspect-item` 사진 첨부와 우측 패널 전환 완료. 등록/수정 UI는 모달 대신 480px 우측 패널로 열리고, 사진 미리보기/선택/업로드/삭제가 가능하다. 백엔드는 `EQUIP_INSPECT_ITEM_MASTERS.IMAGE_URL`과 `POST/DELETE /master/equip-inspect-item-masters/:itemCode/image`를 추가했고 JSHANES에 이미지 컬럼 마이그레이션을 적용했다. 누락 데이터는 `EQUIP_TYPE COMMON=공통` 공통코드 추가, 기존 공통/작업자 항목 10건 유형 보정, 누락 주기 7건 보정, 측정형 단위/기준값 보강으로 처리했다. 검증: JSHANES 누락 0건, API total 50/missing 0/COMMON 10/imageUrl field true, 브라우저 3002에서 `공통`/판정기준/주기 노출 및 우측 패널 DOM 확인, 이미지 업로드/삭제 API 실측, backend focused test 8/8, frontend/shared tsc, backend build, 구조 테스트 3/3, ERD 재생성 통과.
- `T-KIOSK-EQUIP-INSPECT-MIGRATION-RERUN`: `apps/backend/src/migrations/2026-06-16_equip_inspect_workday_order.sql`을 `oracle_connector.py --execute-file`로 재실행 가능한 idempotent PL/SQL 블록으로 보정 완료. 원인은 커넥터가 `/` 라인으로 statement를 분리하고, 주석으로 시작하는 PL/SQL 블록은 `DECLARE/BEGIN`으로 인식하지 못해 `END;` 세미콜론을 제거한 것. 파일 첫 줄이 `DECLARE`인 단일 블록으로 바꾸고 JSHANES에서 `--execute-file` 재실행 성공, 컬럼 5/인덱스 4/백필 누락 0 확인.
- `T-KIOSK-EQUIP-INSPECT-WORKDAY-ORDER`: 설비일일점검/작업자설비점검 이력 기준 전환 완료. 기존 생산월력/교대패턴을 조업일 판정 기준으로 사용하고, `EQUIP_INSPECT_LOGS`에 `ORDER_NO`, `WORK_DATE`, `INSPECT_AT`, `OP_WINDOW_START_AT`, `OP_WINDOW_END_AT` 추가. `DAILY`는 `WORK_DATE`, `WORKER`는 `ORDER_NO`로 완료 여부를 조회한다. 키오스크는 일일점검 날짜를 프론트에서 보내지 않고, 작업지시 선택/변경 시 `WORKER + orderNo`로 이력을 확인한다. JSHANES 컬럼 5개/인덱스 4개 적용 및 백필 누락 0건 확인. 검증: backend focused test 16/16, frontend structure test 9/9, backend/frontend tsc, backend build, DB post-check, ERD 재생성 통과.
- `T-KIOSK-JOBORDER-PERSIST-REFRESH`: `/production/input-kiosk` 브라우저 새로고침 후 선택 작업지시가 사라지는 문제 수정 완료. 원인은 `kioskStore.ts`의 Zustand persist `partialize`가 `selectedJobOrder`를 제외한 것. `harness-kiosk` persist 대상에 `selectedJobOrder`를 포함했다. 검증: `node --test apps/frontend/src/stores/kiosk-store-persist.structure.test.mjs`, `pnpm --filter @harness/frontend exec tsc --noEmit`, `git diff --check` 통과.
- `T-BOM-ITEM-TYPE-LABEL-FIX`: `/master/bom` 품목유형 표시를 품목마스터와 같은 한글 라벨로 통일 완료. 실측 결과 BOM API/DB는 별도 컬럼이 아니라 `ITEM_MASTERS.ITEM_TYPE`을 사용하고, `PRODUCT_TYPE`은 별도 품목그룹/제품유형 컬럼이다. 문제는 BOM 프론트의 일부 렌더링 지점이 원 코드 `FINISHED/SEMI_PRODUCT/RAW_MATERIAL/CONSUMABLE`을 그대로 출력한 것이었다. 부모 목록은 `useComCodeOptions("ITEM_TYPE")` 라벨 맵을 사용하고, BOM 트리/범례/자품목 선택 힌트는 `comCode.ITEM_TYPE` 번역 라벨을 사용하도록 수정. 검증: 구조 테스트 3/3 PASS, frontend tsc 통과, API/Oracle 확인, 3012 임시 프론트 브라우저에서 원 코드 미노출 및 `완제품/반제품/원자재/소모품` 노출 확인. 3002는 이 작업 중 HTTP 타임아웃 상태라 사용자 프로세스는 건드리지 않았다.
- `T-KIOSK-DAILY-INSPECT-EMPTY-GUIDE`: `/production/input-kiosk` 설비일일점검 모달의 배정 누락 안내 보강 완료. 설비별 DAILY 배정 항목이 없으면 항목이 안 뜨는 정책을 유지하고, 기존 `항목 없음 - 자동완료` 경로와 `confirmWithoutItems` 버튼을 제거했다. 0건일 때 `설비점검항목마스터(/master/equip-inspect-item)`에서 DAILY 항목 등록 후 `설비점검항목(/master/equip-inspect)`에서 설비 선택, DAILY 선택, 점검항목 추가/저장, 모달 재오픈 절차를 안내한다. 배정 API 응답 `sortSeq -> seq` 정규화도 추가. 검증: daily inspect 구조 테스트 3/3, frontend tsc 통과.
- `T-KIOSK-WORKER-INSPECT-EMPTY-FIX`: `/production/input-kiosk` 작업자설비점검 모달 빈 화면 보정 완료. 원인은 모달이 설비별 배정 API(`/master/equip-inspect-items`)만 조회하는데 JSHANES의 절단/압착 등 대부분 설비는 WORKER 배정 0건인 것. 설비별 배정이 없으면 항목이 안 뜨는 업무 규칙은 유지했고, 0건일 때 `기준정보 > 설비점검항목(/master/equip-inspect)`에서 설비 선택 → 작업자점검(WORKER) 선택 → 점검항목 추가 → 저장 → 모달 재오픈 절차를 상세 안내하도록 변경했다. 배정 API 응답 `sortSeq -> seq` 정규화도 추가. 검증: `EQ-CUT-01` 배정 API 0건, WORKER 마스터 API 4건, `EQ-OINSP-01` 배정 API 4건 확인, 구조 테스트와 frontend tsc 통과. `browse`/`playwright`가 없어 실제 브라우저 캡처는 미수행.
- `T-MAT-FLOW-COHERENCE-FIX`: 입하/입하재고/입고/입고후재고/출고/출고후재고/공정입고/공정재고 흐름을 코드와 JSHANES DB로 점검 후 보정 완료. 레거시 `/material/arrivals/po`, `/material/arrivals/manual` 생성 경로가 `MAT_IN`/`MAT_STOCKS`로 직행하지 않고 `MAT_LOTS` 발행 후 `MAT_ARRIVAL_STOCKS` + `MAT_ARRIVAL_TRANSACTIONS(ARRIVAL_IN)`를 기록하도록 수정했다. 입하 모달 payload는 `warehouseId`로 정합화. JSHANES에는 `2026-06-16_repair_mat_flow_audit_gaps.sql`을 적용해 현재고를 건드리지 않고 누락 감사원장만 보강(`ARRIVAL_REPAIR` 23건/23, `RECEIVE_REPAIR` 1건/3). 최종 입하원장-입고원장-입하재고 대사 `BAD_ROWS=0`, 음수 재고 0, `MAT_IN/MAT_IN_CANCEL` 0. 검증: ArrivalService spec 34/34 PASS, backend build, frontend tsc, 3003 health 200, 3002 `/material/arrival` 200.
- `T-MAT-ARRIVAL-TRANSACTION-MENU`: `MAT_ARRIVAL_TRANSACTION` 좌측 메뉴 등록 완료. 프론트 `MATERIAL` 카테고리에서 `입하실적조회` 바로 뒤에 path `/material/arrival-transaction`으로 추가했고, ko/en/zh/vi label과 백엔드 `menu-code-validator.ts`, seed SQL을 동기화했다. JSHANES `MENU_CATEGORY_ITEMS`에는 company `40`, plant `1000`, category `MATERIAL`, sort `45`로 등록됐고 `ROLE_MENU_PERMISSIONS`는 `MANAGER` 접근권한 `Y`를 부여했다. 검증: JSON 파싱, backend build, frontend build, `/api/v1/menu-categories/tree` 인증 호출에서 `MAT_ARRIVAL_TRANSACTION` 반환, 3002 URL 200. 검증 중 3002/3003 dev 서버를 각각 재시작했다.
- `T-MAT-ARRIVAL-TRANSACTION-PAGE`: `/material/arrival-transaction` 입하수불조회 화면 추가 완료. `MAT_ARRIVAL_TRANSACTIONS` 조회 전용 DataGrid이며 기간/유형/상태/MAT UID/통합검색 필터를 제공한다. `ArrivalQueryDto`와 `ArrivalService.findAll()`에 `transType`, `matUid`, `arrivalNo` 검색 조건을 보강했다. 검증은 frontend registry 생성, backend build, frontend build, 3002 route HTTP 200. 인증 없는 backend API 직접 호출은 401. 좌측 메뉴 등록은 사용자 지시에 따라 이번 범위에서 제외했고, 메뉴 통합 완료 후 `MATERIAL` 카테고리에 연결해야 한다.
- `T-MAT-ARRIVAL-STOCK-SPLIT`: 입하재고/입고재고 테이블 분리 A안 적용 완료. `MAT_ARRIVAL_STOCKS`, `MAT_ARRIVAL_TRANSACTIONS` 신설 및 엔티티 추가, 기존 `STOCK_TRANSACTIONS.MAT_IN/MAT_IN_CANCEL` 57건을 백업 후 입하원장으로 이관하고 `STOCK_TRANSACTIONS`에서는 삭제했다. JSHANES 검증 결과 입하원장 57건/322,303, 입하재고 35건/6,020, `MAT_IN/MAT_IN_CANCEL` 잔존 0건, `RECEIVE` 44건/316,303. IQC005 입하는 입하재고/입하원장 기록으로, 정상 입고 확정은 입하재고 감소 후 창고재고 증가로 변경했다. `/inventory/transaction` 필터에서 `MAT_IN/MAT_IN_CANCEL` 제거. 보고서 `docs/reports/hanes-mat-arrival-stock-split-migration-2026-06-16.md`, 마이그레이션 `apps/backend/src/migrations/2026-06-16_mat_arrival_stock_split.sql`, ERD `docs/reports/db-schema-erd.md`. 검증: backend build, backend 핵심 테스트 50건, frontend build 통과. 인증 없는 API 직접 호출은 401.
- `T-MULTI-CATEGORY-MENU-PAGE-SCENARIO-QA`: 좌측 등록 메뉴 중 자재재고관리, 생산관리, 품질관리, 검사관리, 제품수불관리, 설비관리, 출하관리 하위 메뉴 50개 상세 시나리오 QA 완료. 기준은 `/api/v1/menu-categories/tree`의 활성 등록 메뉴이며, 결과 `docs/reports/hanes-registered-categories-scenario-qa-2026-06-16/registered-categories-result.json` 기준 50/50 PASS, 300단계, 300개 스크린샷, API 675건, DB count 150건이다. 목차는 `docs/reports/hanes-registered-categories-scenario-qa-2026-06-16/index.html`, 실행 스크립트는 `tools/hanes-registered-categories-page-scenario-qa.mjs`. 수정 사항은 `/production/result`의 `search` 조회 400 해소(`ProdResultQueryDto.search`, `ProdResultService.findAll()` 통합 검색)와 `/shipping/pack`의 `/shipping/boxes?search=...` 400 해소(`BoxQueryDto.search`, `BoxService.findAll()` 박스번호/품목코드 검색)이다. 백엔드 tsc와 결과 HTML/JSON/스크린샷 무결성 검증 통과.
- `T-MATERIAL-MENU-PAGE-SCENARIO-QA`: 좌측 `자재수불관리` 카테고리에 실제 등록된 하위 메뉴만 상세 시나리오 QA 완료. 실제 등록 기준은 `/api/v1/menu-categories/tree`의 `MATERIAL` 카테고리이며 16개 코드(`PUR_PO`, `PUR_PO_STATUS`, `MAT_ARRIVAL`, `MAT_ARRIVAL_RESULT`, `QC_CONCESSION`, `MAT_RECEIVE`, `MAT_RECEIVE_HISTORY`, `MAT_REQUEST`, `MAT_ISSUE`, `MAT_ISSUE_OTHER`, `MAT_LOT_SPLIT`, `MAT_LOT_MERGE`, `MAT_SCRAP`, `MAT_ADJUSTMENT`, `MAT_MISC_RECEIPT`, `MAT_RECEIPT_CANCEL`)만 포함했다. 최종 결과 `docs/reports/hanes-material-menu-scenario-qa-2026-06-15/material-menu-result.json` 기준 16/16 PASS, 96단계, 96개 스크린샷, HTML/API/DB 섹션 링크 검증 완료. 목차는 `docs/reports/hanes-material-menu-scenario-qa-2026-06-15/index.html`, 실행 스크립트는 `tools/hanes-material-menu-page-scenario-qa.mjs`. 운영 재고 변경 저장/삭제는 임의 실행하지 않고 페이지별 저장형 상세 시나리오 대상으로 보고서에 분리 표기했다.
- `T-MASTER-REPORT-SEARCH-DUPLICATE-FIX`: 기준정보 잔여 19개 페이지 QA 보고서 검색어와 중복방어 시나리오 보정 완료. `tools/hanes-master-remaining-page-scenario-qa.mjs`는 더 이상 `HNS02`를 고정 검색어로 쓰지 않고 화면 데이터 기반 검색어 또는 빈 조회를 사용한다. 모든 잔여 페이지에 `duplicate-defense` 단계가 들어갔고, 최신 결과 `docs/reports/hanes-page-scenario-qa-2026-06-15/master-remaining-result.json` 기준 19/19 PASS, 중복방어 단계 누락 0, non-PASS 0, 증적 누락 0. 최신 CRUD 증적은 `docs/reports/hanes-master-crud-runtime-test-26061513195369.json`이며 134/134 PASS, `DUPLICATE_GUARD` 30/30 PASS, cleanup 31/31 성공. 중복방어 보강 파일은 `process.service.ts`, `iqc-item.service.ts`, `label-template.service.ts`, `work-instruction.service.ts`, `equip-bom.service.ts` 및 각 spec. 백엔드 tsc와 관련 단위 테스트 통과. `master-bom.html`에는 실제 BOM 부모 기준 데이터가 HNS02 계열뿐이라 HNS02 증적이 남아 있다.
- `T-MASTER-EQUIP-REPORT-EVIDENCE-FIX`: `/master/equip` 설비관리 QA 보고서의 STEP 05 증적 불일치를 보정 완료. 원인은 검색 단계에서 입력한 `HNS02` 필터가 저장/API/DB 검증 캡처까지 남은 것이었다. `tools/hanes-master-remaining-page-scenario-qa.mjs`는 버튼 목록화와 저장 검증 증적 전에 검색 조건 초기화+재조회를 수행하고, STEP 05 캡션을 `검색 초기화 후 API/DB 검증 기준 화면`으로 기록한다. 재생성 결과 `docs/reports/hanes-page-scenario-qa-2026-06-15/master-remaining-result.json` 기준 19/19 PASS, `master-equip` 5/5 PASS, 05번 이미지 누락 0, 육안 확인 시 검색어 빈 상태의 설비 목록 44건 그리드가 표시된다.
- `T-MASTER-REMAINING-PAGE-SCENARIO-QA`: 기준정보 잔여 19개 화면 페이지 단위 상세 시나리오 QA 완료. 대상은 `/master/part`, `/master/bom` 제외 기준정보 화면 전부다. 최종 결과 `docs/reports/hanes-page-scenario-qa-2026-06-15/master-remaining-result.json` 기준 status PASS, 19/19 PASS, failedSteps 0, missingReports 0, missingShots 0, CRUD failures 0, Oracle 잔여 0건. 기존 품목/BOM 포함 목차 `docs/reports/hanes-page-scenario-qa-2026-06-15/index.html`에는 기준정보 21개 페이지 링크가 들어 있다. 실행 스크립트는 `tools/hanes-master-remaining-page-scenario-qa.mjs`. 저장 검증은 `docs/reports/hanes-master-crud-runtime-test-26061511325085.json`의 101단계 기준정보 CRUD 결과를 연결했다.
- `T-MASTER-BOM-PAGE-SCENARIO-QA`: `/master/bom` BOM관리 화면을 페이지 단위 상세 시나리오로 테스트 완료. `tools/hanes-master-bom-page-scenario-qa.mjs`가 실제 브라우저, UI 네트워크, 직접 API, Oracle DB 검증을 함께 수행한다. 최종 부모는 `HNS02`, 테스트 자품목은 `FBOMC-260615112131`, 결과는 13단계 전부 PASS, 기록 API 38건, DB 검증 11건, 스크린샷 13장, failedApi 0, consoleErrors 0, pageErrors 0, 최종 BOM/자품목 잔여 0건. 목차는 `docs/reports/hanes-page-scenario-qa-2026-06-15/index.html`, 페이지 상세는 `docs/reports/hanes-page-scenario-qa-2026-06-15/pages/master-bom.html`, 상세 JSON은 `docs/reports/hanes-page-scenario-qa-2026-06-15/master-bom-result.json`.
- `T-MASTER-PART-PAGE-SCENARIO-QA`: `/master/part` 품목관리 화면을 페이지 단위 상세 시나리오로 테스트 완료. `tools/hanes-master-part-page-scenario-qa.mjs`가 실제 브라우저와 API/Oracle DB 검증을 함께 수행한다. 최종 테스트 키는 `FECRUD-260615104355`, 결과는 10단계 전부 PASS, API 호출 기록 24건, DB 검증 9건, 스크린샷 누락 0개, 최종 테스트 데이터 잔여 0건. 목차는 `docs/reports/hanes-page-scenario-qa-2026-06-15/index.html`, 페이지 상세는 `docs/reports/hanes-page-scenario-qa-2026-06-15/pages/master-part.html`, 상세 JSON은 `docs/reports/hanes-page-scenario-qa-2026-06-15/master-part-result.json`.
- `T-MENU-QA-DETAIL-REPORT`: 좌측 메뉴 QA 최종 HTML 보고서를 메뉴별 상세 절차형으로 재생성 완료. `docs/reports/ui-test-crud-red-menu-qa-2026-06-15.html`에는 96개 메뉴 각각의 메뉴명/코드/경로/그룹, 수행 절차, 확인 기준, 최종 화면 캡처가 별도 섹션으로 들어간다. 재생성 스크립트는 `tools/generate-menu-qa-detailed-report.mjs`. 검증은 `detailSections=96`, `<img>=96`, 스크린샷 누락 0, 최종 PASS 96/96, 재검증 필요/실패 문구 없음, 관련 파일 `git diff --check` 통과.
- `T-UI-CRUD-RED-MENU-QA`: 현재 좌측 메뉴에 노출된 96개 화면 전체를 실제 브라우저로 순회 검증 완료. 최종 결과는 `docs/reports/ui-test-crud-red-menu-qa-2026-06-15/result.json` 기준 96/96 PASS, 실패 0. 최종 HTML 보고서는 `docs/reports/ui-test-crud-red-menu-qa-2026-06-15.html`, 스크린샷은 `docs/reports/ui-test-crud-red-menu-qa-2026-06-15/screenshots/*.png`. 수정 사항은 `/production/wip-stock` 조회를 `ProductStock` 기준으로 정상화, `/system/config`의 `configKey` 식별자 사용, runner 재시도/로그인 오탐/HTML 양식 보강. 검증은 결과 JSON/HTML/스크린샷 무결성, `production-views.service.spec.ts`, frontend `tsc --noEmit`, 백엔드 health DB connected로 완료.
- `T-SQL-ACTUAL-GLOBAL`: 모든 `DataGrid.sqlQuery` SQL 조회문이 하드코딩 preview SQL보다 실제 실행 SQL을 우선 표시하도록 전역 적용 완료. 백엔드 `SqlDebugTypeormLogger`/요청 SQL context/`SqlDebugInterceptor`가 GET 응답 `meta.debugSql`에 실제 SELECT/parameters/tables/queries를 붙이고, 프론트 Axios 응답 인터셉터가 이를 캐시한다. `SqlViewerModal`은 preview SQL과 같은 테이블의 최신 실제 SQL을 우선 표시한다. `getManyAndCount()`의 COUNT-only SELECT는 대표 SQL에서 후순위 처리. 검증: 구조 테스트 2건, `sql-debug-context.spec.ts`, FE/BE tsc, backend build, diff check, 실제 API `material/iqc-history/pending-arrivals` 및 `master/parts` 응답 `meta.debugSql` 확인 완료. 백엔드 3003은 새 `dist/main` 프로세스 PID 4972로 재시작됨.
- `T-SQL-SCHEMA-TOGGLE`: SQL 조회문 모달의 컬럼명세를 기본 숨김으로 바꾸고 `컬럼명세 보기/숨기기` 버튼으로 사용자가 원할 때만 `/system/table-schema`를 호출하도록 공통 적용 완료.
- `T-IQC-SQL-DISPLAY`: `/material/iqc` 최초 그리드 SQL 조회문을 기존 `MAT_ARRIVALS` 하드코딩 대신 백엔드 실제 QueryBuilder SQL/parameters 기반으로 표시하도록 완료.
- `T-QC-SAMPLE-MENU-LABEL`: 품질검사 하위 `QC_SELF_INSPECT_HISTORY` 좌측 메뉴 한글 라벨을 `공정샘풀검사`로 변경 완료. 변경 파일은 `apps/frontend/src/locales/ko.json`이고, `menuConfig.ts`는 기존 `menu.quality.selfInspectHistory` labelKey를 그대로 사용한다. `ko.json` JSON 파싱 검증 통과.
- `T-MASTER-FE-QA`: 기준정보 `/master/*` 프론트엔드 21개 하위 메뉴 실테스트 완료. `tools/hanes-master-frontend-qa.mjs`가 `http://localhost:3002` 실제 프론트와 `http://localhost:3003/api/v1` 백엔드에 `admin@hanes.com`, company `40`, plant `1000` 세션으로 접속해 화면 진입, 검색/조회, 추가/신규 폼 비파괴 열기, 콘솔/API 오류 감시, 화면 캡처를 수행했다. 최종 실행 `node tools\hanes-master-frontend-qa.mjs` 결과 21/21 성공, 실패 0건, 캡처 60개. HTML 보고서는 `docs/reports/hanes-master-frontend-qa-2026-06-12.html`, 상세 JSON은 `docs/reports/hanes-master-frontend-qa-2026-06-12/result.json`.
- `T-IQC-SERIAL3-RUNTIME`: 수입검사(IQC) 절차대로 시리얼 3개 실데이터 처리 완료. 최종 stamp `26061203191605`, 구매발주 `PO-IQC3-26061203191605`, 입하 `R26061200008`, 시리얼 `VH1-RM260612-00017/00018/00019`, 입고 `RCV20260612-0007`이다. 실제 API 17단계가 모두 성공했고, Oracle `JSHANES` 조회로 발주 확정, 입하 3건 `PASS/DONE`, LOT 3건 `PASS/NORMAL`, IQC 이력 `PASS`, 입고 3건 `DONE`, 수불 6건, 각 시리얼 `RM_MAIN` 재고 20 및 `W001` 재고 0을 확인했다. 테스트 데이터는 삭제하지 않았다. 산출물은 `tools/hanes-iqc-serial3-runtime-test.mjs`, `docs/reports/hanes-iqc-serial3-runtime-test-26061203191605.json`, `docs/reports/hanes-iqc-serial3-runtime-test-2026-06-12.md`.
- `T-MASTER-CRUD-RUNTIME`: 기준정보 화면/API CRUD 실데이터 점검 완료. 최종 stamp `26061202474131`로 `http://localhost:3003/api/v1`에 company `40`, plant `1000`, 계정 `admin@hanes.com` 헤더를 사용해 101개 API 단계가 모두 성공했고 cleanup 30건도 모두 성공했다. 범위는 공통코드/거래처/품목/공정/라인/작업자, 창고/로케이션/이동규칙, 설비/공정-설비/설비BOM/설비점검, BOM/라우팅/공정CAPA, 제조사바코드, IQC 기준정보, 라벨/작업지도서/교대/작업달력, 계측기다. Oracle `JSHANES` 조회로 최종 stamp가 관련 32개 테이블에 남지 않음(`nonzero=[]`)을 확인했다. 산출물은 `tools/hanes-master-crud-runtime-test.mjs`, `docs/reports/hanes-master-crud-runtime-test-26061202474131.json`, `docs/reports/hanes-master-crud-runtime-test-2026-06-12.md`.
- `T-INTEGRATION-NORMAL-REVERSE`: HNS02 기준 정상 처리와 역처리 재테스트 완료. 정상 데이터는 `PO-NR-26061202215660`, `JO-OK-26061202215660`, `PR26061200022`, `P26061200009`, `BX-OK-26061202215660`, `SO-OK-26061202215660`이며 출하 후 출하지시 `CLOSED`, 박스 `SHIPPED`, FG/WIP 재고 0 확인. 역처리 데이터는 `JO-RV-26061202215660`, `PR26061200023`, `P26061200010`, `BX-RV-26061202215660`, `SO-RV-26061202215660`이고 `cancel-ship-box` 후 출하지시 `CONFIRMED`, `SHIPPED_QTY=0`, 박스 `CLOSED`, 라벨 `PACKED`, FG 재고 1, `FG_OUT_CANCEL PTX2026061200020` 확인. 삭제/취소는 DRAFT 출하지시 `SO-DEL-26061202215660`, 빈 박스 `BX-DEL-26061202215660`, 작업지시 `JO-CXL-26061202215660`, 생산실적 `PR26061200024`, 자재출고 `ISS20260612-0013/1`로 별도 검증했다. 보고서 `docs/reports/hanes-integration-normal-reverse-test-2026-06-12.md`, 증적 JSON `docs/reports/hns02-normal-reverse-runtime-test-26061202215660.json`.
- `T-INTEGRATION-FLOW-ISSUES-FIX`: 최종보고서 등록 문제점 3건 해결. 제품라벨 API는 문자열 생산실적번호를 받고 `FG_LABELS`를 생성/보강하며 `LABEL_PRINT_LOGS.PRINTED_AT`을 명시한다. 박스 마감은 `FG_LABELS.BOX_NO`를 함께 찍어 박스재고 조회가 가능하게 했다. 완제품 입고는 `WIP_MAIN -> FG_MAIN` 이동(`WIP_OUT`)으로 처리해 WIP 잔량을 남기지 않는다. JSHANES 재테스트 `JO-FIX-105908`/`P26061200004`/`BX-FIX-105908`/`SO-FIX-105908` 결과 출하지시 `CLOSED`, 박스 `SHIPPED`, `missingLabel=0`, 출하 전 박스재고 serial 1건, WIP/FG 재고 0 확인. 보고서 `docs/reports/hanes-integration-flow-test-2026-06-12.md` 갱신.
- `T-INTEGRATION-FLOW-REPORT`: HNS02 기준 전 공정 통합 테스트 완료. 실제 API/JSHANES DB로 PO `PO-IT-0612102556` → 입하 `R26061200002` → IQC/검사성적서 → PDA 자재입고 `RCV20260612-0001` → 작업지시 `JO-IT-0612102813` → 자재요청/출고 `MR2606120004`/`ISS20260612-0001` → 생산실적 `PR26061200015` → 제품입고 `PTX2026061200002` → 포장 `BX-IT-0612103010` → OQC `OQC-20260612-001` → 출하지시/출하 `SO-IT-0612103010`을 완료했다. 출하 중 `shipBox()`가 `prdUid='*'`로 제품재고를 차감해 실제 시리얼 재고를 찾지 못하는 결함을 수정했고, 단위 테스트 19건 및 같은 실데이터 재호출로 검증했다. 보고서는 `docs/reports/hanes-integration-flow-test-2026-06-12.md`.
- `T-CUSTOMER-INTRO-FLOW-SLIDE`: 고객 소개 자료 4페이지에 HANES MES 기능흐름도 추가 완료. `docs/presentation/hanes-mes-introduction.html`은 24장, `docs/presentation/hanes-mes-introduction.pptx`도 24장으로 재생성. PPTX 패키지 media 24개/빈 media 0개, HTML 이미지 누락 0개, 신규 4페이지 overflow 없음 확인.
- `T-KIOSK-AUTOISSUE-BOM-MISMATCH-GUARD`: 키오스크 투입스캔 LOT가 BOM 품목과 불일치하면 실적처리/자동차감 전 중단하도록 `AutoIssueService`에 방어 추가. `JOB_MATERIAL_LOTS.itemCode`와 실제 `MAT_LOTS.itemCode`를 유효 BOM child 품목과 대조하고, 불일치/누락 시 `BadRequestException`을 던진다. 검증은 auto-issue 단위 테스트 13건, job-material-lot+auto-issue 17건, backend tsc 통과.
- `T-CUSTOMER-INTRO-PPTX-EXPORT`: 고객 소개 HTML 23장 기준으로 `docs/presentation/hanes-mes-introduction.pptx`를 편집 가능한 PPTX로 재생성 완료. 제목/본문/절차 박스/메뉴 표/캡션/하단 문구는 PowerPoint 텍스트·도형 객체이며, 실제 화면 캡처와 회사 배경 이미지는 이미지 객체다. 검증은 PPTX 패키지 슬라이드 23장, 미디어 25개, 빈 미디어 0개, PowerPoint PNG 렌더 23장 및 대표 슬라이드 시각 확인.
- `T-QUALITY-INSPECT-USEMEMO`: `/quality/inspect` 외관검사 화면의 `useMemo is not defined` 런타임 오류 수정 완료. 원인은 통계 카드 제거 과정에서 React import의 `useMemo`가 같이 빠졌지만 DataGrid `columns` 정의는 계속 `useMemo`를 사용한 것. `useMemo` import만 복원했고 `pnpm --filter @harness/frontend exec tsc --noEmit --pretty false` 통과, 같은 누락 패턴 추가 검색 결과 없음.
- `T-DATA-CLEAN-HNS02`: JSHANES HNS02 BOM 기준 데이터 클린징 완료. `BOM_MASTERS.BOM_GRP='HNS02'` parent/child 합산 47개 기준으로 `ITEM_MASTERS=47`, `BOM_MASTERS=47`만 유지. 입하/입고/IQC/자재입출고/재고/제품재고/제품실적/작업지시/창고입고/검사의뢰/품질검사/시뮬레이션 데이터 제거 완료. 최종 검증 결과 비-HNS02 `ITEM_CODE` 잔여 없음, 요청 업무 테이블 잔여 없음.
- `T-INV-TRANSACTION-CARDS`: `/inventory/transaction` 재고수불현황 상단 정보카드 3개 제거 완료. `StatCard` 렌더링과 카드 전용 통계 상태/계산/import만 제거했고 그리드 조회·필터·MAT UID 검색·내보내기는 유지했다. `inventory/transaction` 잔여 참조 검색과 diff check 통과, URL HTTP 200 확인. frontend typecheck는 1차 통과 후 최종 재실행에서 다른 변경 파일 `quality/inspect/page.tsx`의 `useMemo` import 누락/implicit any 오류로 실패했다. 기존 `next lint` 스크립트는 ESLint 설정 프롬프트로 실패하고 `browse.exe`는 자체 서버 시작 타임아웃으로 DOM 검증을 못 했다.
- `T-MAT-LOT-IQC-UID-SEPARATE`: JSHANES에서 `MAT_LOTS`와 `IQC_LOGS`가 같은 `MAT_UID`를 공유하던 시드성 LOT를 분리했다. IQC 이력은 `VH1-RM...` 그대로 두고 재고/LOT 쪽 `MAT_LOTS`, `MAT_STOCKS`, `STOCK_TRANSACTIONS`만 `MLT-RM...`으로 변경했다. 검증 결과 `MAT_LOT_IQC_OVERLAP=0`, 파일 재실행 성공.
- `T-IQC-SAMPLE-REMOVE`: IQC 검사구분에서 `SAMPLE` 제거 완료. `IQC_GROUPS.INSPECT_METHOD`와 `ITEM_MASTERS.INSPECT_METHOD`는 `FULL/SKIP`만 쓰고, JSHANES 공통코드 `IQC_INSPECT_METHOD/SAMPLE`은 비활성화했다. 기존 `IQC_GROUPS` SAMPLE 4건은 FULL로 정규화. `IQC_LOGS.INSPECT_CLASS`는 별도/legacy 이력 컬럼이라 기존 SAMPLE 10건은 그대로 두고, 신규 수입검사 입력에서 검사구분 값을 inspectClass로 보내지 않도록 끊었다. 구조 테스트 7건, FE/BE tsc, IqcGroupService 테스트, DB 검증 통과.
- `T-IQC-METHOD-LABELS`: IQC 검사/무검사 구분 표시를 통일했다. `IQC_INSPECT_METHOD` 공통코드는 코드값은 유지하고 JSHANES 표시를 `FULL=검사`, `SAMPLE=검사`, `SKIP=무검사`로 적용했다. 품목정보, IQC 검사그룹, 수입검사 목록/입력, 검사이력 범위 라벨은 `검사구분`으로 맞췄고 ko/en/zh/vi locale 및 fallback 문자열을 갱신했다. 구조 테스트 5건, frontend typecheck, JSHANES 조회 통과.
- `T-IQC-CODE-ALIGN`: IQC 검사방법/검사유형 코드 매핑 통일 완료. 원인은 `FULL/SAMPLE/SKIP`가 필요한 IQC 검사방법이 일반 `INSPECT_METHOD`(VISUAL/MEASUREMENT 등)와 이름만 공유하고, IQC 이력 `INITIAL/RETEST`가 `IQC_TYPE`(IQC/PQC/FQC/OQC)으로 표시된 것. `IQC_INSPECT_METHOD`, `IQC_INSPECT_TYPE` 공통코드를 JSHANES에 추가하고 품목정보/IQC 검사그룹/수입검사/검사입력/IQC 이력 화면을 전용 그룹으로 변경했다. 검사입력 legacy `NONE`은 `SKIP`으로 정규화. 구조 테스트, FE/BE tsc, DB 코드 확인, ERD 문서 재생성 완료.
- `T-PROCESS-EQUIP-SEED`: 공정별 설비 마스터/공정-설비 매핑 시드를 추가하고 JSHANES에 적용했다. `apps/backend/src/migrations/2026-06-11_process_equipment_seed.sql`은 활성 공정 21개 기준으로 WIRE/TERMINAL/INSPECTION은 2대, ASSEMBLY/HEAT/미분류는 1대씩 `EQ-<PROCESS_CODE>-NN` 설비를 생성한다. 실DB 검증 결과 `EQUIP_MASTERS` 시드 36건, `PROCESS_EQUIPMENTS` 시드 36건, 모든 활성 공정 21개에 시드 매핑 존재. 기존 이상 매핑은 삭제하지 않았다.
- `T-MENU-SHELF-LIFE-REINSPECT`: `MAT_SHELF_LIFE_REINSPECT`가 미배치 후 메뉴관리에서 사라지고 카테고리 이동이 실패하는 문제 수정. 원인은 `menuConfig.ts` leaf가 백엔드 `menu-code-validator.ts`에 누락된 것. 누락 leaf 7개를 validator에 추가하고 구조 테스트를 추가했다. JSHANES에는 유수명 3개 메뉴를 MATERIAL 카테고리로 복구하고 MANAGER 권한을 보강했다. `/menu-categories/tree`와 `/menu-category-items/move` 실측 성공.
- `T-FE-THEME-PRESET`: 상단 팔레트 아이콘에서 `default → custom → orchid` 순환 선택 가능하게 변경. Orchid 테마는 사용자가 제공한 OKLCH 토큰을 `data-color-theme="orchid"` preset으로 추가. dev 서버는 `http://localhost:3004`에서 응답 200 확인. `pnpm --filter @harness/frontend build`는 컴파일/타입 단계 통과 후 기존 누락 라우트 `PageNotFoundError`로 정적 생성 실패.
- `T-TAB-LIMIT-10`: 페이지 탭 제한 개수를 10개로 변경 완료. `MAX_TABS=10`, `MAX_ALIVE=10`으로 맞췄고 frontend typecheck 및 layout 구조 테스트 2건 통과.
- `T-CUSTOMER-INTRO-WORK-INSTRUCTION`: 고객용 제품 소개 자료를 다음에도 동일 기준으로 재생성할 수 있도록 작업지시 문서 작성 완료. 산출물은 `docs/presentation/hanes-mes-introduction-work-instruction.md`.
- `T-CUSTOMER-INTRO-HTML-DESIGN`: 고객 소개 HTML의 카드형 AI 느낌을 줄이기 위해 공통 CSS를 재정리했다. 흰 카드+상단 컬러바 문법을 제거하고 산화동/황동/스틸/세이지 계열의 공정 보드 스타일로 변경. 산출물은 `docs/presentation/hanes-mes-introduction.html`.
- `T-CUSTOMER-INTRO-HTML-V2`: 작업지시서 기준 고객 소개 HTML을 22장 가로형 슬라이드로 재구성 완료. 산출물은 `docs/presentation/hanes-mes-introduction.html`. 정적 검증 결과 슬라이드 22장, 이미지 56개, 누락 0개. 이번 단계에서는 PPTX를 생성하지 않았다.
- `T-CUSTOMER-INTRO-MENU-SCREEN-DECK`: 현재 메뉴 화면 캡처 기반 고객용 제품 소개 자료 확장 완료. 산출물은 `docs/presentation/hanes-mes-introduction.pptx`, `docs/presentation/hanes-mes-introduction.html`, 캡처는 `docs/presentation/assets/menu-captures/*`. PPTX 15장/미디어 47개/빈 미디어 0개, HTML 15장/이미지 누락 0개, 레이아웃 오류 0개 확인.
- `T-CUSTOMER-INTRO-PRODUCT-DECK`: 기존 구성안 느낌의 자료를 고객용 제품 소개 자료로 전면 재작성 완료. 산출물은 `docs/presentation/hanes-mes-introduction.html`, `docs/presentation/hanes-mes-introduction.pptx`. PPTX 10장/미디어 7개/빈 미디어 0개, HTML 10장/이미지 누락 0개 확인.
- `T-CUSTOMER-INTRO-PPTX`: 고객 소개용 HANES MES 가로형 PowerPoint 문서 생성 완료. 산출물은 `docs/presentation/hanes-mes-introduction.pptx`. 12장, 미디어 9개, 빈 미디어 0개, 레이아웃 오류 0개 확인.
- `T-CUSTOMER-INTRO-HTML-REV`: 고객 소개 HTML 자료를 12장 워크플로우형으로 보강 완료. 글자 크기 축소, 메뉴 노출 확대, 워크플로우 순서 재구성, overflow 검증 완료.
- `T-CUSTOMER-INTRO-HTML`: 고객 소개용 HANES MES 가로형 HTML 자료 생성 완료. 산출물은 `docs/presentation/hanes-mes-introduction.html`, 화면 캡처는 `docs/presentation/assets/*.png`.
- .ai-coordination 작업추적 문서 초기화 완료.

## Next AI Should

- `T-SHIP-ORDER-TOP-ACTIONS` 완료. `/shipping/order` 행 관리 컬럼은 수정 아이콘만 남고, 상단에 선택 행 기준 `인쇄/확정/확정취소/삭제` 버튼이 있다. 행 클릭 시 선택/하이라이트되고 상태별로 버튼이 비활성화된다. 검증: 신규 RED/GREEN 구조 테스트, 관련 shipping/order 구조 테스트 6개 PASS, FE tsc PASS, 3002 Playwright에서 행 버튼 1개(`수정`), 상단 버튼 4개, CONFIRMED 선택 시 확정취소 활성화, console error 0. 커밋하지 않았다.
- `T-SHIP-OQC-GATE-CONSISTENCY` 완료. `OQC_ENABLED=N`이면 출하지시 fulfillment 후보, 팔레트 적재, 팔레트 출하, `/shipping/pallet` 후보/스캔 조회가 더 이상 `OQC_STATUS=PASS`를 강제하지 않는다. 설정 미로드 상태는 기존처럼 PASS 필터를 유지한다. 검증: backend ship-order spec 34/34, pallet 구조 테스트 4/4, BE/FE tsc, 실제 `node tools/hanes-shipping-workflow-scenario-qa.mjs` PASS(`SWF260626000949`, residue 0). 리포트는 `docs/reports/hanes-shipping-workflow-scenario-qa-2026-06-26/index.html`. 작업 중 HEAD가 `131fb013`으로 갱신되어 코드 변경은 이미 커밋에 포함됐고, 현재 남은 것은 2026-06-26 리포트 미추적 파일과 협업 문서 정리 변경이다.
- `T-SHIP-HISTORY-SHIPPED-DETAIL` 완료 후 REVIEW 상태. `/shipping/history` 우측 패널은 이제 `fulfillment` 대신 기존 `GET /shipping/orders/:id/shipped-detail`를 사용해 팔레트 없는 박스 단건 출하(`boxShipped`)도 표시한다. 실측 케이스 `SH2606250005`: `PALLET_MASTERS` 0건, `BOX_MASTERS` `BX2606250001`/수량 10. 3002에서 우측 패널 `팔레트 0 / 박스 1 / 총수량 10` 표시 확인. 검증: RED/GREEN 구조 테스트, history 구조 테스트 4건, FE tsc, 3002 Playwright, diff check PASS. 스크린샷 `docs/reports/shipping-history-shipped-detail-3002.png`. 커밋하지 않았다.
- `T-SHIP-ORDER-SAVE-CONFIRM` 완료 후 REVIEW 상태. `/shipping/order` 신규/기존 DRAFT 패널 하단에 `저장 후 확정` 버튼을 추가했다. 신규는 `POST /shipping/orders` 응답 `shipOrderNo`로 즉시 `PUT /shipping/orders/:id/confirm`을 호출하고, 기존 DRAFT는 저장 후 confirm한다. 검증: RED/GREEN 구조 테스트, 관련 order 구조 테스트 4건, FE tsc, 기존 3002 Playwright DOM 확인, diff check PASS. 스크린샷 `docs/reports/shipping-order-save-confirm-3002.png`. 커밋하지 않았다.
- `T-SHIP-PALLET-LAYOUT-TIDY` 완료 후 REVIEW 상태. `/shipping/pallet` 우측 포함박스 섹션은 `xl` 이상 18rem, `2xl` 이상 20rem으로 축소했고 1155px 폭에서는 아래로 내려가도록 했다. 툴바는 화면 전용 grid로 바꾸고 날짜 프리셋 버튼/검색 최소폭을 줄여 아이콘/입력 겹침을 제거했다. 검증: 팔레트 구조 테스트 5/5, FE tsc, diff check, 기존 3002 Playwright 렌더 측정 PASS. 스크린샷 `docs/reports/shipping-pallet-layout-3002.png`. 커밋하지 않았다.
- `T-ER-VIEW-MULTI-TABLE-SELECT` 완료. `/system/er-view` 좌측 테이블 목록은 checkbox 다중 선택으로 바뀌었고, 선택된 각 테이블의 기존 `/graph?table=...&depth=1` 응답을 프론트에서 node/edge/relationship `id` 기준 병합해 표시한다. `selectedTables[0]`가 포커스 테이블이며, 새로 체크하거나 그래프 노드를 클릭하면 포커스로 올라간다. 마지막 1개 선택은 해제하지 않는다. 검증: ER VIEW structure test, FE tsc, diff check, 3002 HTTP 200 PASS. 커밋하지 않았다.
- `T-ER-VIEW-FK-NAME-RISK-DISPLAY` 완료. `/system/er-view` 물리 FK 관계에서 실제 `constraintName`을 우측 상세 `현재 FK명`과 그래프 edge 라벨에 표시한다. 추정 관계에는 실행 전 확인용 `FK 후보명`을 표시한다. `PHYSICAL_FK_EXISTS`는 `-100` 리스크 점수가 아니라 `이미 FK 존재: 신규 생성 불필요` 상태 사유로 바꿨고, UI는 양수 리스크만 점수로 보여준다. 검증: backend ER VIEW spec, frontend ER VIEW structure test, FE/BE tsc, diff check PASS. 커밋하지 않았다.
- `T-PARTNER-ID-TO-PARTNER-CODE` 완료. JSHANES `PURCHASE_ORDERS.PARTNER_ID`는 `PARTNER_CODE`로 rename됐고 `USER_TAB_COLUMNS` 기준 `PARTNER_ID` 0건 확인. 구매발주 entity/DTO/service/spec, ERP PO 수신 controller/service/spec, 입하 서비스 PO 참조, 구매발주 프론트 패널/입하 타입, ER VIEW 추정 매핑, `create-hanes-schema.sql`, `docs/reports/db-schema-erd.md`를 갱신했다. 검증: migration 적용/재실행, ERD 재생성, purchase-order/erp-material/arrival/er-view focused spec, backend build, frontend tsc, diff check PASS. 커밋하지 않았다.
- `T-VENDOR-ID-TO-VENDOR-CODE` 완료. JSHANES `MAT_ARRIVALS`, `SUBCON_ORDERS`, `WAREHOUSES`의 `VENDOR_ID`는 모두 `VENDOR_CODE`로 rename됐고 `USER_TAB_COLUMNS` 기준 `VENDOR_ID` 0건 확인. `MatArrival`/`SubconOrder`/`Warehouse` entity, 입하/외주/창고 DTO와 service, 관련 프론트 타입/form, ER VIEW 추정 매핑, `create-hanes-schema.sql`, `docs/reports/db-schema-erd.md`를 갱신했다. 검증: migration 적용/재실행, ERD 재생성, arrival/outsourcing/er-view focused spec, backend build, frontend tsc, diff check PASS. 커밋하지 않았다.
- `T-SHIP-PALLET-SHIP-STATUS-HELP` 완료. `/shipping/pallet-ship` 중앙 팔레트 그리드 상태 컬럼에 `?` 도움말을 추가했고, 출하번호 컬럼은 `shipmentId` 우선 + 단일 fulfillment 출하번호 fallback + `출하 전`/`확인필요` 안내로 보정했다. 검증: 신규 구조 테스트, 관련 shipping pallet 구조 테스트 6/6, FE typecheck, 3002 Playwright DOM 확인, 대상 파일 diff check PASS. 커밋하지 않았다.
- `T-DEFECT-CODE-PAGE-HEIGHT` 완료. `/quality/defect-code`의 `h-[calc(100vh-150px)]` 고정 높이를 제거하고 루트 `flex flex-col`, 헤더 `shrink-0`, 본문 `flex-1 min-h-0` 구조로 바꿔 하단 overflow를 보정했다. 검증: 구조 테스트 9/9 PASS, FE typecheck PASS, 대상 파일 diff check PASS. 3002/3003은 재시작하지 않았다.
# 2026-06-26 00:08 KST - T-LABEL-TEXT-IMAGE-INPUT 완료

- 상태: REVIEW
- 변경 파일:
  - `apps/frontend/src/app/(authenticated)/master/label/components/LabelObjectDesigner.tsx`
  - `apps/frontend/src/app/(authenticated)/master/label/master-label-object-inputs.structure.test.mjs`
  - `apps/backend/src/modules/master/controllers/label-template.controller.ts`
  - `apps/backend/src/modules/master/controllers/label-template-upload.structure.test.mjs`
- 내용:
  - 글자/이미지 객체는 툴바 추가 시 기본 소스 필드에 묶지 않는다. `고정값 사용` option으로 소스 필드를 비울 수 있다.
  - 필드 목록에서 글자를 추가할 때만 해당 sourceField를 설정한다.
  - 텍스트 고정 문구는 `TextareaInput`으로 직접 입력한다.
  - 이미지 객체 속성에 업로드 버튼, URL 입력, 미리보기, 제거 버튼을 추가했다.
  - 라벨 전용 업로드 API `POST /master/label-templates/upload-image`는 `/uploads/label-templates`에 저장하고 `{ url }`을 반환한다.
- 검증:
  - RED/GREEN 구조 테스트 완료
  - PASS: `node --test "apps/frontend/src/app/(authenticated)/master/label/master-label-object-inputs.structure.test.mjs" "apps/frontend/src/app/(authenticated)/master/label/master-label-box-stroke.structure.test.mjs" "apps/backend/src/modules/master/controllers/label-template-upload.structure.test.mjs"`
  - PASS: frontend/backend tsc
  - PASS: 3002 `/master/label` Playwright 확인. 텍스트 고정값, 이미지 업로드 200, 정적 URL 200, 화면 미리보기, console error 0건
  - PASS: 대상 파일 `git diff --check`
- 참고:
  - `LabelObjectDesigner.tsx`는 `T-KIOSK-SG-LABEL-PRINT` active lock에 포함되어 있었지만, 사용자가 본 대화에서 충돌 수정 진행을 승인했다.
  - locale 파일은 건드리지 않고 fallback 문자열만 사용했다.
  - 검증 중 생성한 업로드 파일은 삭제했다.

# 2026-06-25 23:42 KST - T-LABEL-BOX-STROKE 완료

- 상태: REVIEW
- 변경 파일:
  - `apps/frontend/src/app/(authenticated)/master/label/components/LabelDesignRenderer.tsx`
  - `apps/frontend/src/app/(authenticated)/master/label/master-label-box-stroke.structure.test.mjs`
- 내용:
  - 박스/원/선 stroke를 outer CSS border가 아니라 `ShapeStrokeLayer` 내부 레이어로 렌더링하도록 변경했다.
  - 박스/원은 `box-shadow: inset ...`, 선은 내부 absolute span으로 그린다.
  - shape 객체는 `overflow: visible`로 바꿔 출력 캡처 가장자리에서 stroke가 잘리는 조건을 줄였다.
  - `T-KIOSK-SG-LABEL-PRINT` active lock 범위인 `LabelObjectDesigner.tsx`, `types.ts`, `page.tsx`, locale 파일은 수정하지 않았다.
- 검증:
  - RED: 신규 구조 테스트가 `ShapeStrokeLayer` 부재로 실패
  - PASS: `node --test "apps/frontend/src/app/(authenticated)/master/label/master-label-box-stroke.structure.test.mjs" "apps/frontend/src/app/(authenticated)/production/input-kiosk/components/kiosk-sg-label-print.structure.test.mjs"`
  - PASS: `pnpm.cmd --filter @harness/frontend exec tsc --noEmit --pretty false`
  - PASS: 3002 `/master/label` Playwright 인증 세션에서 `data-label-shape-stroke=2`, console error 0건
  - PASS: 대상 파일 `git diff --check`
- 참고:
  - `apps/frontend/src/app/(authenticated)/consumables/label/consumable-label-reprint.structure.test.mjs`는 이번 변경과 무관하게 현재 코드가 i18n 함수 호출로 버튼 텍스트를 렌더링해 테스트의 `>미리보기<` 정규식과 맞지 않아 실패한다. 이번 scope 밖이라 수정하지 않았다.

- `T-DEFECT-CODE-GRID-LEVEL-COLUMNS` 완료. `/quality/defect-code` 좌측 전체 불량코드 그리드의 단일 `분류` 경로 컬럼을 `1레벨`, `2레벨`, `3레벨` 컬럼으로 분리했다. 검증: 구조 테스트 8/8 PASS, FE typecheck PASS, 대상 파일 diff check PASS. 3002/3003은 재시작하지 않았다.
- `T-DEFECT-CODE-MASTER` 후속 보정 완료. `/quality/defect-code`는 좌측 전체 불량코드 그리드 + 우측 1/2/3레벨 선택 등록/수정 폼으로 단순화했다. 기존 분류 트리와 분류 선택 조회 필터는 제거했고, 우측 하단에는 `분류 빠른 추가`만 남겼다. 검증: 구조 테스트 7/7 PASS, locale JSON parse PASS, FE typecheck PASS, 트리/분류필터 잔재 검색 없음, `git diff --check` PASS. 3002/3003은 사용자 요청대로 재시작하지 않았다.
- `T-WIP-FG-LABEL-SOURCE-SPLIT` 완료 후 REVIEW 상태. 재공 상세 라벨 API는 이제 `/production/wip-stock/labels?itemCode=&itemType=`이며, `SEMI_PRODUCT`는 `SG_LABELS`, `FINISHED`는 `FG_LABELS`를 조회한다. 프론트 우측 패널은 공통 컬럼 `구분/라벨바코드/상태/잔량/검사/작업지시/현재공정/장착설비/발행일시`를 표시한다. 기존 `/fg-labels` endpoint는 호환용 FINISHED wrapper로 남겼다. 3002 실측: `HNS02C2ABCDE` 반제품은 `SG_LABELS` SQL과 `labelType=SG` 1건 반환, FINISHED 요청은 `FG_LABELS` SQL 확인. 구조 테스트 9건, FE/BE tsc, 브라우저 API/DOM 검증 PASS. 커밋하지 않았다.
- `T-WIP-STOCK-LABEL-DETAIL-PANEL`은 후속 `T-WIP-FG-LABEL-SOURCE-SPLIT`으로 보강됐다. `/production/wip-stock` 우측 `상세 라벨정보` 패널은 유지하되, 현재 행 클릭 API는 `/production/wip-stock/labels?itemCode=&itemType=`가 맞다. 이전 `/fg-labels` 단일 조회 설명은 현재 계약이 아니다.
- `T-FG-STOCK-CARD-REMOVE-TYPE-FILTER` 완료 후 REVIEW 상태. `/production/fg-stock` 상단 정보카드 2개를 제거했고, 좌측 그리드 툴바에 `유형: 전체/완제품/반제품` Select를 추가했다. 기본값은 `FINISHED`, 선택 변경 시 API `itemType`과 SQL 미리보기 조건이 함께 바뀐다. `/production/wip-stock`는 필터 없이 `SEMI_PRODUCT` 고정 유지. 검증: 신규 구조 테스트 RED→GREEN, split/SQL 구조 테스트 PASS, FE tsc PASS, 3002 HTTP 200, Playwright DOM/API/console 검증 PASS, diff check PASS. 커밋하지 않았다.
- `T-WIP-MAT-LABEL-RAW-PROCESS` 완료 후 REVIEW 상태. `/production/wip-material-stock` 한국어 메뉴/화면 제목은 `원자재공정재고`, `/production/wip-material-trans`는 `원자재공정수불`로 변경했다. 라우트/메뉴코드/DB 메뉴는 변경하지 않았다. 검증: 신규 구조 테스트 RED→GREEN, menu locale coverage, FE tsc, 3002 HTTP 200, Playwright DOM 제목/console error 0, diff check PASS. 커밋하지 않았다.
- `T-IQC-PART-SPEC-AQL-SUMMARY` 완료 후 REVIEW 상태. `/master/iqc-part-spec` 우측 상단에 선택 품목 AQL 요약을 추가했다. 품목마스터 응답의 검사수준/기본시료수/Critical AQL/Major AQL/Minor AQL을 표시하고, `LOT 수량 미리보기` 값으로 `/quality/aql/resolve-iqc`를 호출해 샘플수량 및 Major/Minor Ac/Re를 보여준다. 기존 검사항목 저장 흐름은 유지. 검증: 신규 구조 테스트 RED→GREEN, 기존 layout 구조 테스트 PASS, FE tsc PASS, 3002 HTTP 200, diff check PASS. 커밋하지 않았다.
- `T-IQC-DEFECT-CODE-SEVERITY-AQL` 완료 후 REVIEW 상태. IQC 불량코드 등급은 `COM_CODES.DEFECT_GRADE` 전용 컬럼으로 관리하고, `DEFECT_TYPE`에는 등급 필수/허용값 체크 제약이 있다. `/material/iqc`는 불량코드+수량을 저장 payload `defects`로 보내며, 서버가 Critical/Major/Minor로 집계해 Critical 즉시 FAIL, Major/Minor 독립 Ac/Re 판정을 수행한다. JSHANES 기존 12개 불량코드는 등급 백필 완료. 검증: backend AQL/IQC spec 26/26, IQC modal structure 3/3, BE/FE tsc, ERD 재생성, DB 컬럼/제약/코드 조회, diff check PASS. 커밋하지 않았다.
- `T-SHIP-CONFIRM-CARD-REMOVE` 완료 후 REVIEW 상태. `/shipping/confirm` 상단 `StatCard` 정보카드는 제거됐고 구조 테스트/FE tsc/diff check는 통과했다. 3002 서버는 node PID `55728`이 listen 중이나 HTTP 요청이 30초 타임아웃됐으므로, 필요하면 서버 상태를 복구한 뒤 브라우저 미표시만 재확인한다.
- `T-SHIP-CONFIRM-BOXSCAN-ORDERNO` 완료 후 REVIEW 상태. `/shipping/confirm` 박스스캔출하 모달은 출하지시 조회 후 요약 영역에 `order.shipOrderNo`를 큰 monospace 값으로 보여준다. 구조 테스트/FE tsc/3002 Playwright 검증 완료. 커밋하지 않았다.
- `T-SHIP-BOX-STOCK-CARD-REMOVE` 완료 후 REVIEW 상태. `/shipping/box-stock`은 `Card/CardContent` wrapper 없이 일반 레이아웃으로 박스 목록/개별제품 DataGrid를 표시한다. 구조 테스트와 FE tsc, 3002 HTTP/Playwright 검증 완료. 커밋하지 않았다.
- `T-MENU-LOCALE-MISSING` 완료 후 REVIEW 상태. 신규 구조 테스트가 `menuConfig.ts`의 모든 `labelKey`를 ko/en/zh/vi locale에서 검증한다. 현재 누락이던 `menu.material.shelfLifeHistory`는 en/zh/vi에 추가 완료했고 focused test/JSON parse PASS. 커밋하지 않았다.
- `T-SHIP-OQC-GATE-OFF` 완료 후 REVIEW 상태. JSHANES `40/1000`에서 기존 API `PATCH /api/system/configs/OQC_ENABLED`로 `CONFIG_VALUE=N` 처리했다. DB와 `/api/system/configs/active` 모두 `OQC_ENABLED=N` 확인. 코드 변경 없음, 실제 출하 처리 미수행.
- `T-SHIP-ORDER-AUTO-NO` 완료 후 REVIEW 상태. `/shipping/order` 신규 등록은 이제 `shipOrderNo`를 전송하지 않고 서버가 `NumberingService.nextShipmentNo()`로 자동 채번한다. 3003 API 실측에서 `SH2606190001` 발급 후 삭제/DB 잔여 0건을 확인했고, 3002 브라우저 등록 모달은 `출하지시번호=자동생성`, disabled true, console error 0이다. 검증: backend RED/GREEN, frontend 구조 RED/GREEN, `ship-order.service.spec.ts`, BE/FE tsc, `git diff --check`.
- `T-SHIP-SO999-APPROVE` 완료 후 REVIEW 상태. JSHANES `40/1000`에서 `SO-20260619-999`를 기존 API로 `CONFIRMED` 처리했고, OQC 게이트가 켜져 있어 연결 박스 `BX2606190002`의 `OQC-20260619-001`도 기존 API로 `PASS` 처리했다. 최종 조건: SO `CONFIRMED`, 박스 `CLOSED/PASS`, 팔레트 미적재, `FG_MAIN/HNS02` 가용수량 10. 실제 `ship-box` 출하는 수행하지 않았다.
- `T-EQUIP-INSPECT-WORKER-PK-COLLISION` 완료 후 REVIEW 상태. `/equipment/daily-inspect` WORKER 저장이 `EQUIP_INSPECT_LOGS` 기존 물리 PK(`EQUIP_CODE`, `INSPECT_TYPE`, `INSPECT_DATE`)와 충돌하던 문제를 보정했다. JSHANES `EQ-ATCNS-01`/`WORKER`/2026-06-19 날짜 00:00:00 충돌 로그 2건(`WO2606190100`, `WO2606190118`)을 삭제했고 최종 잔여 0건을 확인했다. 서비스는 WORKER 저장만 raw SQL `TO_DATE(:3, 'YYYY-MM-DD HH24:MI:SS')`로 실제 점검시각을 PK에 보존하고 DAILY 경로는 유지한다. 검증: RED/GREEN focused test, `pnpm --filter @harness/backend test -- equip-inspect.service.spec.ts`, backend tsc, diff check PASS.
- `T-IQC-HISTORY-CERT-TIMESTAMP` 완료 후 REVIEW 상태. `/material/iqc-history` 성적서 업로드가 ISO UTC inspectDate(`2026-06-19T07:56:27.354Z`)를 Oracle 로컬 TIMESTAMP(`2026-06-19 16:56:27.354`)와 exact match하지 못해 404 나는 문제를 보정했다. `uploadCert()`는 기존 `findOne(Date)` 실패 시 Asia/Seoul timestamp 문자열 + `TO_TIMESTAMP` QueryBuilder fallback으로 조회/업데이트한다. 검증: focused Jest `-t "성적서 업로드"`, backend tsc, diff check PASS. 실제 더미 업로드는 DB/파일 변경이라 생략.
- `T-ISSUE-REQUEST-BARCODE-VALIDATION` 완료 후 REVIEW 상태. 웹 요청출고 API `IssueRequestService.issueFromRequest()`가 실제 출고 생성 전에 요청항목 `ITEM_CODE`와 스캔 LOT `ITEM_CODE`를 대조하도록 보정했다. 불일치 시 `MatIssueService.createInTx()` 호출 전 차단한다. 검증: 회귀 테스트 RED→GREEN, `pnpm --filter @harness/backend test -- issue-request.service.spec.ts`, backend tsc, JSHANES 완료 요청 품목 불일치 0건. 현재 JSHANES `40/1000`에는 APPROVED/REQUESTED 출고요청이 없어 브라우저 실출고 재현은 하지 않았다.
- `T-IQC-AQL-CRUD` 완료 후 REVIEW 상태. 기준관리 DB/API/UI는 구현 완료했고 JSHANES 검증 데이터는 삭제했다. 남은 후속은 IQC 품목별 AQL 기준 연결과 IQC 검사 입력 시 lotQty 기준 sampleSize/Ac/Re 적용이다.
- `T-IQC-AQL-PLAN`은 REVIEW 상태. 사용자 리뷰 후 구현 진행 시 `docs/superpowers/plans/2026-06-19-iqc-aql-implementation.md`를 기준으로 TDD 단계별 실행한다. 현재 워크트리에는 기존 무관 변경 `apps/backend/src/modules/material/services/iqc-history.service.ts`와 `.claude/worktrees/`가 있으므로 커밋 시 범위 선별이 필요하다.
- `T-MATERIAL-ARRIVAL-QTY-FORMAT` 완료 후 REVIEW 상태. `/material/arrival` 자재입하처리 모달의 입수량/계산식/예상 시리얼수 숫자 표시만 천단위 포맷으로 보정했고, 입력 필드 동작은 유지했다. 신규 구조 테스트와 FE tsc를 완료했고 커밋은 하지 않았다.
- `T-KIOSK-MOUNTED-RELOAD` 완료 후 REVIEW 상태. 키오스크 소모품 재조회는 선택 설비 기준 `CONSUMABLE_STOCKS` mounted 상태를 읽도록 보정했고, 3002 브라우저에서 mounted UID 2건 표시까지 확인했다. 커밋은 하지 않았다.
- `T-WIP-STOCK-ACTUAL-SQL` 완료 후 REVIEW 상태. `/production/wip-stock` SQL 미리보기는 이제 실제 `PRODUCT_STOCKS` 기반 조회 SQL과 화면 필터를 반영한다. 신규 구조 테스트, FE tsc, 3002 브라우저 SQL 모달 검증을 완료했고 커밋은 하지 않았다.
- `T-DATAGRID-HOVER-SCROLL-REMOVE` 완료 후 REVIEW 상태. 공용 `DataGrid`의 좌우 hover 자동 스크롤 핸들 기능을 제거했고 `ScrollHandle.tsx`를 삭제했다. 신규 구조 테스트, 제품 도면관리 구조 테스트, FE tsc, 3002 브라우저 DOM 검증을 완료했다. 커밋은 하지 않았다.
- `T-HARNESS-CIRCUIT-PAYLOAD` 완료 후 REVIEW 상태. 회로 저장 payload에서 엔티티 메타 필드를 제거해 `PUT /production/specifications/revisions/8` 400을 해결했고, Rev 생성은 prompt 대신 모달로 바꿔 취소 시 `/revise` 호출 0건을 확인했다. 커밋은 하지 않았다.
- `T-HARNESS-CONNECTION-SYMBOL` 완료 후 REVIEW 상태. `/production/specification-setup` 회로 그리드 `연결` 컬럼을 SVG 미리보기 + 선택 컨트롤로 바꿨고, 구조 테스트 RED/GREEN, FE tsc, 3002 브라우저 렌더링을 확인했다. 커밋은 하지 않았다.
- `T-HARNESS-DRAWING-SEED` 완료 후 REVIEW 상태. `HDW-SEED-HNS02-MAIN`, `HDW-SEED-HNS02-C1ABCD` seed를 JSHANES에 적용했고, 재실행성/API/3002 브라우저 표시를 확인했다. 검증 중 누락된 Revision 상세 GET 라우트와 화면 회로 상세 로딩도 보정했다. 커밋은 하지 않았다.
- `T-HARNESS-DRAWING-MGMT` 완료 후 REVIEW 상태. `/production/specification-setup` 제품 도면관리 신규 화면/API/DB를 추가했고, JSHANES 마이그레이션, ERD 재생성, API 생성-승인-Rev생성-삭제, 3002 브라우저 화면 검증을 완료했다. 테스트 데이터는 삭제해 잔여 0건. 커밋은 하지 않았다.
- `T-IQC-PART-SPEC-LEFT-PANEL` 완료 후 REVIEW 상태. `/master/iqc-part-spec` 좌측 품목 목록 패널은 12컬럼 중 4칸에서 3칸으로 줄이고 우측 규격 관리 패널은 9칸으로 넓혔다. 신규 구조 테스트 RED/GREEN, FE tsc, 3002 HTTP 200, diff check PASS. 커밋하지 않았다.
- `T-ER-VIEW-MENU-VISIBILITY` 완료 후 REVIEW 상태. `/system/er-view`가 메뉴에 안 보이던 원인은 `SYS_ER_VIEW`가 `menuConfig.ts` 외의 메뉴 소스에 누락된 것. `menu-code-validator.ts`와 `apps/backend/src/seeds/menu-config.json`에 추가했고, `apps/backend/src/migrations/2026-06-23_er_view_menu_seed.sql`을 JSHANES에 적용해 `MENU_CATEGORY_ITEMS`에 `SYSTEM` sort 95로 배치했다. DB post-check는 `SYS_ER_VIEW/SYSTEM/95/IS_ACTIVE=Y`. 기존 시스템 메뉴와 동일하게 `ROLE_MENU_PERMISSIONS`는 추가하지 않아 ADMIN 전체허용 메뉴로 노출된다. 검증: validator structure PASS, ER VIEW structure PASS, backend tsc PASS, JSON parse PASS, diff check PASS.
- `T-ER-VIEW-TABLE-NODES` 완료 후 REVIEW 상태. `/system/er-view` 그래프가 기본 ReactFlow 박스라 ERD처럼 보이지 않던 문제를 보정했다. backend graph node에 컬럼/PK/FK 후보 metadata를 추가하고, frontend는 `TableNode` custom node로 테이블명/주석/컬럼행/PK/FK badge를 표시한다. 화면 루트와 graph panel은 한 화면 고정 overflow-hidden 구조로 바꾸고 ReactFlow zoom/pan/fit controls + minimap을 명시했다. Chrome Playwright 인증 세션에서 `MAT_LOTS` 선택 시 table node 4개, edge 3개, controls 3개, minimap, body/html overflow 없음 확인. 커밋하지 않았다.

1. Read `AGENTS.md`.
2. Read `.ai-coordination/README.md`, `STATE.md`, `TASKS.md`, `DECISIONS.md`, and `LOCKS.md`.
3. Read `PROTOCOL.md` for conflicts, stale locks, broad changes, DB changes, or review handoff.
4. Claim files in `LOCKS.md` before editing.
5. Keep `TASKS.md` active-work-only.
6. Update `JOURNAL.md` and its own handoff file before stopping.

## Current Known Issues

- `T-PRODUCT-RECEIVE-CANCEL-RETRY` 완료 후 REVIEW 상태. 원인은 박스 완제품 입고가 `PRODUCT_TRANSACTIONS.WIP_OUT + REF_TYPE=BOX`로 기록되는데 `/product/receive`와 `/product/receipt-cancel`이 `FG_IN/WIP_IN` 계열만 조회해 실제 입고 전표를 숨기고 취소 대상에서 빠뜨린 것이다. 두 화면 모두 박스 전용 `WIP_OUT/WIP_OUT_CANCEL` 조회를 병합하고 수량 표시를 입고 관점으로 보정했다. 구조 테스트, FE tsc, diff check PASS. 3002 브라우저 실제 취소 클릭은 미검증.
- `T-ALL-MENU-QA` 진행 중. 전체 자동 스윕은 Next dev cold compile 때문에 장시간 소요되어 타임박스 방식으로 전환했다. `tools/hanes-all-menu-page-scenario-qa.mjs`에 `HANES_QA_BUDGET_MS`가 있으며, 예산 초과 시 현재까지 결과를 `PARTIAL`로 저장한다. 현재 frontend는 3002, backend는 3003 health 200으로 복구했다. `/material/receive-label`은 기본 `BROWSER` 출력 상태에서 선택하지 않은 `ZPL_USB`용 `localhost:9100` Zebra 체크가 즉시 발생하던 문제를 `useZebraPrinter(enabled)`로 보정했고, 단건 QA 재테스트 PASS. 추가 미실행 청크 02~04 총 15개도 PASS.
- `tools/hanes-all-menu-report-aggregate.mjs`를 추가했다. 현재 누적 요약은 `docs/reports/hanes-all-menu-scenario-qa-summary-2026-06-23/index.html` 및 `all-menu-summary.json`. menuConfig 기준 156개 중 PASS 107, FAIL 1, MISSING 48이다. 유일 FAIL은 `SHIP_RETURN /shipping/return`의 `GET /api/shipping/orders/shipped` 500이며 `T-SHIP-ORDER-CANCEL` active lock 범위라 수정하지 않았다. 다음 미실행 시작 후보는 `INV_TRANSACTION`, `INV_MAT_PHYSICAL_INV`, `INV_MAT_PHYSICAL_INV_APPLY`, `INV_MAT_PHYSICAL_INV_HISTORY`, `INV_ARRIVAL_STOCK`이다.
- 현재 PC의 agent 프린터 목록에는 `OneNote(데스크톱) - 보호됨`, `Microsoft Print to PDF`만 잡히며 Zebra 실출력 검증은 미완료. 트레이 아이콘 메뉴는 코드/빌드/API 실행 경로까지 확인했고 실제 우클릭 메뉴 육안 검증은 남아 있음.
- agent 설정 파일은 `C:\Users\hsyou\AppData\Roaming\HANES\print-agent\config.json`이며 현재 기본 프린터는 `Microsoft Print to PDF`로 저장돼 있다.
- `T-CONSUMABLE-LABEL-REPRINT`는 웹 팝업 인쇄 흐름과 print-log API까지 검증했다. 실제 Zebra 물리 출력은 프린터 부재로 미검증.
- `T-CONSUMABLE-LIFE-LARGE-INFO-CARDS` 완료. `/consumables/life` 상단 작은 상태 배지 4개를 큰 요약 정보카드 4개로 변경했다. 각 카드는 118px 이상 높이, 큰 수치, 상태별 아이콘/색상 톤을 가진다. 구조 테스트/FE tsc/diff check/3002 HTTP 200 확인. Playwright DOM 자동 검증은 루트에서 `@playwright/test` require 불가로 생략. commit/push 안 함.
- `T-CONSUMABLE-LABEL-ONE-LINE-STATUS` 완료. `/consumables/label` UID 발행 상태/결과 배너를 제거하고 헤더 우측 고정 폭 한 줄 상태로 축소했다. 그리드 toolbar 검색 input 옆에 `카테고리 필터` Select를 추가해 실제 마스터 `category` 값 기준으로 고정 필터링한다. 카테고리 변경 시 숨은 선택값은 초기화한다. 구조 테스트/FE tsc/diff check 통과. commit/push 안 함.
- `T-CONSUMABLE-LABEL-HIDDEN-IFRAME-PRINT` 완료. `/consumables/label` UID 발행 시 새 탭/전체 화면으로 전환하지 않고 숨김 iframe에 라벨 HTML을 주입해 `contentWindow.print()`를 호출한다. 3014 mock 브라우저에서 `window.openCalled=0`, iframe 0x0/opacity 0 숨김 상태, `iframePrintCalled=1` 확인. commit/push 안 함.
- `T-CONSUMABLE-LABEL-ACTUAL-PRINT` 완료. `/consumables/label` UID 발행 시 이제 클릭 즉시 인쇄창을 먼저 열고, UID 생성 후 그 창에 라벨 HTML을 쓰며 `window.print()`를 호출한다. API 지연 mock에서 `window.open`이 API 요청보다 먼저 호출됨을 확인했고, 실제 API 발행 `C26061700019`의 출력 HTML에도 `window.print()`가 포함됨을 확인했다. 테스트 데이터는 `CONSUMABLE_STOCKS`/`LABEL_PRINT_LOGS`에서 삭제해 잔여 0건. commit/push 안 함.
- `T-CONSUMABLE-LABEL-503-FEEDBACK` 완료. `/consumables/label` UID 발행 API가 503을 반환해도 AxiosError 객체를 console.error로 그대로 출력하지 않고, 서버 메시지를 toast/화면 배너에 표시한다. 3014 dev 서버에서 503 mock 검증 완료. 직접 API 테스트로 생성된 `C26061700017`은 `CONSUMABLE_STOCKS`/`LABEL_PRINT_LOGS`에서 삭제해 잔여 0건 확인. commit/push 안 함.
- `T-CONSUMABLE-LABEL-ISSUE-FEEDBACK` 완료. `/consumables/label` UID 발행 버튼 클릭 시 발행중/출력중/완료/실패 toast와 화면 배너가 표시되고, 성공 후 발행 UID가 화면에 남는다. 3014 dev 브라우저 mock 검증 및 실제 API 발행 `C26061700016` 검증 완료. 테스트로 생성된 `CONSUMABLE_STOCKS` 1건과 `LABEL_PRINT_LOGS` 2건은 삭제했고 JSHANES 잔여 0건 확인. 사용자 지시에 따라 commit/push는 하지 않았다.
- `T-SHIPPING-PACK-EMPTY-BOX-DELETE` 완료. `/shipping/pack` 행 액션은 4개 고정 아이콘 슬롯(제품 담기/마감·재오픈/라벨/빈 박스 삭제)으로 정렬했고, 빈 OPEN 박스만 삭제 확인 모달을 통해 `DELETE /shipping/boxes/:boxNo`를 호출한다. 제품 담기 모달에는 `현재 담는 박스`와 박스번호를 크게 표시하고 해당 행을 하이라이트한다. 구조 테스트, frontend tsc, 3002 브라우저 DOM, API 빈 박스 생성/삭제 검증 완료.
- `T-SYSTEM-LABEL-MENU-RENAME` 완료. `MST_LABEL` 메뉴 labelKey `menu.master.label` 한글 값을 요청 표기 그대로 `라벨다자인관리`로 변경했다. 페이지 제목 `label.title`은 유지.
- `T-CONSUMABLE-LABEL-PRINTLOG-PAYLOAD` 완료. `/consumables/label` 브라우저 인쇄 후 공용 `/material/label-print/log` 호출 payload를 `uidList`로 수정했다. 실제 3002에서 첫 행 선택 후 `UID 발행` 시 create 201, print-log 201 확인. 검증 UID `C26061700007` 관련 stock/log는 삭제 후 잔여 0.
- `T-CONSUMABLE-LABEL-IMAGE-PRINTLOG` 완료. `/consumables/label` 그리드에 소모품 이미지 37개 표시 확인. `POST /consumables/label/create`의 `LABEL_PRINT_LOGS.PRINTED_AT` null 오류는 `printedAt`/`seq` 명시로 수정했고 실제 API 성공 확인. 검증용 UID `C26061700004`와 로그는 삭제해 잔여 0.
- `T-EQUIPMENT-INSPECT-HISTORY-ACTUAL-SQL` 완료. `/equipment/inspect-history`의 SQL 보기 preview가 실제 조회 테이블 `EQUIP_INSPECT_LOGS`/`EQUIP_MASTERS`를 포함하므로 전역 SQL 디버그 캐시와 매칭된다. 3002 브라우저 모달에서 실제 SELECT와 bind 변수 표시 확인. 이번 작업 외 기존 dirty file: `apps/backend/src/modules/equipment/dto/equip-inspect.dto.ts`, `apps/backend/src/modules/equipment/services/equip-inspect.service.ts`, `.claude/worktrees/`.
- `T-UI-CRUD-RED-MENU-QA`, `T-MENU-QA-DETAIL-REPORT`, `T-MASTER-PART-PAGE-SCENARIO-QA`, `T-MASTER-BOM-PAGE-SCENARIO-QA`, `T-MASTER-REMAINING-PAGE-SCENARIO-QA`는 완료되어 active task와 lock에서 제거했다. 프론트 3002는 이번 작업 중 응답 불가 상태의 PID 2796을 정리하고 `apps/frontend`에서 `pnpm run dev`로 재시작했다. 현재 확인된 프론트 3002 PID는 17636이고 백엔드 3003은 API 200으로 확인됐다.
- `T-MASTER-PART-PAGE-STANDARD` 문서 작업은 완료했다. 새 문서 `docs/standards/master-part-page-standard.md`를 추가했고 `docs/standards/ui-screen-patterns.md`에 링크를 넣었다. `/master/part` 실제 화면 기준으로 상단 액션, 12px 배지, 아이콘 중심 행 액션, 우측 슬라이드 패널, DB 컬럼 툴팁, 유지보수 체크리스트를 정리했다. `git diff --check` PASS, 추가 코드 변경 없음.
- `T-MASTER-EQUIP-IMAGE-UPLOAD` 완료. 설비마스터에 `IMAGE_URL` 컬럼을 추가하고 `/equipment/equips/:id/image` 업로드/삭제 API, `/master/equip` 우측 패널 사진 업로드/미리보기 UI를 품목마스터 패턴과 맞췄다. JSHANES 컬럼/주석 적용, `docs/reports/db-schema-erd.md` 재생성, backend/frontend typecheck, focused Jest, 구조 테스트, diff check PASS. 다음 작업자는 현재 남아 있는 다른 active lock을 건드리지 말고 각 범위만 분리해서 이어가면 된다.
